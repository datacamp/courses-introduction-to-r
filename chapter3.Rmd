---
title_meta: 챕터 3
title: 행렬
description: >-
  이 장에서는 R에서 행렬을 다루는 방법을 배웁니다. 장이 끝날 무렵에는 행렬을 직접 생성하고 기본적인 연산을 수행할 수 있게 됩니다.
  스타워즈 영화의 박스오피스 수익 데이터를 분석하며 R에서 행렬을 활용하는 방법을 익혀 보세요. 포스가 함께하길!
---

## 행렬이란 무엇인가요?

```yaml
type: NormalExercise
key: d61aeba84c
xp: 100
skills:
  - 1
```

R에서 행렬은 동일한 데이터 유형(숫자형, 문자형, 또는 논리형)의 요소들이 고정된 행과 열로 배열된 구조입니다. 행과 열로만 구성되어 있기 때문에 행렬은 2차원이라고 불립니다.

R에서는 `matrix()` 함수를 사용해 행렬을 만들 수 있습니다. 다음 예시를 살펴보세요:

```
matrix(1:9, byrow = TRUE, nrow = 3)
```

`matrix()` 함수의 인수는 다음과 같습니다:

- 첫 번째 인수는 행렬의 행과 열에 배치할 요소들의 집합입니다. 여기서는 `1:9`의 축약 표현인 `c(1, 2, 3, 4, 5, 6, 7, 8, 9)`를 사용합니다.
- `byrow` 인수는 행렬을 행 방향으로 채울지 여부를 지정합니다. 열 방향으로 채우려면 `byrow = FALSE`로 설정하면 됩니다.
- 세 번째 인수인 `nrow`는 행렬의 행 개수를 지정합니다. 여기서는 3개의 행을 만듭니다.

`@instructions`
1부터 9까지의 숫자를 행 방향으로 채워 3행짜리 행렬을 만들어 보세요.

`@hint`
지침을 꼼꼼히 읽어 보세요. 답이 이미 나와 있답니다!

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Construct a matrix with 3 rows that contain the numbers 1 up to 9
```

`@solution`
```{r}
# Construct a matrix with 3 rows that contain the numbers 1 up to 9
matrix(1:9, byrow = TRUE, nrow = 3)
```

`@sct`
```{r}
ex() %>% check_function("matrix") %>% {
	check_arg(., 'data') %>% check_equal(incorrect_msg = "행렬을 올바르게 생성하셨나요? 과제를 확인해 보세요. 답이 이미 주어져 있습니다!")
	check_arg(., 'byrow') %>% check_equal(incorrect_msg = "행렬을 올바르게 생성하셨나요? 과제를 확인해 보세요. 답이 이미 주어져 있습니다!")
	check_arg(., 'nrow') %>% check_equal(incorrect_msg = "행렬을 올바르게 생성하셨나요? 과제를 확인해 보세요. 답이 이미 주어져 있습니다!")
	}

ex() %>% check_output_expr("matrix(1:9, byrow=TRUE, nrow=3)",missing_msg = "행렬 정의에 문제가 있는 것 같습니다. 과제를 확인해 보세요. 답이 이미 주어져 있습니다!")

success_msg("훌륭합니다! 다음 연습으로 계속하세요.")
```

---

## 행렬을 분석하세요

```yaml
type: NormalExercise
key: effc2fb945
xp: 100
skills:
  - 1
```

이제 본격적으로 실습해 볼 시간입니다. 다음 연습 문제에서는 스타워즈 시리즈의 박스오피스 수익 데이터를 분석합니다. 포스가 함께하길!

편집기에는 세 개의 벡터가 정의되어 있습니다. 각 벡터는 스타워즈 첫 세 편의 박스오피스 수익을 나타냅니다. 각 벡터의 첫 번째 요소는 미국 내 수익, 두 번째 요소는 미국 외 수익을 나타냅니다(출처: Wikipedia).

이 연습 문제에서는 이 수치들을 하나의 벡터로 합친 다음, 그 벡터로 행렬을 만들어 봅니다.

`@instructions`
- `c(new_hope, empire_strikes, return_jedi)`를 사용하여 세 벡터를 하나의 벡터로 합치세요. 이 벡터의 이름을 `box_office`로 지정합니다.
- `matrix()` 함수를 사용하여 행이 3개인 행렬을 만드세요. 각 행은 영화 한 편을 나타냅니다. 첫 번째 인수로 모든 박스오피스 수익이 담긴 벡터 `box_office`를 입력하고, `nrow = 3`과 `byrow = TRUE`를 지정합니다. 결과 행렬의 이름을 `star_wars_matrix`로 지정합니다.

`@hint`
- `box_office <- c(new_hope, empire_strikes, return_jedi)`를 사용하면 각 벡터의 숫자를 합쳐 총 6개의 요소로 이루어진 하나의 벡터를 만들 수 있습니다.
- `matrix(box_office, nrow = ..., byrow =  ...)` 는 두 번째 지침의 답을 작성할 때 사용할 템플릿입니다.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Box office Star Wars (in millions!)
new_hope <- c(460.998, 314.4)
empire_strikes <- c(290.475, 247.900)
return_jedi <- c(309.306, 165.8)

# Create box_office
box_office <- 

# Construct star_wars_matrix
star_wars_matrix <- 
```

`@solution`
```{r}
# Box office Star Wars (in millions!)
new_hope <- c(460.998, 314.4)
empire_strikes <- c(290.475, 247.900)
return_jedi <- c(309.306, 165.8)

# Create box_office
box_office <- c(new_hope, empire_strikes, return_jedi)

# Construct star_wars_matrix
star_wars_matrix <- matrix(box_office, nrow = 3, byrow = TRUE) 
```

`@sct`
```{r}
msg <- "박스 오피스 변수 `new_hope`, `empire_strikes`, `return_jedi`에 대해 아무것도 변경하지 마십시오!"

ex() %>% check_object("new_hope", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("empire_strikes", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("return_jedi", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("box_office") %>% check_equal(incorrect_msg = "`new_hope`, `empire_strikes`, `return_jedi`의 값을 벡터 `box_office`로 올바르게 결합하셨습니까?")

ex() %>% check_function("matrix") %>% {
	check_arg(., 'data') %>% check_equal(incorrect_msg = "`matrix()`에 전달하는 인수를 올바르게 지정했는지 확인하십시오: `box_office`, `nrow = 3`, `by_row = TRUE`.")
	check_arg(., 'nrow') %>% check_equal(incorrect_msg = "`matrix()`에 전달하는 인수를 올바르게 지정했는지 확인하십시오: `box_office`, `nrow = 3`, `by_row = TRUE`.")
	check_arg(., 'byrow') %>% check_equal(incorrect_msg = "`matrix()`에 전달하는 인수를 올바르게 지정했는지 확인하십시오: `box_office`, `nrow = 3`, `by_row = TRUE`.")
	}

ex() %>% check_object("star_wars_matrix") %>% check_equal(incorrect_msg = "`matrix()` 호출 결과를 `star_wars_matrix`에 할당하셨습니까?")

success_msg("포스가 당신과 함께합니다! 다음 연습으로 계속하십시오.")
```

---

## 행렬 이름 지정하기

```yaml
type: NormalExercise
key: f734e8bf74
xp: 100
skills:
  - 1
```

`star_wars_matrix`에 어떤 데이터가 저장되어 있는지 쉽게 파악할 수 있도록, 각 행에 영화 제목을 추가하려고 합니다. 행 이름을 추가하면 데이터를 읽기도 편하고, 행렬에서 원하는 요소를 선택할 때도 유용합니다.

벡터와 마찬가지로, 행렬의 행과 열에도 이름을 추가할 수 있습니다.

```
rownames(my_matrix) <- row_names_vector
colnames(my_matrix) <- col_names_vector
```

두 개의 벡터 `region`과 `titles`가 미리 준비되어 있습니다. 각각 `star_wars_matrix`의 열과 행 이름을 지정하는 데 사용하세요.

`@instructions`
- `colnames()`를 사용하여 `star_wars_matrix`의 열 이름을 `region` 벡터로 지정하세요.
- `rownames()`를 사용하여 `star_wars_matrix`의 행 이름을 `titles` 벡터로 지정하세요.
- `star_wars_matrix`를 출력하여 결과를 확인하세요.

`@hint`
`colnames(star_wars_matrix) <- region`을 사용하면 `star_wars_matrix`의 열 이름을 지정할 수 있습니다. 같은 방법으로 행 이름도 지정해 보세요.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Box office Star Wars (in millions!)
new_hope <- c(460.998, 314.4)
empire_strikes <- c(290.475, 247.900)
return_jedi <- c(309.306, 165.8)

# Construct matrix
star_wars_matrix <- matrix(c(new_hope, empire_strikes, return_jedi), nrow = 3, byrow = TRUE)

# Vectors region and titles, used for naming
region <- c("US", "non-US")
titles <- c("A New Hope", "The Empire Strikes Back", "Return of the Jedi")

# Name the columns with region


# Name the rows with titles


# Print out star_wars_matrix
```

`@solution`
```{r}
# Box office Star Wars (in millions!)
new_hope <- c(460.998, 314.4)
empire_strikes <- c(290.475, 247.900)
return_jedi <- c(309.306, 165.8)

# Construct matrix
star_wars_matrix <- matrix(c(new_hope, empire_strikes, return_jedi), nrow = 3, byrow = TRUE)

# Vectors region and titles, used for naming
region <- c("US", "non-US")
titles <- c("A New Hope", "The Empire Strikes Back", "Return of the Jedi")

# Name the columns with region
colnames(star_wars_matrix) <- region

# Name the rows with titles
rownames(star_wars_matrix) <- titles

# Print out star_wars_matrix
star_wars_matrix
```

`@sct`
```{r}
msg <- "박스 오피스 변수 `new_hope`, `empire_strikes` 및 `return_jedi`에 대해 아무것도 변경하지 마십시오!"

ex()  %>% check_object("new_hope", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex()  %>% check_object("empire_strikes", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex()  %>% check_object("return_jedi", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

msg <- "`star_wars_matrix`의 내용은 변경하지 마십시오; 행과 열의 이름만 변경하십시오!"

ex()  %>% check_object("star_wars_matrix") %>% check_equal(incorrect_msg = msg)

msg <- "정의된 `region` 및 `titles` 벡터에 대해 아무것도 변경하지 마십시오."

ex()  %>% check_object("region", undefined_msg = msg)  %>%  check_equal(incorrect_msg = msg)
ex() %>% check_object("titles", undefined_msg = msg) %>% check_equal( incorrect_msg = msg)

ex() %>% check_object("star_wars_matrix") %>% check_equal(eq_condition = "equal",incorrect_msg = "`star_wars_matrix`의 행과 열 이름을 올바르게 설정했습니까? 열 이름에는 `colnames(star_wars_matrix) <- region`을 사용하십시오; 행 이름을 지정하기 위해 유사한 작업을 수행하십시오.")

ex() %>% check_output_expr("star_wars_matrix", missing_msg = "행과 열의 이름을 지정한 후 `star_wars_matrix`를 출력하는 것을 잊지 마십시오.")

success_msg("훌륭합니다! R 제다이가 되는 길에 있습니다! 다음 연습으로 계속하십시오.")
```

---

## 전 세계 박스오피스 수익 계산하기

```yaml
type: NormalExercise
key: 3fd7499a12
xp: 100
skills:
  - 1
```

영화가 불멸의 명작으로 자리매김하기 위해 가장 중요한 것은 바로 전 세계 박스오피스 수익입니다. 

세 편의 스타워즈 영화의 총 박스오피스 수익을 계산하려면 미국 수익 열과 미국 외 수익 열의 합계를 구해야 합니다. 

R에서는 `rowSums()` 함수를 사용하면 행렬의 각 행 합계를 편리하게 계산할 수 있습니다. 이 함수는 새로운 벡터를 생성합니다.

```
rowSums(my_matrix)
```

`@instructions`
세 편의 영화에 대한 전 세계 박스오피스 수익을 계산하고, 그 결과를 `worldwide_vector`라는 벡터에 저장하세요.

`@hint`
`rowSums(star_wars_matrix)`를 사용하면 각 행의 합계, 즉 영화별 총 박스오피스 수익을 계산할 수 있습니다.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Construct star_wars_matrix
box_office <- c(460.998, 314.4, 290.475, 247.900, 309.306, 165.8)
region <- c("US", "non-US")
titles <- c("A New Hope", 
                 "The Empire Strikes Back", 
                 "Return of the Jedi")
               
star_wars_matrix <- matrix(box_office, 
                      nrow = 3, byrow = TRUE,
                      dimnames = list(titles, region))

# Calculate worldwide box office figures
worldwide_vector <- 
```

`@solution`
```{r}
# Construct star_wars_matrix
box_office <- c(460.998, 314.4, 290.475, 247.900, 309.306, 165.8)
region <- c("US", "non-US")
titles <- c("A New Hope", 
            "The Empire Strikes Back", 
            "Return of the Jedi")
               
star_wars_matrix <- matrix(box_office, 
                      nrow = 3, byrow = TRUE,
                      dimnames = list(titles, region))

# Calculate worldwide box office figures
worldwide_vector <- rowSums(star_wars_matrix)
```

`@sct`
```{r}
msg <- "사전 설정된 변수 `box_office_all` 및 `star_wars_marix`에 대해 아무것도 변경하지 마십시오!"

ex() %>% check_object("box_office", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("star_wars_matrix", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("worldwide_vector") %>% check_equal(incorrect_msg = "`rowSums()`를 `star_wars_matrix`에 호출하고 결과를 `worldwide_vector`에 저장하십시오.")

success_msg("잘하셨습니다! 다음 연습으로 계속하십시오.")
```

---

## 전 세계 박스오피스 열 추가하기

```yaml
type: NormalExercise
key: 86b87a8545
xp: 100
skills:
  - 1
```

이전 연습 문제에서 세 편의 스타워즈 영화의 전 세계 박스오피스 수익을 담은 벡터를 계산했습니다. 그런데 이 벡터는 아직 `star_wars_matrix`에 포함되어 있지 않습니다.

`cbind()` 함수를 사용하면 행렬에 하나 또는 여러 개의 열을 추가할 수 있습니다. 이 함수는 행렬과 벡터를 열 방향으로 합쳐 줍니다. 예를 들면 다음과 같습니다.

```
big_matrix <- cbind(matrix1, matrix2, vector1 ...)
```

`@instructions`
`worldwide_vector`를 `star_wars_matrix`의 새로운 열로 추가하고, 결과를 `all_wars_matrix`에 할당하세요. `cbind()` 함수를 사용하면 됩니다.

`@hint`
이 연습 문제에서는 `cbind()`에 두 개의 변수를 전달해야 합니다. `star_wars_matrix`와 `worldwide_vector`를 순서대로 넣으세요. 결과를 `all_wars_matrix`에 할당하세요.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Construct star_wars_matrix
box_office <- c(460.998, 314.4, 290.475, 247.900, 309.306, 165.8)
region <- c("US", "non-US")
titles <- c("A New Hope", 
            "The Empire Strikes Back", 
            "Return of the Jedi")
               
star_wars_matrix <- matrix(box_office, 
                      nrow = 3, byrow = TRUE,
                      dimnames = list(titles, region))

# The worldwide box office figures
worldwide_vector <- rowSums(star_wars_matrix)

# Bind the new variable worldwide_vector as a column to star_wars_matrix
all_wars_matrix <- 
```

`@solution`
```{r}
# Construct star_wars_matrix
box_office <- c(460.998, 314.4, 290.475, 247.900, 309.306, 165.8)
region <- c("US", "non-US")
titles <- c("A New Hope", 
            "The Empire Strikes Back", 
            "Return of the Jedi")
               
star_wars_matrix <- matrix(box_office, 
                      nrow = 3, byrow = TRUE,
                      dimnames = list(titles, region))

# The worldwide box office figures
worldwide_vector <- rowSums(star_wars_matrix)

# Bind the new variable worldwide_vector as a column to star_wars_matrix
all_wars_matrix <- cbind(star_wars_matrix, worldwide_vector)
```

`@sct`
```{r}
msg <- "사전 설정된 변수 `box_office_all` 및 `star_wars_marix`에 대해 아무것도 변경하지 마십시오!"
ex() %>% check_object("box_office", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("star_wars_matrix", undefined_msg = msg) %>% check_equal(, incorrect_msg = msg)

ex() %>% check_object("worldwide_vector") %>% check_equal(incorrect_msg = "`rowSums(star_wars_matrix)`의 결과를 `worldwide_vector`에 저장하십시오.")

msg <- "`cbind()`를 사용하여 `worldwide_vector`를 `star_wars_matrix`에 올바르게 추가했습니까? `cbind()`에 `star_wars_matrix`와 `world_wide_vector`를 이 순서로 전달해야 합니다. 결과 행렬 `all_wars_matrix`는 세 개의 행과 세 개의 열로 구성되어야 합니다."
ex() %>% check_object("all_wars_matrix") %>% check_equal(incorrect_msg = msg)

success_msg("잘하셨습니다! 행렬에 열을 추가한 후에는 논리적으로 다음 단계는 행을 추가하는 것입니다. 다음 연습에서 배우십시오.");
```

---

## 행 추가하기

```yaml
type: NormalExercise
key: bcadb29139
xp: 100
skills:
  - 1
```

모든 작용에 반작용이 있듯, `cbind()`에는 `rbind()`가 있습니다. (비유가 좀 어색하다는 건 저희도 압니다.)

지금까지 정의한 모든 변수가 저장되는 R 작업 공간([작업 공간이 무엇인지 알아보기](http://www.statmethods.net/interface/workspace.html))에는 이미 두 개의 행렬이 초기화되어 있습니다:

- `star_wars_matrix`: 지금까지 사용해 온 행렬로, 오리지널 3부작 데이터를 담고 있습니다.
- `star_wars_matrix2`: 프리퀄 3부작에 대한 유사한 데이터를 담고 있습니다.

각 행렬을 자세히 살펴보고 싶다면 콘솔에서 직접 확인해 보세요. 작업 공간의 내용을 확인하려면 콘솔에 `ls()`를 입력하면 됩니다.

`@instructions`
`rbind()`를 사용해 `star_wars_matrix`와 `star_wars_matrix2`를 이 순서대로 합치세요. 결과로 만들어진 행렬을 `all_wars_matrix`에 할당하세요.

`@hint`
다음과 같이 두 행렬을 합쳐 보세요:
```
rbind(matrix1, matrix2)
```
결과를 `all_wars_matrix`에 할당하세요.

`@pre_exercise_code`
```{r}
# Construct matrix
box_office_all <- c(461, 314.4, 290.5, 247.9, 309.3, 165.8)
movie_names <- c("A New Hope","The Empire Strikes Back","Return of the Jedi")
col_titles <- c("US","non-US")
star_wars_matrix <- matrix(box_office_all, nrow = 3, byrow = TRUE, dimnames = list(movie_names, col_titles))

# Construct matrix2
box_office_all2 <- c(474.5, 552.5, 310.7, 338.7, 380.3, 468.5)
movie_names2 <- c("The Phantom Menace", "Attack of the Clones", "Revenge of the Sith")
star_wars_matrix2 <- matrix(box_office_all2, nrow=3, byrow = TRUE, dimnames = list(movie_names2, col_titles))

# remove all except all_wars_matrix
rm(box_office_all)
rm(movie_names)
rm(col_titles)
rm(box_office_all2)
rm(movie_names2)
```

`@sample_code`
```{r}
# star_wars_matrix and star_wars_matrix2 are available in your workspace
star_wars_matrix  
star_wars_matrix2 

# Combine both Star Wars trilogies in one matrix
all_wars_matrix <- 
```

`@solution`
```{r}
# star_wars_matrix and star_wars_matrix2 are available in your workspace
star_wars_matrix  
star_wars_matrix2 

# Combine both Star Wars trilogies in one matrix
all_wars_matrix <- rbind(star_wars_matrix, star_wars_matrix2)
```

`@sct`
```{r}
msg = "워크스페이스에 정의된 변수(`star_wars_matrix` 및 `star_wars_matrix2`)를 재정의하지 마십시오."
ex() %>% check_object("star_wars_matrix",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("star_wars_matrix2", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("all_wars_matrix") %>% check_equal(incorrect_msg = "`rbind()`를 사용하여 `all_wars_matrix()`를 올바르게 생성하셨습니까? `rbind()`는 두 개의 인수를 가져야 합니다: `star_wars_matrix` 및 `star_wars_matrix2`, 이 순서로.")

success_msg("멋집니다! 다음 연습을 계속 진행하여 `rbind()` 함수의 결과를 `colSums()` 함수와 어떻게 결합할 수 있는지 확인해 보세요!")
```

---

## 전체 시리즈의 총 박스오피스 수익

```yaml
type: NormalExercise
key: 1bfe5ae096
xp: 100
skills:
  - 1
```

`cbind()`에 `rbind()`가 있듯이, `colSums()`에는 `rowSums()`가 있습니다. 이전 연습 문제에서 만든 `all_wars_matrix`가 R 작업 공간에 이미 저장되어 있습니다. `all_wars_matrix`를 입력해서 다시 한번 확인해 보세요. 이제 전체 시리즈의 총 박스오피스 수익을 계산해 봅시다.

`@instructions`
- 미국과 미국 외 지역의 총 수익을 계산하고 `total_revenue_vector`에 할당하세요. `colSums()` 함수를 사용하면 됩니다.
- `total_revenue_vector`를 출력해서 결과를 확인해 보세요.

`@hint`
지역별 총 박스오피스 수익을 구하려면 `colSums()` 함수에 `star_wars_matrix`를 인수로 사용하세요.

`@pre_exercise_code`
```{r}
load(url("https://assets.datacamp.com/course/intro_to_r/all_wars_matrix.RData"))
```

`@sample_code`
```{r}
# all_wars_matrix is available in your workspace
all_wars_matrix

# Total revenue for US and non-US
total_revenue_vector <- 
  
# Print out total_revenue_vector
```

`@solution`
```{r}
# all_wars_matrix is available in your workspace
all_wars_matrix

# Total revenue for US and non-US
total_revenue_vector <- colSums(all_wars_matrix)

# Print out total_revenue_vector
total_revenue_vector
```

`@sct`
```{r}
msg = "`all_wars_matrix`의 내용을 변경하지 마십시오. 작업 공간에 이미 생성되어 있습니다."
ex() %>% check_object("all_wars_matrix", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_function("colSums") %>% check_arg('x') %>% check_equal(incorrect_msg = "`colSums()` 함수를 `all_wars_matrix`에 사용하셨습니까?")

ex() %>% check_object("total_revenue_vector") %>% check_equal(incorrect_msg = "`colSums(all_wars_matrix)`의 결과를 `total_revenue_vector`에 올바르게 할당하셨습니까?")

ex() %>% check_output_expr("total_revenue_vector", missing_msg = "`total_revenue_vector`를 출력하는 것을 잊지 마십시오!")

success_msg("아주 좋습니다! 다음 연습으로 넘어가서 행렬 서브셋팅을 배우십시오.")
```

---

## 행렬 요소 선택하기

```yaml
type: NormalExercise
key: 41d9d69713
xp: 100
skills:
  - 1
```

벡터와 마찬가지로, 행렬에서도 대괄호 `[ ]`를 사용해 하나 또는 여러 요소를 선택할 수 있습니다. 벡터는 1차원이지만, 행렬은 2차원입니다. 따라서 쉼표를 사용해 선택할 행과 열을 구분해야 합니다. 예를 들면 다음과 같습니다:

- `my_matrix[1,2]`는 1행 2열의 요소를 선택합니다.
- `my_matrix[1:3,2:4]`는 1, 2, 3행과 2, 3, 4열의 데이터로 이루어진 행렬을 반환합니다.

특정 행이나 열의 모든 요소를 선택하려면 쉼표 앞이나 뒤에 숫자를 입력하지 않으면 됩니다:

- `my_matrix[,1]`은 첫 번째 열의 모든 요소를 선택합니다.
- `my_matrix[1,]`은 첫 번째 행의 모든 요소를 선택합니다.

이제 새로 익힌 지식을 활용해 스타워즈로 돌아가 봅시다! 이전 연습 문제와 마찬가지로, `all_wars_matrix`는 이미 워크스페이스에 불러와져 있습니다.

`@instructions`
- 모든 영화의 미국 외 수익(`all_wars_matrix`의 두 번째 열 전체)을 선택하고, 결과를 `non_us_all`에 저장하세요.
- `mean()`에 `non_us_all`을 적용해 모든 영화의 평균 미국 외 수익을 계산하세요. 결과를 그대로 출력하면 됩니다.
- 이번에는 `all_wars_matrix`에서 처음 두 영화의 미국 외 수익을 선택하세요. 결과를 `non_us_some`에 저장하세요.
- `mean()`을 다시 사용해 `non_us_some`의 평균값을 출력하세요.

`@hint`
`my_matrix[,2]`를 사용하면 행렬 `my_matrix`의 두 번째 열 전체를 선택할 수 있습니다.

`@pre_exercise_code`
```{r}
load(url("https://assets.datacamp.com/course/intro_to_r/all_wars_matrix.RData"))
```

`@sample_code`
```{r}
# all_wars_matrix is available in your workspace
all_wars_matrix

# Select the non-US revenue for all movies
non_us_all <- 
  
# Average non-US revenue

  
# Select the non-US revenue for first two movies
non_us_some <- 
  
# Average non-US revenue for first two movies

```

`@solution`
```{r}
# all_wars_matrix is available in your workspace
all_wars_matrix

# Select the non-US revenue for all movies
non_us_all <- all_wars_matrix[,2]
  
# Average non-US revenue
mean(non_us_all)
  
# Select the non-US revenue for first two movies
non_us_some <- all_wars_matrix[1:2,2]
  
# Average non-US revenue for first two movies
mean(non_us_some)
```

`@sct`
```{r}
msg = "`all_wars_matrix`의 내용을 변경하지 마십시오. 이 행렬은 이미 작업 공간에 생성되어 있습니다."

ex() %>% check_object("all_wars_matrix", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("non_us_all") %>% check_equal(incorrect_msg = "`non_us_all`에 `all_wars_matrix`의 두 번째 열 전체를 할당하셨습니까? 이를 위해 `[, 2]`를 사용할 수 있습니다!")

ex() %>% check_output_expr("mean(non_us_all)", missing_msg = "`mean(non_us_all)`을 호출하여 `non_us_all`의 값의 평균을 계산하셨습니까? 결과를 단순히 출력하십시오.")

ex() %>% check_object("non_us_some") %>% check_equal(incorrect_msg = "`non_us_some`에 첫 두 영화의 비미국 수익을 할당하셨습니까? 이를 위해 `[1:2,2]`를 사용할 수 있습니다!")

ex() %>% check_output_expr("mean(non_us_some)", missing_msg = "`mean(non_us_some)`을 호출하여 `non_us_some`의 값의 평균을 계산하셨습니까? 결과를 단순히 출력하십시오.")

success_msg("잘하셨습니다! 다음 연습으로 계속하십시오.")
```

---

## 행렬로 간단한 산술 연산 해보기

```yaml
type: NormalExercise
key: c81c656f06
xp: 100
skills:
  - 1
```

벡터에서 배운 것과 마찬가지로, `+`, `-`, `/`, `*` 등의 표준 연산자는 R에서 행렬에 대해 요소별(element-wise)로 작동합니다.

예를 들어, `2 * my_matrix`는 `my_matrix`의 각 요소에 2를 곱합니다.

루카스필름의 신입 데이터 분석가로서 각 영화별로 지역별 관객 수를 파악해야 합니다. 총 수익 데이터는 이미 `all_wars_matrix`에 있습니다. 티켓 가격이 5달러라고 가정할 때, 박스오피스 수익을 티켓 가격으로 나누면 방문객 수를 구할 수 있습니다.

`@instructions`
- `all_wars_matrix`를 5로 나눠 방문객 수(단위: 백만 명)를 구하세요. 결과 행렬을 `visitors`에 할당하세요.
- `visitors`를 출력해서 결과를 확인하세요.

`@hint`
방문객 수는 `all_wars_matrix`를 5로 나눈 값입니다.

`@pre_exercise_code`
```{r}
load(url("https://assets.datacamp.com/course/intro_to_r/all_wars_matrix.RData"))
```

`@sample_code`
```{r}
# all_wars_matrix is available in your workspace
all_wars_matrix

# Estimate the visitors
visitors <- 
  
# Print the estimate to the console

```

`@solution`
```{r}
# all_wars_matrix is available in your workspace
all_wars_matrix

# Estimate the visitors
visitors <- all_wars_matrix / 5

# Print the estimate to the console
visitors
```

`@sct`
```{r}
msg = "`all_wars_matrix`의 내용을 변경하지 마십시오. 이 행렬은 이미 작업 공간에 생성되어 있습니다."

ex() %>% check_object("all_wars_matrix", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("visitors") %>% check_equal(incorrect_msg = "`visitors`가 올바르지 않은 것 같습니다. `all_wars_matrix`를 5로 나누고 결과 행렬을 `visitors`로 저장하십시오.")

ex() %>% check_output_expr("visitors", missing_msg = "`visitors`를 출력하여 확인하는 것을 잊지 마십시오.")

success_msg("훌륭합니다! 이 결과가 무엇을 말해주나요? 무려 9,200만 명이 미국 극장에서 A New Hope를 보았습니다! 다음 연습으로 계속하십시오.")
```

---

## 행렬의 산술 연산 (2)

```yaml
type: NormalExercise
key: 1e0b39d6e9
xp: 100
skills:
  - 1
```

`2 * my_matrix`가 `my_matrix`의 모든 요소에 2를 곱했던 것처럼, `my_matrix1 * my_matrix2`는 `my_matrix1`과 `my_matrix2`의 대응하는 요소끼리 곱한 값으로 이루어진 행렬을 만듭니다.

이전 연습 문제의 결과를 살펴본 루카스 사장은 시간이 지남에 따라 티켓 가격이 올랐다고 지적합니다. 그는 `ticket_prices_matrix`에 있는 가격을 바탕으로 분석을 다시 해달라고 요청합니다(출처: 상상력).

_행렬에 익숙한 분들을 위해 알려드리자면, 이 연산은 표준 행렬 곱셈이 아닙니다. 표준 행렬 곱셈을 수행하려면 R에서 `%*%`를 사용해야 합니다._

`@instructions`
- `all_wars_matrix`를 `ticket_prices_matrix`로 나누어 여섯 편의 영화에 대한 미국 및 미국 외 지역의 추정 방문객 수를 구하세요. 결과를 `visitors`에 저장하세요.
- `visitors` 행렬에서 미국 방문객 수를 나타내는 첫 번째 열 전체를 선택하고, 이를 `us_visitors`에 저장하세요.
- 미국 방문객의 평균 수를 계산하고 결과를 출력하세요.

`@hint`
- `mean()` 함수를 사용하면 입력값들의 평균을 계산할 수 있습니다.
- 미국 방문객 수를 구하려면 `visitors[ ,1]`를 사용해 `visitors`의 첫 번째 열을 선택하세요.

`@pre_exercise_code`
```{r}
load(url("https://assets.datacamp.com/course/intro_to_r/all_wars_matrix.RData"))
movie_names <- c("A New Hope","The Empire Strikes Back","Return of the Jedi", "The Phantom Menace", "Attack of the Clones", "Revenge of the Sith")
col_titles <- c("US","non-US")
ticket_prices_matrix <- matrix(c(5, 5, 6, 6, 7, 7, 4, 4, 4.5, 4.5, 4.9, 4.9), nrow = 6, byrow = TRUE, dimnames = list(movie_names,col_titles))
```

`@sample_code`
```{r}
# all_wars_matrix and ticket_prices_matrix are available in your workspace
all_wars_matrix
ticket_prices_matrix

# Estimated number of visitors
visitors <- 

# US visitors
us_visitors <- 

# Average number of US visitors

```

`@solution`
```{r}
# all_wars_matrix and ticket_prices_matrix are available in your workspace
all_wars_matrix
ticket_prices_matrix

# Estimated number of visitors
visitors <- all_wars_matrix / ticket_prices_matrix

# US visitors
us_visitors <- visitors[ ,1]

# Average number of US visitors
mean(us_visitors)
```

`@sct`
```{r}
msg <- "`all_wars_matrix`의 내용을 변경하지 마십시오. 이 행렬은 이미 작업 공간에 생성되어 있습니다."
ex() %>% check_object("all_wars_matrix", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("ticket_prices_matrix", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("visitors") %>% check_equal(incorrect_msg = "`visitors` 행렬을 올바르게 생성하셨습니까? `all_wars_matrix`를 `ticket_prices_matrix`로 나누어야 합니다.")

ex() %>% check_object("us_visitors") %>% check_equal(incorrect_msg = "`us_visitors`를 생성하려면 `visitors`의 첫 번째 열 전체를 올바르게 선택해야 합니다. 이를 위해 `[,1]`을 사용할 수 있습니다!")

ex() %>% check_output_expr("mean(us_visitors)", missing_msg = "`us_visitors`를 생성한 후에는 `mean()`을 사용하여 미국 방문자의 평균 수를 계산할 수 있습니다. 결과를 출력하는 것을 잊지 마십시오.")

success_msg("사실입니다: R의 힘이 당신과 함께합니다! 이 연습은 행렬에 대한 장을 마무리합니다. R 언어 여행의 다음 목적지: 요인입니다.")
```
