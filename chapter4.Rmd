---
title_meta: "Kapitel\_4"
title: Faktoren
description: >-
  Daten lassen sich oft einer begrenzten Anzahl von Kategorien zuordnen. Die
  menschliche Haarfarbe kann zum Beispiel als schwarz, braun, blond, rot, grau
  oder weiß kategorisiert werden. (Und dazu könnte es noch ein paar Optionen für
  Menschen mit gefärbten Haaren geben.) In R werden kategoriale Daten in
  Faktoren gespeichert. Faktoren sind bei der Datenanalyse sehr wichtig, also
  lernst du jetzt, wie du sie erstellen und vergleichen oder ihre Elemente
  abrufen kannst.
---

## Was sind Faktoren und wozu braucht man sie?

```yaml
type: NormalExercise
key: 05273321916d99bb9c0deadf75c6834d25a47244
xp: 100
skills:
  - 1
```

In diesem Kapitel tauchst du ein in die wunderbare Welt der **Faktoren**.

Faktor meint einen statistischen Datentyp, der zur Speicherung kategorialer Variablen verwendet wird. Der Unterschied zwischen einer kategorialen und einer kontinuierlichen Variable besteht darin, dass eine kategoriale Variable einer **begrenzten Anzahl von Kategorien** angehört. Eine kontinuierliche Variable hingegen kann unendlich vielen Werten entsprechen.

In R ist es wichtig klarzustellen, ob es sich um eine kontinuierliche oder eine kategoriale Variable handelt, da die statistischen Modelle, die du in Zukunft entwickeln wirst, beide Typen unterschiedlich behandeln. (Du wirst später sehen, warum das so ist.)

Ein gutes Beispiel für eine kategoriale Variable ist das Geschlecht. In vielen Fällen kannst du die Geschlechtskategorien auf „männlich“ oder „weiblich“ beschränken. (Manchmal brauchst du vielleicht andere Kategorien, beispielsweise um Chromosomenvariationen, zwittrige Tiere oder unterschiedliche kulturelle Normen zu berücksichtigen. Aber du wirst immer eine begrenzte Anzahl von Kategorien haben.)

`@instructions`
Weise der Variablen `theory` den Wert `"factors"` zu.

`@hint`
Weise einfach eine Variable zu (`<-`); achte dabei auf die richtige Großschreibung.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Assign to the variable theory what this chapter is about!
```

`@solution`
```{r}
# Assign to the variable theory what this chapter is about!
theory <- "factors"
```

`@sct`
```{r}
msg_undef <- "Es sieht so aus, als hätten Sie die Variable `theory` nicht definiert."
msg_incor <- "Der Wert von `theory` scheint nicht korrekt zu sein. Stellen Sie sicher, dass Sie den Zeichenfolgenwert `\"factors\"` zuweisen. Denken Sie daran, dass R zwischen Groß- und Kleinschreibung unterscheidet."
msg_err <- "Stellen Sie sicher, dass Sie `theory` korrekt definiert haben, indem Sie `<-` für die Zuweisung verwenden."

# If get error and theory is undefined, point out the error
ex()  %>% check_or(check_error(.,msg_err), check_object(.,"theory")  %>% check_equal(eval = FALSE))

check_object(.,"theory", undefined_msg = msg_undef)  %>% check_equal(incorrect_msg = msg_incor)
success_msg("Gut gemacht! Bereit zu starten? Fahren Sie mit der nächsten Übung fort!")
```

---

## Was sind Faktoren und wozu braucht man sie? (2)

```yaml
type: NormalExercise
key: 6cc21c842b075347926bb1b244782213df32e370
xp: 100
skills:
  - 1
```

Um Faktoren in R zu erstellen, verwendest du die Funktion `factor()`. Als Erstes erzeugst du einen Vektor, der alle Dateneinträge (Beobachtungen) enthält, die zu einer begrenzten Anzahl von Kategorien gehören. Zum Beispiel enthält `sex_vector` das Geschlecht von fünf verschiedenen Personen:

```
sex_vector <- c("Male","Female","Female","Male","Male")
```

Offensichtlich gibt es hier zwei Kategorien, die in der R-Sprache auch als **Levels oder Ausprägungen des Faktors** bezeichnet werden: „Male“ und „Female“.

Die Funktion `factor()` kodiert den Vektor als Faktor:

```
factor_sex_vector <- factor(sex_vector)
```

`@instructions`
- Wandle den Zeichenkettenvektor `sex_vector` mit `factor()` in einen Faktor um und weise das Ergebnis `factor_sex_vector` zu.
- Gib `factor_sex_vector` aus und stelle sicher, dass R die Faktorausprägungen unter den tatsächlichen Werten ausgibt.

`@hint`
Verwende einfach die Funktion `factor()` mit `sex_vector`. Sieh dir die Aufgabenstellung genau an – die Lösung steht schon irgendwo...

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Sex vector
sex_vector <- c("Male", "Female", "Female", "Male", "Male")

# Convert sex_vector to a factor
factor_sex_vector <-

# Print out factor_sex_vector

```

`@solution`
```{r}
# Sex vector
sex_vector <- c("Male", "Female", "Female", "Male", "Male")

# Convert sex_vector to a factor
factor_sex_vector <- factor(sex_vector)

# Print out factor_sex_vector
factor_sex_vector
```

`@sct`
```{r}
ex()  %>%  check_object("factor_sex_vector")  %>% check_equal(incorrect_msg = "Haben Sie den Faktor von `sex_vector` `factor_sex_vector` zugewiesen?")
ex()  %>%  check_output_expr("factor_sex_vector", missing_msg = "Vergessen Sie nicht, `factor_sex_vector` auszugeben!")

success_msg("Großartig! Wenn Sie mehr über die `factor()`-Funktion erfahren möchten, zögern Sie nicht, `?factor` in die Konsole einzugeben. Dies öffnet eine Hilfeseite. Fahren Sie mit der nächsten Übung fort.");

```

---

## Was sind Faktoren und wozu braucht man sie? (3)

```yaml
type: NormalExercise
key: 5bd4f50afc2c2dbc881e16b8ca94ca56960dff42
xp: 100
skills:
  - 1
```

Es gibt zwei Arten von kategorialen Variablen: **nominale kategoriale Variablen** und **ordinale kategoriale Variablen**.

Eine nominale Variable ist eine kategoriale Variable ohne eine implizite Reihenfolge. Das bedeutet, dass man nicht sagen kann, ob eine Kategorie „wertvoller ist“ als eine andere Kategorie. Stell dir zum Beispiel die kategoriale Variable `animals_vector` mit den Kategorien `"Elephant"`, `"Giraffe"`, `"Donkey"` und `"Horse"` vor. Hier kann man keine bestimmte Rangordnung erkennen. (Auch wenn du vielleicht anderer Meinung bist. ;-) )

Im Gegensatz dazu haben ordinale Variablen eine natürliche Reihenfolge. Nimm zum Beispiel die kategoriale Variable `temperature_vector` mit den Kategorien: `"Low"`, `"Medium"` und `"High"`. Hier ist es offensichtlich, dass `"Medium"` mehr ist als `"Low"` und dass `"High"` mehr ist als `"Medium"`.

`@instructions`
Sende die Antwort ab, um zu prüfen, wie R nominale und ordinale Variablen erstellt und ausgibt. Mach dir keine Sorgen, wenn du den Code noch nicht ganz verstehst – das klären wir später.

`@hint`
Sende einfach die Antwort ab und wirf einen Blick auf die Konsole. Beachte, wie R die Reihenfolge der Levels eines Faktors für ordinale kategoriale Variablen angibt.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Animals
animals_vector <- c("Elephant", "Giraffe", "Donkey", "Horse")
factor_animals_vector <- factor(animals_vector)
factor_animals_vector

# Temperature
temperature_vector <- c("High", "Low", "High","Low", "Medium")
factor_temperature_vector <- factor(temperature_vector, order = TRUE, levels = c("Low", "Medium", "High"))
factor_temperature_vector
```

`@solution`
```{r}
# Animals
animals_vector <- c("Elephant", "Giraffe", "Donkey", "Horse")
factor_animals_vector <- factor(animals_vector)
factor_animals_vector

# Temperature
temperature_vector <- c("High", "Low", "High","Low", "Medium")
factor_temperature_vector <- factor(temperature_vector, order = TRUE, levels = c("Low", "Medium", "High"))
factor_temperature_vector
```

`@sct`
```{r}
msg <- "Ändern Sie nichts am Beispielcode. Reichen Sie einfach die Antwort ein und überprüfen Sie die Lösung!"

ex() %>% check_object("animals_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("temperature_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_animals_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("factor_animals_vector", missing_msg = msg)

ex() %>% check_object("factor_temperature_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("factor_temperature_vector", missing_msg = msg)


success_msg("Können Sie schon erkennen, was in dieser Übung passiert? Großartig! Fahren Sie mit der nächsten Übung fort und gehen Sie auf die Details der Faktorstufen ein.")
```

---

## Ausprägungen von Faktoren

```yaml
type: NormalExercise
key: 1aa698978d32d1a0befa4700d7da85a648e1d69e
xp: 100
skills:
  - 1
```

Wenn du einen Datensatz zum ersten Mal erhältst, wirst du oft feststellen, dass er Faktoren mit spezifischen Ausprägungen enthält. Manchmal möchtest du jedoch die Namen dieser Ausprägungen für mehr Klarheit oder aus anderen Gründen ändern. In R kannst du dies mit der Funktion `levels()` tun:

```
levels(factor_vector) <- c("name1", "name2",...)
```

Zur Veranschaulichung denke zum Beispiel an Rohdaten, die du durch eine Umfrage erhältst. In Fragebögen wird häufig nach dem Geschlecht der teilnehmenden Person gefragt. Der Einfachheit halber wurden hier nur zwei Kategorien erfasst: `"M"` und `"F"`. (Für Umfragedaten brauchst du normalerweise mehr Kategorien; aber in jedem Fall verwendest du einen Faktor, um die kategorialen Daten zu speichern.) 

```
survey_vector <- c("M", "F", "F", "M", "M")
```

Die Erfassung des Geschlechts mit den Abkürzungen `"M"` und `"F"` kann bequem sein, wenn du die Daten mit Stift und Papier notieren musst. Aber bei der Datenanalyse kann das zu Verwirrung führen. Daher möchtest du an diesem Punkt oft die Ausprägungen des Faktors in `"Male"` und `"Female"` ändern, statt `"M"` und `"F"` zu nutzen, um Klarheit zu schaffen.

**Vorsicht!** Die Reihenfolge, in der du die Levels zuweist, ist wichtig. Wenn du `levels(factor_survey_vector)` eingibst, erhältst du als Ausgabe `[1] "F" "M"`. Wenn du die Ausprägungen des Faktors beim Erstellen des Vektors nicht angibst, weist `R` sie automatisch in alphabetischer Reihenfolge zu. Um `"F"` korrekt auf `"Female"` und `"M"` auf `"Male"` abzubilden, sollten die Ausprägungen als `c("Female", "Male")` angegeben werden, also in genau dieser Reihenfolge.

`@instructions`
- Schau dir den Code an, der einen Faktorvektor aus `survey_vector` erstellt. Du solltest in der nächsten Anweisung `factor_survey_vector` verwenden.
- Ändere die Faktorausprägungen von `factor_survey_vector` in `c("Female", "Male")`. Achte hier auf die Reihenfolge der Vektorelemente!

`@hint`
Beachte die Reihenfolge, in der du die Levels des Faktors eingeben musst! Tipp: Achte auf die Reihenfolge, in der die Levels ausgegeben werden, wenn du `levels(factor_survey_vector)` eingibst.

`@pre_exercise_code`
```{r}
# no pec
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
```

`@sample_code`
```{r}
# Code to build factor_survey_vector
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)

# Specify the levels of factor_survey_vector
levels(factor_survey_vector) <-

factor_survey_vector
```

`@solution`
```{r}
# Code to build factor_survey_vector
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)

# Specify the levels of factor_survey_vector
levels(factor_survey_vector) <- c("Female", "Male")

factor_survey_vector
```

`@sct`
```{r}
msg = "Ändern Sie nicht die Definition von `survey_vector`!"
ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)
msg = "Ändern oder entfernen Sie nicht den Code zur Erstellung des Faktorvektors."
ex()  %>% check_function("factor", not_called_msg = msg) %>% check_arg('x')  %>% check_equal(incorrect_msg = msg)

# MC-note: ideally would want to test assign operator `<-`, and have it highlight whole line.

# MC-note: or negate this test_student_typed, to highlight where they type this incorrect phrase
# test_student_typed('c("Male", "Female")')

ex()  %>% check_object("factor_survey_vector") %>% check_equal(eq_condition = "equal", incorrect_msg = paste("Haben Sie die richtigen Faktorstufen `factor_survey_vector` zugewiesen? Verwenden Sie `levels(factor_survey_vector) <- c(\"Female\", \"Male\")`. Denken Sie daran, dass R zwischen Groß- und Kleinschreibung unterscheidet!"))


success_msg("Wunderbar! Fahren Sie mit der nächsten Übung fort.")

```

---

## Faktorinhalt zusammenfassen

```yaml
type: NormalExercise
key: a549f13c0644ccc89cd39a10aa48706754637ed0
xp: 100
skills:
  - 1
```

Nach Abschluss dieses Kurses wird `summary()` bestimmt eine deiner Lieblingsfunktionen in R sein. So bekommst du einen schnellen Überblick über den Inhalt einer Variablen:

```
summary(my_var)
```

Um auf unsere Umfrage zurückzukommen: Nun möchtest du gerne wissen, wie viele `"Male"`-Antworten es in der Studie gibt und wie viele `"Female"`-Antworten. Die Funktion `summary` liefert die gewünschten Informationen.

`@instructions`
Lass die Funktion `summary()` für `survey_vector` und `factor_survey_vector` laufen. Schau dir die Ergebnisse für beide Vektoren an. Sind sie in diesem Szenario beide gleich nützlich?

`@hint`
Rufe die Funktion `summary()` sowohl mit `survey_vector` als auch mit `factor_survey_vector` auf – so einfach ist das!

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")
factor_survey_vector

# Generate summary for survey_vector


# Generate summary for factor_survey_vector

```

`@solution`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")
factor_survey_vector

# Generate summary for survey_vector
summary(survey_vector)

# Generate summary for factor_survey_vector
summary(factor_survey_vector)
```

`@sct`
```{r}
msg = "Ändern Sie nichts an den ersten Zeilen, die `survey_vector` und `factor_survey_vector` definieren."

ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_survey_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

msg <- "Haben Sie `summary()` korrekt verwendet, um eine Zusammenfassung für `%s` zu erstellen?"

ex() %>% check_output_expr("summary(survey_vector)", missing_msg = sprintf(msg, "survey_vector"))

ex() %>% check_output_expr("summary(factor_survey_vector)", missing_msg = sprintf(msg, "factor_survey_vector"))

success_msg("Schön! Schauen Sie sich die Ausgabe an. Die Tatsache, dass Sie `\"Male\"` und `\"Female\"` als Faktorstufen in `factor_survey_vector` identifiziert haben, ermöglicht es R, die Anzahl der Elemente für jede Kategorie anzuzeigen.")

```

---

## Kampf der Geschlechter

```yaml
type: NormalExercise
key: 90ecc160d1ebf2f75bf53f9c3843fc1632bdd0a5
xp: 100
skills:
  - 1
```

Du fragst dich vielleicht, was passiert, wenn du versuchst, die Elemente eines Faktors zu vergleichen.  In `factor_survey_vector` hast du einen Faktor mit zwei Ausprägungen: `"Male"` und `"Female"`. Aber wie bewertet R sie im Verhältnis zueinander?

`@instructions`
Lies den Code im Editor und sende die Antwort ab, um zu testen, ob `male` größer ist (`>`) als `female`.

`@hint`
Sende die Antwort einfach ab und sieh dir an, was in der Konsole ausgegeben wird.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")

# Male
male <- factor_survey_vector[1]

# Female
female <- factor_survey_vector[2]

# Battle of the sexes: Male 'larger' than female?
male > female
```

`@solution`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")

# Male
male <- factor_survey_vector[1]

# Female
female <- factor_survey_vector[2]

# Battle of the sexes: Male 'larger' than female?
male > female
```

`@sct`
```{r}
msg = "Ändern Sie nichts am Code; reichen Sie einfach die Antwort ein und sehen Sie sich das Ergebnis an."

ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_survey_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("male", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("female", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("male > female", missing_msg = msg)


success_msg("Wie interessant! Standardmäßig gibt R `NA` zurück, wenn Sie versuchen, Werte in einem Faktor zu vergleichen, da die Idee keinen Sinn ergibt. Als nächstes lernen Sie geordnete Faktoren kennen, bei denen sinnvollere Vergleiche möglich sind.")
```

---

## Geordnete Faktoren

```yaml
type: NormalExercise
key: 9ab0928916bf84ab225713a9a1ce40d9e322c6a0
xp: 100
skills:
  - 1
```

Da `"Male"` und `"Female"` ungeordnete (oder nominale) Ausprägungen des Faktors sind, gibt R eine Warnmeldung aus, die dir sagt, dass der „Größer als“-Operator nicht sinnvoll ist. Wie bereits gesehen, erachtet R die Ausprägungen für solche Faktoren als gleichwertig.

Aber das ist nicht immer der Fall! Manchmal hast du es auch mit Faktoren zu tun, die eine natürliche Reihenfolge ihrer Kategorien haben. Wenn das der Fall ist, müssen wir sicherstellen, dass wir diese Information an R übergeben...

Nehmen wir an, du leitest ein fünfköpfiges Team aus Datenanalysten und möchtest ihre Leistung bewerten. Dazu verfolgst du ihr Arbeitstempo, bewertest jeden Analysten als `"slow"`, `"medium"` oder `"fast"` und speicherst die Ergebnisse unter `speed_vector`.

`@instructions`
Im ersten Schritt weist du `speed_vector` einen Vektor mit fünf Elementen zu, nämlich einem für jeden Analysten. Jedes Element hat den Wert `"slow"`, `"medium"` oder `"fast"`. Verwende die folgende Liste:

- Analyst 1 ist mittelmäßig („medium“),
- Analyst 2 ist langsam („slow“),
- Analyst 3 ist langsam („slow“),
- Analyst 4 ist mittelmäßig („medium“),
- Analyst 5 ist schnell („fast“).

Es ist noch nicht nötig, diese Werte als Faktor zu spezifizieren.

`@hint`
Weise `speed_vector` einen Vektor zu, der die Zeichenketten `"slow"`, `"medium"` und `"fast"` enthält.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create speed_vector
speed_vector <-
```

`@solution`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
```

`@sct`
```{r}
ex() %>% check_object("speed_vector") %>% check_equal(incorrect_msg = "`speed_vector` sollte ein Vektor mit 5 Einträgen sein, einer für jede Geschwindigkeitsbewertung des Analysten. Verwenden Sie keine Großbuchstaben; R ist case-sensitiv!")

success_msg("Gut gemacht! Weiter zur nächsten Übung.")
```

---

## Geordnete Faktoren (2)

```yaml
type: NormalExercise
key: 279077d10248ce03d5f972939ef8576430a16683
xp: 100
skills:
  - 1
```

`speed_vector` sollte in einen ordinalen Faktor umgewandelt werden, da seine Kategorien eine natürliche Reihenfolge haben. Standardmäßig kann `speed_vector` mit der Funktion `factor()` in einen ungeordneten Faktor umgewandelt werden. Um einen geordneten Faktor zu erstellen, musst du zwei zusätzliche Argumente hinzufügen: `ordered` und `levels`.

```
factor(some_vector,

       ordered = TRUE,

       levels = c("lev1", "lev2" ...))

```

Indem du in der Funktion `factor()` das Argument `ordered` auf `TRUE` setzt, gibst du an, dass der Faktor geordnet ist. Mit dem Argument `levels` gibst du die Werte des Faktors in der richtigen Reihenfolge an.

`@instructions`
Erstelle aus `speed_vector` einen geordneten Faktorvektor: `factor_speed_vector`. Setze `ordered` auf `TRUE` und gib `c("slow", "medium", "fast")` für `levels` an.

`@hint`
Verwende die Funktion `factor()`, um `factor_speed_vector` anhand von `speed_character_vector` zu erstellen. Das Argument `ordered` sollte auf `TRUE` gesetzt werden, da es eine natürliche Reihenfolge gibt. Außerdem brauchst du `levels = c("slow", "medium", "fast")`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")

# Convert speed_vector to ordered factor vector
factor_speed_vector <-

# Print factor_speed_vector
factor_speed_vector
summary(factor_speed_vector)
```

`@solution`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")

# Convert speed_vector to ordered factor vector
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Print factor_speed_vector
factor_speed_vector
summary(factor_speed_vector)
```

`@sct`
```{r}
msg = "Ändern Sie nichts an dem Befehl, der die Variable `speed_vector` angibt."

ex() %>% check_object("speed_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_function("factor") %>% {
	check_arg(., 'x') %>% check_equal(incorrect_msg="Das erste Argument, das Sie an `factor()` übergeben, sollte `speed_vector` sein.")
	check_arg(., 'ordered') %>% check_equal(incorrect_msg="Stellen Sie sicher, dass `ordered = TRUE` in Ihrem Aufruf von `factor()` gesetzt ist.")
	check_arg(., 'levels') %>% check_equal(incorrect_msg="Stellen Sie sicher, dass `levels = c(\"slow\", \"medium\", \"fast\")` in Ihrem Aufruf von `factor()` gesetzt ist.")
	}

ex() %>% check_object("factor_speed_vector") %>% check_equal(eq_condition = "equal", incorrect_msg="Es gibt immer noch ein Problem mit `factor_speed_vector`; stellen Sie sicher, dass Sie nur `speed_vector`, `ordered = TRUE` und `levels = c(\"slow\", \"medium\", \"fast\")` in Ihrem Aufruf von `factor()` übergeben.")

                      
                      
success_msg("Großartig! Schauen Sie sich die Konsole an. Es wird nun angezeigt, dass die Levels tatsächlich eine Reihenfolge haben, mit dem `<` Zeichen. Fahren Sie mit der nächsten Übung fort.")
```

---

## Geordnete Faktoren vergleichen

```yaml
type: NormalExercise
key: db16e69805625bcfde227743a8cbc985f8482a37
xp: 100
skills:
  - 1
```

Datenanalyst #2 hat einen schlechten Tag bei der Arbeit und kommt in dein Büro, um sich zu beschweren, dass Datenanalyst #5 das gesamte Projekt verlangsamt. Da du weißt, dass Datenanalyst #2 den Ruf eines Klugscheißers hat, willst du zunächst überprüfen, ob seine Behauptung stimmt.

Da `factor_speed_vector` jetzt geordnet ist, können wir die verschiedenen Elemente (die hier den Datenanalysten entsprechen) vergleichen. Nutze dazu einfach die bereits bekannten Operatoren.

`@instructions`
- Verwende `[2]`, um aus `factor_speed_vector` den Faktorwert für den zweiten Datenanalysten auszuwählen. Speichere ihn als `da2`.
- Verwende `[5]`, um aus `factor_speed_vector` den Faktorwert für den fünften Datenanalysten auszuwählen. Speichere ihn als `da5`.
- Prüfe, ob `da2` größer ist als `da5`, und gib einfach das Ergebnis aus. Zur Erinnerung: Du kannst den `>`-Operator benutzen, um zu prüfen, ob ein Element größer als das andere ist.

`@hint`
- Um den Wert im Faktor für den dritten Datenanalysten auszuwählen, brauchst du `factor_speed_vector[3]`.
- Um zwei Werte zu vergleichen, kannst du `>` verwenden. Zum Beispiel: `da3 > da4`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create factor_speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Factor value for second data analyst
da2 <-

# Factor value for fifth data analyst
da5 <-

# Is data analyst 2 faster than data analyst 5?

```

`@solution`
```{r}
# Create factor_speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Factor value for second data analyst
da2 <- factor_speed_vector[2]

# Factor value for fifth data analyst
da5 <- factor_speed_vector[5]

# Is data analyst 2 faster data analyst 5?
da2 > da5
```

`@sct`
```{r}
msg = "Ändern Sie nichts an den Befehlen, die `speed_vector` und `factor_speed_vector` definieren!"
ex() %>% check_object("speed_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_speed_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)


msg <- "Haben Sie den Faktorwert für den %s Datenanalysten korrekt ausgewählt? Sie können `factor_speed_vector[%s]` verwenden."
ex() %>% check_object("da2") %>% check_equal(eq_condition = "equal", incorrect_msg = sprintf(msg,"zweiten", "2"))

ex() %>% check_object("da5") %>% check_equal(eq_condition = "equal",incorrect_msg = sprintf("fünften", "5"))

ex() %>% check_output_expr("da2 > da5", missing_msg = "Haben Sie `da2` und `da5` korrekt verglichen? Sie können `>` verwenden. Drucken Sie einfach das Ergebnis aus.")


success_msg("Bellissimo! Was sagt Ihnen das Ergebnis? Datenanalyst zwei beschwert sich über den Datenanalysten fünf, obwohl sie tatsächlich diejenigen sind, die alles verlangsamen! Dies schließt das Kapitel über Faktoren ab. Mit einer soliden Basis in Vektoren, Matrizen und Faktoren sind Sie bereit, in die wunderbare Welt der Data Frames einzutauchen, einer sehr wichtigen Datenstruktur in R!")

```
