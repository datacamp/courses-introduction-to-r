---
title_meta: "Chapitre\_4"
title: Facteurs
description: >-
  Les données sont souvent classées dans un nombre limité de catégories. Par
  exemple, la couleur des cheveux humains peut être classée en noir, brun,
  blond, rouge, gris ou blanc, et peut-être quelques autres options pour les
  personnes qui se colorent les cheveux. Dans R, les données catégorielles sont
  stockées dans des facteurs. Les facteurs sont très importants dans l'analyse
  des données, alors commencez dès maintenant à apprendre comment les créer, les
  répartir en sous-ensembles et les comparer.
---

## Qu'est-ce qu'un facteur et pourquoi l'utiliser ?

```yaml
type: NormalExercise
key: 05273321916d99bb9c0deadf75c6834d25a47244
xp: 100
skills:
  - 1
```

Dans ce chapitre, vous plongez dans le monde merveilleux des **facteurs.**

Le terme facteur fait référence à un type de données statistiques utilisé pour stocker des variables catégorielles. La différence entre une variable catégorielle et une variable continue est qu'une variable catégorielle peut appartenir à un **nombre limité de catégories**. Une variable continue, en revanche, peut correspondre à un nombre infini de valeurs.

Il est important que R sache s'il a affaire à une variable continue ou catégorielle, car les modèles statistiques que vous développerez à l'avenir traitent ces deux types de variables différemment (vous verrez plus loin pourquoi).

Le sexe est un bon exemple de variable catégorielle. Dans de nombreuses circonstances, vous pouvez limiter les catégories de sexe à « Masculin » ou « Féminin » (il peut arriver que vous ayez besoin de catégories différentes. Par exemple, vous devrez peut-être tenir compte des variations chromosomiques, des animaux hermaphrodites ou des différentes normes culturelles, mais vous disposerez toujours d'un nombre fini de catégories).

`@instructions`
Attribuez à la variable `theory` la valeur `"factors"`.

`@hint`
Il suffit d'affecter une variable (`<-`) ; veillez à mettre les majuscules correctement.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Assign to the variable theory what this chapter is about!
```

`@solution`
```{r}
# Assign to the variable theory what this chapter is about!
theory <- "factors"
```

`@sct`
```{r}
msg_undef <- "Il semble que vous n'avez pas défini la variable `theory`."
msg_incor <- "La valeur de `theory` semble incorrecte. Assurez-vous de lui attribuer la chaîne de caractères `\"factors\"`. N'oubliez pas que R est sensible à la casse."
msg_err <- "Assurez-vous d'avoir défini `theory` correctement, en utilisant `<-` pour l'affectation."

# If get error and theory is undefined, point out the error
ex()  %>% check_or(check_error(.,msg_err), check_object(.,"theory")  %>% check_equal(eval = FALSE))

check_object(.,"theory", undefined_msg = msg_undef)  %>% check_equal(incorrect_msg = msg_incor)
success_msg("Bon travail ! Prêt à commencer ? Passez à l'exercice suivant !")
```

---

## Qu'est-ce qu'un facteur et pourquoi l'utiliser ? (2)

```yaml
type: NormalExercise
key: 6cc21c842b075347926bb1b244782213df32e370
xp: 100
skills:
  - 1
```

Pour créer des facteurs dans R, utilisez la fonction `factor()`. La première chose à faire est de créer un vecteur contenant toutes les observations qui appartiennent à un nombre limité de catégories. Par exemple, `sex_vector` contient le sexe de 5 personnes différentes :

```
sex_vector <- c("Male","Female","Female","Male","Male")
```

Il est clair qu'il y a deux catégories, ou, en termes de R, deux **« niveaux de facteurs »,** à l'œuvre ici : "Male"</span> et <span translate="no">"Female"</span>.

La fonction `factor()` codera le vecteur comme un facteur :

```
factor_sex_vector <- factor(sex_vector)
```

`@instructions`
- Convertissez le vecteur de caractères `sex_vector` en un facteur avec `factor()` et affectez le résultat à `factor_sex_vector`
- Affichez `factor_sex_vector` et vérifiez que R affiche les niveaux de facteurs sous les valeurs réelles.

`@hint`
Il suffit d'utiliser la fonction `factor()` sur `sex_vector`. Jetez un coup d'œil à l’énoncé, la réponse s'y trouve déjà quelque part…

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Sex vector
sex_vector <- c("Male", "Female", "Female", "Male", "Male")

# Convert sex_vector to a factor
factor_sex_vector <-

# Print out factor_sex_vector

```

`@solution`
```{r}
# Sex vector
sex_vector <- c("Male", "Female", "Female", "Male", "Male")

# Convert sex_vector to a factor
factor_sex_vector <- factor(sex_vector)

# Print out factor_sex_vector
factor_sex_vector
```

`@sct`
```{r}
ex()  %>%  check_object("factor_sex_vector")  %>% check_equal(incorrect_msg = "Avez-vous assigné le facteur de `sex_vector` à `factor_sex_vector`?")
ex()  %>%  check_output_expr("factor_sex_vector", missing_msg = "N'oubliez pas d'imprimer `factor_sex_vector` !")

success_msg("Super ! Si vous souhaitez en savoir plus sur la fonction `factor()`, n'hésitez pas à taper `?factor` dans la console. Cela ouvrira une page d'aide. Continuez à l'exercice suivant.");
```

---

## Qu'est-ce qu'un facteur et pourquoi l'utiliser ? (3)

```yaml
type: NormalExercise
key: 5bd4f50afc2c2dbc881e16b8ca94ca56960dff42
xp: 100
skills:
  - 1
```

Il existe deux types de variables catégorielles : une **variable catégorielle nominale** et une **variable catégorielle ordinale**.

Une variable nominale est une variable catégorielle sans ordre implicite. Cela signifie qu'il est impossible de dire que « l'un vaut plus que l'autre ». Par exemple, imaginez la variable catégorielle `animals_vector` avec les catégories `"Elephant"`, `"Giraffe"`, `"Donkey"` et `"Horse"`. Ici, il est impossible de dire que l'un se situe au-dessus ou au-dessous de l'autre (notez que certains d'entre vous pourraient ne pas être d'accord ;-) ).

En revanche, les variables ordinales ont un ordre naturel. Considérons par exemple la variable catégorielle `temperature_vector` avec les catégories : `"Low"`, `"Medium"` et `"High"`. Ici, il est évident que `"Medium"` se trouve au-dessus de `"Low"`, et que `"High"` se trouve au-dessus de `"Medium"`.

`@instructions`
Exécutez le code pour vérifier comment R construit et affiche les variables nominales et ordinales. Ne vous inquiétez pas si vous ne comprenez pas encore tout le code, nous y reviendrons.

`@hint`
Il suffit d’envoyer la réponse et de regarder la console. Remarquez que R indique l'ordre des niveaux de facteurs pour les variables catégorielles ordinales.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Animals
animals_vector <- c("Elephant", "Giraffe", "Donkey", "Horse")
factor_animals_vector <- factor(animals_vector)
factor_animals_vector

# Temperature
temperature_vector <- c("High", "Low", "High","Low", "Medium")
factor_temperature_vector <- factor(temperature_vector, order = TRUE, levels = c("Low", "Medium", "High"))
factor_temperature_vector
```

`@solution`
```{r}
# Animals
animals_vector <- c("Elephant", "Giraffe", "Donkey", "Horse")
factor_animals_vector <- factor(animals_vector)
factor_animals_vector

# Temperature
temperature_vector <- c("High", "Low", "High","Low", "Medium")
factor_temperature_vector <- factor(temperature_vector, order = TRUE, levels = c("Low", "Medium", "High"))
factor_temperature_vector
```

`@sct`
```{r}
msg <- "Ne changez rien au code d'exemple. Soumettez simplement la réponse et inspectez la solution !"

ex() %>% check_object("animals_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("temperature_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_animals_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("factor_animals_vector", missing_msg = msg)

ex() %>% check_object("factor_temperature_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("factor_temperature_vector", missing_msg = msg)


success_msg("Pouvez-vous déjà dire ce qui se passe dans cet exercice ? Génial ! Continuez à l'exercice suivant et entrez dans les détails des niveaux de facteur.")
```

---

## Niveaux de facteur

```yaml
type: NormalExercise
key: 1aa698978d32d1a0befa4700d7da85a648e1d69e
xp: 100
skills:
  - 1
```

Lorsque vous obtenez un ensemble de données pour la première fois, vous remarquerez souvent qu'il contient des facteurs avec des niveaux de facteurs spécifiques. Cependant, vous souhaiterez parfois changer les noms de ces niveaux pour des raisons de clarté ou autres. R vous permet de le faire avec la fonction `levels()` :

```
levels(factor_vector) <- c("name1", "name2",...)
```

Les données brutes qui vous sont fournies par une enquête en sont une bonne illustration. Une question commune à tous les questionnaires est celle du sexe du répondant. Par souci de simplicité, seules deux catégories ont été enregistrées : `"M"` et `"F"` (il faut généralement plus de catégories pour les données d'enquête ; dans tous les cas, on utilise un facteur pour stocker les données catégorielles). 

```
survey_vector <- c("M", "F", "F", "M", "M")
```

L'enregistrement du sexe à l'aide des abréviations `"M"` et `"F"` peut être pratique si vous recueillez des données avec un stylo et du papier, mais cela peut prêter à confusion lors de l'analyse des données. À ce stade, vous souhaiterez souvent, par souci de clarté, remplacer les niveaux de facteur par `"Male"` et `"Female"` au lieu de `"M"` et `"F"`.

**Attention :** l'ordre dans lequel vous attribuez les niveaux est important. Si vous tapez `levels(factor_survey_vector)`, vous verrez que le résultat est `[1] "F" "M"`. Si vous ne spécifiez pas les niveaux du facteur lors de la création du vecteur, `R` les attribuera automatiquement par ordre alphabétique. Pour faire correspondre correctement `"F"` à `"Female"` et `"M"` à `"Male"`, les niveaux doivent être réglés sur `c("Female", "Male")`, dans cet ordre.

`@instructions`
- Consultez le code qui construit un vecteur de facteurs à partir de `survey_vector`. Vous devez utiliser `factor_survey_vector` dans l'instruction suivante.
- Modifiez les niveaux de facteur de `factor_survey_vector` à `c("Female", "Male")`. Faites attention à l'ordre des éléments vectoriels.

`@hint`
Faites attention à l'ordre dans lequel vous devez saisir les niveaux de facteurs. Conseil : regardez l'ordre dans lequel les niveaux sont affichés lorsque vous tapez `levels(factor_survey_vector)`.

`@pre_exercise_code`
```{r}
# no pec
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
```

`@sample_code`
```{r}
# Code to build factor_survey_vector
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)

# Specify the levels of factor_survey_vector
levels(factor_survey_vector) <-

factor_survey_vector
```

`@solution`
```{r}
# Code to build factor_survey_vector
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)

# Specify the levels of factor_survey_vector
levels(factor_survey_vector) <- c("Female", "Male")

factor_survey_vector
```

`@sct`
```{r}
msg = "Ne modifiez pas la définition de `survey_vector` !"
ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)
msg = "Ne modifiez pas ou ne supprimez pas le code pour créer le vecteur de facteurs."
ex()  %>% check_function("factor", not_called_msg = msg) %>% check_arg('x')  %>% check_equal(incorrect_msg = msg)

# MC-note: ideally would want to test assign operator `<-`, and have it highlight whole line.

# MC-note: or negate this test_student_typed, to highlight where they type this incorrect phrase
# test_student_typed('c("Male", "Female")')

ex()  %>% check_object("factor_survey_vector") %>% check_equal(eq_condition = "equal", incorrect_msg = paste("Avez-vous attribué les bons niveaux de facteur à `factor_survey_vector` ? Utilisez `levels(factor_survey_vector) <- c(\"Female\", \"Male\")`. N'oubliez pas que R est sensible à la casse !"))

success_msg("Magnifique ! Passez à l'exercice suivant.")
```

---

## Résumer un facteur

```yaml
type: NormalExercise
key: a549f13c0644ccc89cd39a10aa48706754637ed0
xp: 100
skills:
  - 1
```

Après avoir terminé ce cours, l'une de vos fonctions préférées dans R sera `summary()`. Cela vous donnera un aperçu rapide du contenu d'une variable :

```
summary(my_var)
```

Pour en revenir à notre enquête, vous aimeriez savoir combien de réponses `"Male"` vous avez dans votre étude, et combien de réponses `"Female"`. La fonction `summary` vous donne la réponse à cette question.

`@instructions`
Demandez à `summary()` de consulter `survey_vector` et `factor_survey_vector`. Interprétez les résultats des deux vecteurs. Sont-ils aussi utiles l'un que l'autre dans ce cas ?

`@hint`
Appelez la fonction `summary()` sur `survey_vector` et `factor_survey_vector`, c'est aussi simple que cela.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")
factor_survey_vector

# Generate summary for survey_vector


# Generate summary for factor_survey_vector

```

`@solution`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")
factor_survey_vector

# Generate summary for survey_vector
summary(survey_vector)

# Generate summary for factor_survey_vector
summary(factor_survey_vector)
```

`@sct`
```{r}
msg = "Ne changez rien aux premières lignes qui définissent `survey_vector` et `factor_survey_vector`."

ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_survey_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

msg <- "Avez-vous correctement utilisé `summary()` pour générer un résumé pour `%s` ?"

ex() %>% check_output_expr("summary(survey_vector)", missing_msg = sprintf(msg, "survey_vector"))

ex() %>% check_output_expr("summary(factor_survey_vector)", missing_msg = sprintf(msg, "factor_survey_vector"))

success_msg("Bien joué ! Jetez un œil à la sortie. Le fait que vous ayez identifié `\"Male\"` et `\"Female\"` comme niveaux de facteur dans `factor_survey_vector` permet à R de montrer le nombre d'éléments pour chaque catégorie.")
```

---

## Bataille des sexes

```yaml
type: NormalExercise
key: 90ecc160d1ebf2f75bf53f9c3843fc1632bdd0a5
xp: 100
skills:
  - 1
```

Vous vous demandez peut-être ce qui se passe lorsque vous essayez de comparer les éléments d'un même facteur.  Dans `factor_survey_vector`, vous avez un facteur à deux niveaux : `"Male"` et `"Female"`. Mais comment R évalue-t-il ces éléments les uns par rapport aux autres ?

`@instructions`
Lisez le code dans l'éditeur et soumettez la réponse pour tester si `male` est plus grand que (`>`) `female`.

`@hint`
Il suffit d’envoyer la réponse et de jeter un coup d'œil à la sortie affichée sur la console.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")

# Male
male <- factor_survey_vector[1]

# Female
female <- factor_survey_vector[2]

# Battle of the sexes: Male 'larger' than female?
male > female
```

`@solution`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")

# Male
male <- factor_survey_vector[1]

# Female
female <- factor_survey_vector[2]

# Battle of the sexes: Male 'larger' than female?
male > female
```

`@sct`
```{r}
msg = "Ne changez rien au code ; soumettez simplement la réponse et regardez le résultat."

ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_survey_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("male", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("female", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("male > female", missing_msg = msg)


success_msg("Comme c'est intéressant ! Par défaut, R renvoie `NA` lorsque vous essayez de comparer des valeurs dans un facteur, car l'idée n'a pas de sens. Ensuite, vous apprendrez les facteurs ordonnés, où des comparaisons plus significatives sont possibles.")
```

---

## Facteurs ordonnés

```yaml
type: NormalExercise
key: 9ab0928916bf84ab225713a9a1ce40d9e322c6a0
xp: 100
skills:
  - 1
```

Comme `"Male"` et `"Female"` sont des niveaux de facteurs non ordonnés (ou nominaux), R renvoie un message d'avertissement, vous indiquant que l'opérateur « supérieur à » n'est pas significatif. Comme nous l'avons vu précédemment, R attribue une valeur égale aux niveaux de ces facteurs.

Mais ce n'est pas toujours le cas. Parfois, vous aurez également affaire à des facteurs dont les catégories sont naturellement ordonnées. Si c'est le cas, il faut s’assurer que cette information est transmise à R…

Supposons que vous dirigiez une équipe de recherche composée de cinq analystes de données et que vous souhaitiez évaluer leurs performances. Pour ce faire, vous suivez leur rapidité, évaluez chaque analyste comme `"slow"`, `"medium"` ou `"fast"`, et enregistrez les résultats dans `speed_vector`.

`@instructions`
Dans un premier temps, attribuez à `speed_vector` un vecteur à 5 entrées, une pour chaque analyste. Chaque entrée doit être soit `"slow"`, `"medium"`, soit `"fast"`. Utilisez la liste ci-dessous :

- L'analyste 1 est moyen,
- L'analyste 2 est lent,
- L'analyste 3 est lent,
- L'analyste 4 est moyen et
- L'analyste 5 est rapide.

Il n'est pas encore nécessaire de préciser ces facteurs.

`@hint`
Affectez à `speed_vector` un vecteur contenant les chaînes de caractères `"slow"`, `"medium"`, ou `"fast"`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create speed_vector
speed_vector <-
```

`@solution`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
```

`@sct`
```{r}
ex() %>% check_object("speed_vector") %>% check_equal(incorrect_msg = "`speed_vector` doit être un vecteur avec 5 entrées, une pour chaque évaluation de vitesse de l'analyste. N'utilisez pas de lettres majuscules ; R est sensible à la casse !")

success_msg("Un travail bien fait ! Continuez à l'exercice suivant.")
```

---

## Facteurs ordonnés (2)

```yaml
type: NormalExercise
key: 279077d10248ce03d5f972939ef8576430a16683
xp: 100
skills:
  - 1
```

`speed_vector` doit être converti en facteur ordinal car ses catégories ont un ordre naturel. Par défaut, la fonction `factor()` transforme `speed_vector` en un facteur non ordonné. Pour créer un facteur ordonné, vous devez ajouter deux arguments supplémentaires : `ordered` et `levels`.

```
factor(some_vector,

       ordered = TRUE,

       levels = c("lev1", "lev2" ...))

```

En fixant l'argument `ordered` à `TRUE` dans la fonction `factor()`, vous indiquez que le facteur est ordonné. Avec l'argument `levels`, vous donnez les valeurs du facteur dans l'ordre correct.

`@instructions`
À partir de `speed_vector`, créez un vecteur de facteurs ordonnés : `factor_speed_vector`. Attribuez la valeur `TRUE` à `ordered` et la valeur `levels` à `c("slow", "medium", "fast")`.

`@hint`
Utilisez la fonction `factor()` pour créer `factor_speed_vector` à partir de `speed_character_vector`. L'argument `ordered` doit être remplacé par `TRUE` puisqu'il y a un ordre naturel. Définissez également `levels = c("slow", "medium", "fast")`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")

# Convert speed_vector to ordered factor vector
factor_speed_vector <-

# Print factor_speed_vector
factor_speed_vector
summary(factor_speed_vector)
```

`@solution`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")

# Convert speed_vector to ordered factor vector
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Print factor_speed_vector
factor_speed_vector
summary(factor_speed_vector)
```

`@sct`
```{r}
msg = "Ne changez rien à la commande qui spécifie la variable `speed_vector`."

ex() %>% check_object("speed_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_function("factor") %>% {
	check_arg(., 'x') %>% check_equal(incorrect_msg="Le premier argument que vous passez à `factor()` doit être `speed_vector`.")
	check_arg(., 'ordered') %>% check_equal(incorrect_msg="Assurez-vous de définir `ordered = TRUE` dans votre appel de `factor()`.")
	check_arg(., 'levels') %>% check_equal(incorrect_msg="Assurez-vous de définir `levels = c(\"slow\", \"medium\", \"fast\")` dans votre appel de `factor()`.")
	}

ex() %>% check_object("factor_speed_vector") %>% check_equal(eq_condition = "equal", incorrect_msg="Il y a encore quelque chose qui ne va pas avec `factor_speed_vector`; assurez-vous de ne passer que `speed_vector`, `ordered = TRUE` et `levels = c(\"slow\", \"medium\", \"fast\")` dans votre appel de `factor()`.")

                      
                      
success_msg("Super ! Jetez un œil à la console. Il est maintenant indiqué que les niveaux ont effectivement un ordre associé, avec le signe `<`. Passez à l'exercice suivant.")
```

---

## Comparaison de facteurs classés

```yaml
type: NormalExercise
key: db16e69805625bcfde227743a8cbc985f8482a37
xp: 100
skills:
  - 1
```

Après une mauvaise journée de travail, l'analyste de données numéro deux entre dans votre bureau et commence à se plaindre que l'analyste de données numéro cinq ralentit l'ensemble du projet. Comme vous savez que « l'analyste de données numéro deux » a la réputation d'être un petit malin, vous décidez d'abord de vérifier si son affirmation est vraie.

Le fait que `factor_speed_vector` soit maintenant ordonné nous permet de comparer différents éléments (les analystes de données dans ce cas). Pour ce faire, vous pouvez simplement utiliser les opérateurs bien connus.

`@instructions`
- Utilisez `[2]` pour sélectionner à partir de `factor_speed_vector` la valeur du facteur pour le deuxième analyste de données. Conservez-le dans `da2`.
- Utilisez `[5]` pour sélectionner la valeur du facteur `factor_speed_vector` pour le cinquième analyste de données. Conservez-le dans `da5`.
- Vérifiez si `da2` est supérieur à `da5` ; affichez simplement le résultat. N'oubliez pas que vous pouvez utiliser l'opérateur `>` pour vérifier si un élément est plus grand que l'autre.

`@hint`
- Pour sélectionner la valeur du facteur pour le troisième analyste de données, vous avez besoin de `factor_speed_vector[3]`.
- Pour comparer deux valeurs, vous pouvez utiliser `>`. Par exemple :

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create factor_speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Factor value for second data analyst
da2 <-

# Factor value for fifth data analyst
da5 <-

# Is data analyst 2 faster than data analyst 5?

```

`@solution`
```{r}
# Create factor_speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Factor value for second data analyst
da2 <- factor_speed_vector[2]

# Factor value for fifth data analyst
da5 <- factor_speed_vector[5]

# Is data analyst 2 faster data analyst 5?
da2 > da5
```

`@sct`
```{r}
msg = "Ne changez rien aux commandes qui définissent `speed_vector` et `factor_speed_vector` !"
ex() %>% check_object("speed_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_speed_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)


msg <- "Avez-vous correctement sélectionné la valeur du facteur pour le %s data analyst ? Vous pouvez utiliser `factor_speed_vector[%s]`."
ex() %>% check_object("da2") %>% check_equal(eq_condition = "equal", incorrect_msg = sprintf(msg,"second", "2"))

ex() %>% check_object("da5") %>% check_equal(eq_condition = "equal",incorrect_msg = sprintf("fifth", "5"))

ex() %>% check_output_expr("da2 > da5", missing_msg = "Avez-vous correctement comparé `da2` et `da5` ? Vous pouvez utiliser le `>`. Imprimez simplement le résultat.")


success_msg("Bellissimo ! Que vous dit le résultat ? Le data analyst deux se plaint du data analyst cinq alors qu'en fait c'est lui qui ralentit tout ! Cela conclut le chapitre sur les facteurs. Avec une base solide en vecteurs, matrices et facteurs, vous êtes prêt à plonger dans le monde merveilleux des data frames, une structure de données très importante en R !")
```
