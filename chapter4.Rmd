---
title_meta: Hoofdstuk 4
title: Factors
description: >-
  Gegevens vallen vaak in een beperkt aantal categorieën. Zo kun je haarkleur
  bijvoorbeeld indelen in zwart, bruin, blond, rood, grijs of wit, en misschien
  nog een paar opties voor mensen die hun haar verven. In R wordt categorische
  data opgeslagen in factors. Factors zijn erg belangrijk in data-analyse, dus
  begin nu met leren hoe je ze maakt, subset en vergelijkt.
---

## Wat is een factor en waarom zou je die gebruiken?

```yaml
type: NormalExercise
key: 05273321916d99bb9c0deadf75c6834d25a47244
xp: 100
skills:
  - 1
```

In dit hoofdstuk duik je in de wonderlijke wereld van **factors**.

De term factor verwijst naar een statistisch gegevenstype dat categorische variabelen opslaat. Het verschil tussen een categorische en een continue variabele is dat een categorische variabele kan behoren tot een **beperkt aantal categorieën**. Een continue variabele daarentegen kan overeenkomen met een oneindig aantal waarden.

Het is belangrijk dat R weet of het met een continue of een categorische variabele te maken heeft, omdat de statistische modellen die je later gaat bouwen beide typen anders behandelen. (Je ziet later waarom dat zo is.)

Een goed voorbeeld van een categorische variabele is sekse. In veel situaties kun je de categorieën voor sekse beperken tot "Mannelijk" of "Vrouwelijk". (Soms heb je andere categorieën nodig. Bijvoorbeeld als je rekening moet houden met chromosomale variatie, hermafrodiete dieren of verschillende culturele normen, maar het aantal categorieën blijft altijd eindig.)

`@instructions`
Wijs aan de variabele `theory` de waarde `"factors"` toe.

`@hint`
Wijs simpelweg een variabele toe (`<-`); let op het gebruik van hoofdletters.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Assign to the variable theory what this chapter is about!
```

`@solution`
```{r}
# Assign to the variable theory what this chapter is about!
theory <- "factors"
```

`@sct`
```{r}
msg_undef <- "Het lijkt erop dat je de variabele `theory` niet hebt gedefinieerd."
msg_incor <- "De waarde van `theory` lijkt onjuist. Zorg ervoor dat je de tekenreeks `\"factors\"` toewijst. Vergeet niet dat R hoofdlettergevoelig is."
msg_err <- "Zorg ervoor dat je `theory` correct hebt gedefinieerd, met behulp van `<-` voor toewijzing."

# If get error and theory is undefined, point out the error
ex()  %>% check_or(check_error(.,msg_err), check_object(.,"theory")  %>% check_equal(eval = FALSE))

check_object(.,"theory", undefined_msg = msg_undef)  %>% check_equal(incorrect_msg = msg_incor)
success_msg("Goed gedaan! Klaar om te beginnen? Ga verder met de volgende oefening!")
```

---

## Wat is een factor en waarom zou je die gebruiken? (2)

```yaml
type: NormalExercise
key: 6cc21c842b075347926bb1b244782213df32e370
xp: 100
skills:
  - 1
```

Om factoren in R te maken, gebruik je de functie `factor()`. Het eerste wat je moet doen, is een vector maken met alle observaties die tot een beperkt aantal categorieën behoren. Zo bevat `sex_vector` bijvoorbeeld het geslacht van 5 verschillende personen:

```
sex_vector <- c("Male","Female","Female","Male","Male")
```

Het is duidelijk dat er hier twee categorieën, of in R-termen **'factor levels'**, zijn: <span translate="no">"Male"</span> en <span translate="no">"Female"</span>.

De functie `factor()` codeert de vector als een factor:

```
factor_sex_vector <- factor(sex_vector)
```

`@instructions`
- Zet de character-vector `sex_vector` om naar een factor met `factor()` en ken het resultaat toe aan `factor_sex_vector`.
- Print `factor_sex_vector` en controleer of R de factor levels onder de daadwerkelijke waarden afdrukt.

`@hint`
Gebruik simpelweg de functie `factor()` voor `sex_vector`. Kijk nog eens naar de opdracht; het antwoord staat al ergens...

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Sex vector
sex_vector <- c("Male", "Female", "Female", "Male", "Male")

# Convert sex_vector to a factor
factor_sex_vector <-

# Print out factor_sex_vector

```

`@solution`
```{r}
# Sex vector
sex_vector <- c("Male", "Female", "Female", "Male", "Male")

# Convert sex_vector to a factor
factor_sex_vector <- factor(sex_vector)

# Print out factor_sex_vector
factor_sex_vector
```

`@sct`
```{r}
ex()  %>%  check_object("factor_sex_vector")  %>% check_equal(incorrect_msg = "Heb je de factor van `sex_vector` toegewezen aan `factor_sex_vector`?")
ex()  %>%  check_output_expr("factor_sex_vector", missing_msg = "Vergeet niet om `factor_sex_vector` af te drukken!")

success_msg("Geweldig! Als je meer wilt weten over de `factor()` functie, aarzel dan niet om `?factor` in de console te typen. Dit opent een help-pagina. Ga verder naar de volgende oefening.")
```

---

## Wat is een factor en waarom zou je die gebruiken? (3)

```yaml
type: NormalExercise
key: 5bd4f50afc2c2dbc881e16b8ca94ca56960dff42
xp: 100
skills:
  - 1
```

Er zijn twee soorten categorische variabelen: een **nominale categorische variabele** en een **ordinale categorische variabele**.

Een nominale variabele is een categorische variabele zonder impliciete volgorde. Dat betekent dat je niet kunt zeggen dat "de ene meer waard is dan de andere". Denk bijvoorbeeld aan de categorische variabele `animals_vector` met de categorieën `"Olifant"`, `"Giraf"`, `"Ezel"` en `"Paard"`. Hier is het onmogelijk te zeggen dat de ene boven of onder de andere staat. (Sommigen van jullie zijn het daar misschien niet mee eens ;-) ).

Daarentegen hebben ordinale variabelen wel een natuurlijke volgorde. Neem bijvoorbeeld de categorische variabele `temperature_vector` met de categorieën: `"Low"`, `"Medium"` en `"High"`. Hier is het duidelijk dat `"Medium"` boven `"Low"` staat en `"High"` boven `"Medium"`.

`@instructions`
Verzend het antwoord om te zien hoe R nominale en ordinale variabelen opbouwt en afdrukt. Maak je geen zorgen als je nog niet alle code begrijpt; daar komen we zo op terug.

`@hint`
Verzend het antwoord en kijk in de console. Let op hoe R de volgorde van de factor levels aangeeft voor ordinale categorische variabelen.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Animals
animals_vector <- c("Elephant", "Giraffe", "Donkey", "Horse")
factor_animals_vector <- factor(animals_vector)
factor_animals_vector

# Temperature
temperature_vector <- c("High", "Low", "High","Low", "Medium")
factor_temperature_vector <- factor(temperature_vector, order = TRUE, levels = c("Low", "Medium", "High"))
factor_temperature_vector
```

`@solution`
```{r}
# Animals
animals_vector <- c("Elephant", "Giraffe", "Donkey", "Horse")
factor_animals_vector <- factor(animals_vector)
factor_animals_vector

# Temperature
temperature_vector <- c("High", "Low", "High","Low", "Medium")
factor_temperature_vector <- factor(temperature_vector, order = TRUE, levels = c("Low", "Medium", "High"))
factor_temperature_vector
```

`@sct`
```{r}
msg <- "Verander niets aan de voorbeeldcode. Dien simpelweg het antwoord in en bekijk de oplossing!"

ex() %>% check_object("animals_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("temperature_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_animals_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("factor_animals_vector", missing_msg = msg)

ex() %>% check_object("factor_temperature_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("factor_temperature_vector", missing_msg = msg)


success_msg("Kun je al zien wat er gebeurt in deze oefening? Geweldig! Ga door naar de volgende oefening en duik in de details van factor levels.")
```

---

## Factor levels

```yaml
type: NormalExercise
key: 1aa698978d32d1a0befa4700d7da85a648e1d69e
xp: 100
skills:
  - 1
```

Als je voor het eerst met een gegevensset werkt, merk je vaak dat die factoren bevat met bepaalde factor levels. Soms wil je de namen van die levels aanpassen ter verduidelijking of om andere redenen. In R kan dat met de functie `levels()`:

```
levels(factor_vector) <- c("name1", "name2",...)
```

Een goed voorbeeld is ruwe data uit een enquête. Een veelgestelde vraag in elke vragenlijst is het geslacht van de respondent. Hier zijn, voor de eenvoud, slechts twee categorieën vastgelegd: `"M"` en `"F"`. (Voor enquêtegegevens heb je doorgaans meer categorieën nodig; hoe dan ook, je gebruikt een factor om categorische data op te slaan.) 

```
survey_vector <- c("M", "F", "F", "M", "M")
```

Het noteren van geslacht met de afkortingen `"M"` en `"F"` is handig als je met pen en papier gegevens verzamelt, maar kan tijdens de analyse voor verwarring zorgen. Op dat moment wil je de factor levels vaak aanpassen naar `"Male"` en `"Female"` in plaats van `"M"` en `"F"` voor meer duidelijkheid.

**Let op:** de volgorde waarin je de levels toewijst is belangrijk. Als je `levels(factor_survey_vector)` typt, zie je als output `[1] "F" "M"`. Als je de levels niet opgeeft bij het maken van de vector, wijst `R` ze automatisch alfabetisch toe. Om `"F"` correct te koppelen aan `"Female"` en `"M"` aan `"Male"`, moeten de levels worden ingesteld op `c("Female", "Male")`, in deze volgorde.

`@instructions`
- Bekijk de code die een factorvector maakt van `survey_vector`. Gebruik `factor_survey_vector` in de volgende stap.
- Pas de factor levels van `factor_survey_vector` aan naar `c("Female", "Male")`. Let op de volgorde van de vectorelementen.

`@hint`
Let op de volgorde waarin je de factor levels invult. Tip: kijk naar de volgorde waarin de levels worden getoond als je `levels(factor_survey_vector)` typt.

`@pre_exercise_code`
```{r}
# no pec
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
```

`@sample_code`
```{r}
# Code to build factor_survey_vector
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)

# Specify the levels of factor_survey_vector
levels(factor_survey_vector) <-

factor_survey_vector
```

`@solution`
```{r}
# Code to build factor_survey_vector
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)

# Specify the levels of factor_survey_vector
levels(factor_survey_vector) <- c("Female", "Male")

factor_survey_vector
```

`@sct`
```{r}
msg = "Verander de definitie van `survey_vector` niet!"
ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)
msg = "Verander of verwijder de code om de factorvector te maken niet."
ex()  %>% check_function("factor", not_called_msg = msg) %>% check_arg('x')  %>% check_equal(incorrect_msg = msg)

# MC-note: ideally would want to test assign operator `<-`, and have it highlight whole line.

# MC-note: or negate this test_student_typed, to highlight where they type this incorrect phrase
# test_student_typed('c("Male", "Female")')

ex()  %>% check_object("factor_survey_vector") %>% check_equal(eq_condition = "equal", incorrect_msg = paste("Heb je de juiste factorlevels toegewezen aan `factor_survey_vector`? Gebruik `levels(factor_survey_vector) <- c(\"Female\", \"Male\")`. Vergeet niet dat R hoofdlettergevoelig is!"))


success_msg("Geweldig! Ga door naar de volgende oefening.")
```

---

## Een factor samenvatten

```yaml
type: NormalExercise
key: a549f13c0644ccc89cd39a10aa48706754637ed0
xp: 100
skills:
  - 1
```

Na deze cursus wordt `summary()` een van je favoriete functies in R. Hiermee krijg je snel een overzicht van de inhoud van een variabele:

```
summary(my_var)
```

Terug naar onze enquête: je wilt weten hoe vaak er `"Male"`en hoe vaak er `"Female"`is geantwoord in je studie. De functie `summary` geeft je het antwoord op deze vraag.

`@instructions`
Vraag een `summary()` op van `survey_vector` en `factor_survey_vector`. Interpreteer de resultaten van beide vectors. Zijn ze in dit geval even nuttig?

`@hint`
Roep de functie `summary()` aan voor zowel `survey_vector` als `factor_survey_vector`, zo simpel is het!

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")
factor_survey_vector

# Generate summary for survey_vector


# Generate summary for factor_survey_vector

```

`@solution`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")
factor_survey_vector

# Generate summary for survey_vector
summary(survey_vector)

# Generate summary for factor_survey_vector
summary(factor_survey_vector)
```

`@sct`
```{r}
msg = "Verander niets aan de eerste paar regels die `survey_vector` en `factor_survey_vector` definiëren."

ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_survey_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

msg <- "Heb je `summary()` correct gebruikt om een samenvatting voor `%s` te genereren?"

ex() %>% check_output_expr("summary(survey_vector)", missing_msg = sprintf(msg, "survey_vector"))

ex() %>% check_output_expr("summary(factor_survey_vector)", missing_msg = sprintf(msg, "factor_survey_vector"))

success_msg("Mooi! Kijk eens naar de output. Het feit dat je `\"Male\"` en `\"Female\"` als factorlevels in `factor_survey_vector` hebt geïdentificeerd, stelt R in staat om het aantal elementen voor elke categorie te tonen.")
```

---

## Strijd der seksen

```yaml
type: NormalExercise
key: 90ecc160d1ebf2f75bf53f9c3843fc1632bdd0a5
xp: 100
skills:
  - 1
```

Je vraagt je misschien af wat er gebeurt als je elementen van een factor probeert te vergelijken. In `factor_survey_vector` heb je een factor met twee niveaus: `"Male"` en `"Female"`. Maar hoe waardeert R deze ten opzichte van elkaar?

`@instructions`
Lees de code in de editor en voer deze uit om te testen of `male` groter is dan (`>`) `female`.

`@hint`
Verzend gewoon het antwoord en bekijk de uitvoer die in de console wordt afgedrukt.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")

# Male
male <- factor_survey_vector[1]

# Female
female <- factor_survey_vector[2]

# Battle of the sexes: Male 'larger' than female?
male > female
```

`@solution`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")

# Male
male <- factor_survey_vector[1]

# Female
female <- factor_survey_vector[2]

# Battle of the sexes: Male 'larger' than female?
male > female
```

`@sct`
```{r}
msg = "Verander niets aan de code; dien simpelweg het antwoord in en bekijk het resultaat."

ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_survey_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("male", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("female", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("male > female", missing_msg = msg)


success_msg("Wat interessant! Standaard geeft R `NA` terug wanneer je probeert waarden in een factor te vergelijken, omdat het idee niet logisch is. Vervolgens leer je over geordende factoren, waar zinvollere vergelijkingen mogelijk zijn.")
```

---

## Geordende factors

```yaml
type: NormalExercise
key: 9ab0928916bf84ab225713a9a1ce40d9e322c6a0
xp: 100
skills:
  - 1
```

Omdat `"Male"` en `"Female"` ongeordende (of nominale) factor levels zijn, geeft R een waarschuwing dat de groter-dan-operator hier niet zinvol is. Zoals je eerder zag, kent R voor dit soort factoren gelijke waarden toe aan de levels.

Maar dat is niet altijd zo! Soms werk je met factoren waarvan de categorieën wel een natuurlijke volgorde hebben. In dat geval moet je R die informatie meegeven...

Stel dat je een onderzoeksteam van vijf data-analisten leidt en hun prestaties wilt beoordelen. Je houdt hun snelheid bij, beoordeelt elke analist als `"slow"`, `"medium"` of `"fast"`, en slaat de resultaten op in `speed_vector`.

`@instructions`
Wijs in de eerste stap `speed_vector` een vector toe met 5 elementen, één voor elke analist. Elk element is ofwel `"slow"`, `"medium"` of `"fast"`. Gebruik de onderstaande lijst:

- Analist 1 is medium,
- Analist 2 is slow,
- Analist 3 is slow,
- Analist 4 is medium en
- Analist 5 is fast.

Je hoeft nog niet aan te geven dat dit factoren zijn.

`@hint`
Wijs aan `speed_vector` een vector toe met de tekenreeksen `"slow"`, `"medium"` of `"fast"`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create speed_vector
speed_vector <-
```

`@solution`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
```

`@sct`
```{r}
ex() %>% check_object("speed_vector") %>% check_equal(incorrect_msg = "`speed_vector` moet een vector zijn met 5 items, één voor de snelheidsscore van elke analist. Gebruik geen hoofdletters; R is hoofdlettergevoelig!")

success_msg("Goed gedaan! Ga door naar de volgende oefening.")
```

---

## Geordende factoren (2)

```yaml
type: NormalExercise
key: 279077d10248ce03d5f972939ef8576430a16683
xp: 100
skills:
  - 1
```

`speed_vector` moet worden omgezet naar een ordinale factor, omdat de categorieën een natuurlijke volgorde hebben. Standaard zet de functie `factor()` `speed_vector` om in een ongeordende factor. Om een geordende factor te maken, moet je twee extra argumenten toevoegen: `ordered` en `levels`.

```
factor(some_vector,
       ordered = TRUE,
       levels = c("lev1", "lev2" ...))
```

Door het argument `ordered` op `TRUE` te zetten in de functie `factor()`, geef je aan dat de factor geordend is. Met het argument `levels` geef je de waarden van de factor in de juiste volgorde op.

`@instructions`
Maak van `speed_vector` een geordende factorvector: `factor_speed_vector`. Zet `ordered` op `TRUE` en stel `levels` in op `c("slow", "medium", "fast")`.

`@hint`
Gebruik de functie `factor()` om `factor_speed_vector` te maken op basis van `speed_character_vector`. Zet het argument `ordered` op `TRUE`, omdat er een natuurlijke volgorde is. Stel ook `levels = c("slow", "medium", "fast")` in.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")

# Convert speed_vector to ordered factor vector
factor_speed_vector <-

# Print factor_speed_vector
factor_speed_vector
summary(factor_speed_vector)
```

`@solution`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")

# Convert speed_vector to ordered factor vector
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Print factor_speed_vector
factor_speed_vector
summary(factor_speed_vector)
```

`@sct`
```{r}
msg = "Verander niets aan het commando dat de variabele `speed_vector` specificeert."

ex() %>% check_object("speed_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_function("factor") %>% {
	check_arg(., 'x') %>% check_equal(incorrect_msg="Het eerste argument dat je aan `factor()` doorgeeft, moet `speed_vector` zijn.")
	check_arg(., 'ordered') %>% check_equal(incorrect_msg="Zorg ervoor dat je `ordered = TRUE` instelt in je aanroep van `factor()`.")
	check_arg(., 'levels') %>% check_equal(incorrect_msg="Zorg ervoor dat je `levels = c(\"slow\", \"medium\", \"fast\")` instelt in je aanroep van `factor()`.")
	}

ex() %>% check_object("factor_speed_vector") %>% check_equal(eq_condition = "equal", incorrect_msg="Er is nog steeds iets mis met `factor_speed_vector`; zorg ervoor dat je alleen `speed_vector`, `ordered = TRUE` en `levels = c(\"slow\", \"medium\", \"fast\")` doorgeeft in je aanroep van `factor()`.")

                      
                      
success_msg("Geweldig! Kijk eens naar de console. Het is nu aangegeven dat de Levels inderdaad een volgorde hebben, met het `<` teken. Ga door naar de volgende oefening.")
```

---

## Geordende factoren vergelijken

```yaml
type: NormalExercise
key: db16e69805625bcfde227743a8cbc985f8482a37
xp: 100
skills:
  - 1
```

‘Data-analist nummer twee’ heeft een slechte dag op het werk en stormt je kantoor binnen. Hij klaagt dat ‘data-analist nummer vijf’ het hele project vertraagt. Omdat je weet dat ‘data-analist nummer twee’ bekendstaat als een betweter, besluit je eerst te checken of zijn uitspraak klopt.

Doordat `factor_speed_vector` nu geordend is, kunnen we verschillende elementen (in dit geval de data-analisten) met elkaar vergelijken. Dat kan eenvoudig met de bekende operatoren.

`@instructions`
- Gebruik `[2]` om de factorwaarde voor de tweede data-analist uit `factor_speed_vector` te selecteren. Sla deze op als `da2`.
- Gebruik `[5]` om de factorwaarde voor de vijfde data-analist uit `factor_speed_vector` te selecteren. Sla deze op als `da5`.
- Controleer of `da2` groter is dan `da5` en print het resultaat. Onthoud dat je de `>`-operator kunt gebruiken om te checken of het ene element groter is dan het andere.

`@hint`
- Om de factorwaarde voor de derde data-analist te selecteren, heb je `factor_speed_vector[3]` nodig.
- Om twee waarden te vergelijken, kun je `>` gebruiken. Bijvoorbeeld: `da3 > da4`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create factor_speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Factor value for second data analyst
da2 <-

# Factor value for fifth data analyst
da5 <-

# Is data analyst 2 faster than data analyst 5?

```

`@solution`
```{r}
# Create factor_speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Factor value for second data analyst
da2 <- factor_speed_vector[2]

# Factor value for fifth data analyst
da5 <- factor_speed_vector[5]

# Is data analyst 2 faster data analyst 5?
da2 > da5
```

`@sct`
```{r}
msg = "Verander niets aan de opdrachten die `speed_vector` en `factor_speed_vector` definiëren!"
ex() %>% check_object("speed_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_speed_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)


msg <- "Heb je de factorwaarde correct geselecteerd voor de %s data-analist? Je kunt `factor_speed_vector[%s]` gebruiken."
ex() %>% check_object("da2") %>% check_equal(eq_condition = "equal", incorrect_msg = sprintf(msg,"tweede", "2"))

ex() %>% check_object("da5") %>% check_equal(eq_condition = "equal",incorrect_msg = sprintf("vijfde", "5"))

ex() %>% check_output_expr("da2 > da5", missing_msg = "Heb je `da2` en `da5` correct vergeleken? Je kunt de `>` gebruiken. Print gewoon het resultaat uit.")


success_msg("Bellissimo! Wat vertelt het resultaat je? Data-analist twee klaagt over data-analist vijf terwijl zij degene zijn die alles vertragen! Dit sluit het hoofdstuk over factoren af. Met een solide basis in vectoren, matrices en factoren, ben je klaar om te duiken in de wonderlijke wereld van dataframes, een zeer belangrijke datastructuur in R!")
```
