---
title_meta: Chapter 4
title: 팩터
description: >-
  데이터는 종종 제한된 몇 가지 범주로 나뉩니다. 예를 들어 사람의 머리카락 색은 검정, 갈색, 금발, 빨강, 회색, 흰색 등으로 구분할 수
  있고, 염색에 따라 더 많은 선택지도 있겠죠. R에서는 이런 범주형 데이터를 팩터로 저장합니다. 팩터는 데이터 분석에서 매우 중요하니,
  지금부터 팩터를 생성하고, 부분 집합을 만들고, 비교하는 방법을 배워 보세요.
---

## 요인은 무엇이며, 왜 사용할까요?

```yaml
type: NormalExercise
key: 05273321916d99bb9c0deadf75c6834d25a47244
xp: 100
skills:
  - 1
```

이번 장에서는 **요인(factor)** 의 세계를 살펴봅니다.

factor는 범주형 변수를 저장하는 통계 데이터 유형을 뜻합니다. 범주형 변수와 연속형 변수의 차이는, 범주형 변수는 **제한된 개수의 범주**에 속한다는 점입니다. 반면 연속형 변수는 이론적으로 무한한 값에 대응할 수 있습니다.

R이 어떤 변수가 연속형인지 범주형인지 아는 것은 매우 중요합니다. 앞으로 만들 통계 모델에서 두 변수를 다루는 방식이 다르기 때문입니다. (왜 그런지는 뒤에서 확인하게 됩니다.)

범주형 변수의 좋은 예는 성별입니다. 많은 경우 성별 범주는 "Male" 또는 "Female"로 제한할 수 있습니다. (상황에 따라 다른 범주가 필요할 수도 있습니다. 예를 들어 염색체 변이, 자웅동체 동물, 문화적 규범 등을 고려해야 할 수 있지만, 언제나 범주의 수는 유한합니다.)

`@instructions`
변수 `theory` 에 값 `"factors"` 를 할당하세요.

`@hint`
변수에 값을 할당하세요 (`<-`). 대소문자를 정확히 구분해 주세요.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Assign to the variable theory what this chapter is about!
```

`@solution`
```{r}
# Assign to the variable theory what this chapter is about!
theory <- "factors"
```

`@sct`
```{r}
msg_undef <- "변수 `theory`를 정의하지 않은 것 같습니다."
msg_incor <- "`theory`의 값이 올바르지 않은 것 같습니다. 문자 문자열 `\"factors\"`를 할당했는지 확인하세요. R은 대소문자를 구분합니다."
msg_err <- "`theory`를 올바르게 정의했는지 확인하고, 할당에는 `<-`를 사용하세요."

# If get error and theory is undefined, point out the error
ex()  %>% check_or(check_error(.,msg_err), check_object(.,"theory")  %>% check_equal(eval = FALSE))

check_object(.,"theory", undefined_msg = msg_undef)  %>% check_equal(incorrect_msg = msg_incor)
success_msg("잘하셨습니다! 시작할 준비가 되셨나요? 다음 연습으로 계속하세요!")
```

---

## 요인은 무엇이고 왜 사용할까요? (2)

```yaml
type: NormalExercise
key: 6cc21c842b075347926bb1b244782213df32e370
xp: 100
skills:
  - 1
```

R에서 요인(factor)을 만들려면 `factor()` 함수를 사용합니다. 먼저 해야 할 일은 제한된 몇 가지 범주에 속하는 모든 관측값을 담은 벡터를 만드는 것입니다. 예를 들어, `sex_vector`에는 서로 다른 5명의 성별이 들어 있습니다:

```
sex_vector <- c("Male","Female","Female","Male","Male")
```

여기에는 두 가지 범주, 즉 R에서 말하는 **'요인 수준(factor levels)'**이 있습니다: <span translate="no">"Male"</span>과 <span translate="no">"Female"</span>.

`factor()` 함수는 이 벡터를 요인으로 인코딩합니다:

```
factor_sex_vector <- factor(sex_vector)
```

`@instructions`
- 문자 벡터 `sex_vector`를 `factor()`로 요인으로 변환하고 결과를 `factor_sex_vector`에 할당하세요.
- `factor_sex_vector`를 출력하고, 실제 값 아래에 R이 요인 수준을 함께 출력하는지 확인하세요.

`@hint`
`sex_vector`에 `factor()` 함수를 그대로 사용하면 됩니다. 할당 예시를 살펴보면, 정답이 이미 어딘가에 있어요...

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Sex vector
sex_vector <- c("Male", "Female", "Female", "Male", "Male")

# Convert sex_vector to a factor
factor_sex_vector <-

# Print out factor_sex_vector

```

`@solution`
```{r}
# Sex vector
sex_vector <- c("Male", "Female", "Female", "Male", "Male")

# Convert sex_vector to a factor
factor_sex_vector <- factor(sex_vector)

# Print out factor_sex_vector
factor_sex_vector
```

`@sct`
```{r}
ex()  %>%  check_object("factor_sex_vector")  %>% check_equal(incorrect_msg = "`sex_vector`의 팩터를 `factor_sex_vector`에 할당하셨습니까?")
ex()  %>%  check_output_expr("factor_sex_vector", missing_msg = "`factor_sex_vector`를 출력하는 것을 잊지 마세요!")

success_msg("훌륭합니다! `factor()` 함수에 대해 더 알고 싶다면, 주저하지 말고 콘솔에 `?factor`를 입력하세요. 그러면 도움말 페이지가 열립니다. 다음 연습으로 계속하세요.");
```

---

## 팩터란 무엇이며 왜 사용할까요? (3)

```yaml
type: NormalExercise
key: 5bd4f50afc2c2dbc881e16b8ca94ca56960dff42
xp: 100
skills:
  - 1
```

범주형 변수에는 두 가지 유형이 있습니다: **명목형 범주형 변수**와 **순서형 범주형 변수**.

명목형 변수는 암묵적인 순서가 없는 범주형 변수예요. 즉, ‘어느 한쪽이 다른 쪽보다 더 크다’라고 말할 수 없다는 뜻입니다. 예를 들어, 범주가 `"Elephant"`, `"Giraffe"`, `"Donkey"`, `"Horse"`인 범주형 변수 `animals_vector`를 떠올려 보세요. 이 경우 어느 것이 위나 아래에 있다고 말하기는 불가능합니다(물론 의견이 다를 수도 있겠죠 ;-) ).

반대로, 순서형 변수에는 자연스러운 순서가 있습니다. 예를 들어 `"Low"`, `"Medium"`, `"High"`라는 범주를 가진 범주형 변수 `temperature_vector`를 생각해 보세요. 여기서는 `"Medium"`이 `"Low"`보다 위에 있고, `"High"`가 `"Medium"`보다 위에 있다는 것이 분명합니다.

`@instructions`
답변을 제출해 R이 명목형과 순서형 변수를 어떻게 구성하고 출력하는지 확인하세요. 아직 모든 코드를 이해하지 못해도 괜찮아요. 곧 함께 살펴보겠습니다.

`@hint`
그냥 답변을 제출하고 콘솔 출력을 확인해 보세요. 순서형 범주형 변수의 경우 R이 팩터 레벨의 순서를 어떻게 표시하는지 살펴보세요.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Animals
animals_vector <- c("Elephant", "Giraffe", "Donkey", "Horse")
factor_animals_vector <- factor(animals_vector)
factor_animals_vector

# Temperature
temperature_vector <- c("High", "Low", "High","Low", "Medium")
factor_temperature_vector <- factor(temperature_vector, order = TRUE, levels = c("Low", "Medium", "High"))
factor_temperature_vector
```

`@solution`
```{r}
# Animals
animals_vector <- c("Elephant", "Giraffe", "Donkey", "Horse")
factor_animals_vector <- factor(animals_vector)
factor_animals_vector

# Temperature
temperature_vector <- c("High", "Low", "High","Low", "Medium")
factor_temperature_vector <- factor(temperature_vector, order = TRUE, levels = c("Low", "Medium", "High"))
factor_temperature_vector
```

`@sct`
```{r}
msg <- "샘플 코드에 대해 아무것도 변경하지 마십시오. 답을 제출하고 솔루션을 확인하십시오!"

ex() %>% check_object("animals_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("temperature_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_animals_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("factor_animals_vector", missing_msg = msg)

ex() %>% check_object("factor_temperature_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("factor_temperature_vector", missing_msg = msg)


success_msg("이 연습에서 무슨 일이 일어나고 있는지 이미 알 수 있습니까? 훌륭합니다! 다음 연습으로 넘어가서 팩터 레벨의 세부 사항을 알아보십시오.")
```

---

## 팩터 레벨

```yaml
type: NormalExercise
key: 1aa698978d32d1a0befa4700d7da85a648e1d69e
xp: 100
skills:
  - 1
```

데이터셋을 처음 받으면, 특정 레벨을 가진 팩터가 포함되어 있는 경우가 많습니다. 하지만 더 명확하게 하거나 다른 이유로 이 레벨의 이름을 바꾸고 싶을 때가 있죠. R에서는 `levels()` 함수를 사용해 이를 변경할 수 있어요:

```
levels(factor_vector) <- c("name1", "name2",...)
```

좋은 예로 설문조사에서 제공되는 원시 데이터를 들 수 있습니다. 대부분의 설문에는 응답자의 성별을 묻는 질문이 포함되는데요, 여기서는 단순화를 위해 `"M"`과 `"F"` 두 가지 범주만 기록되었다고 가정하겠습니다. (실제 설문 데이터에서는 더 많은 범주가 필요할 수 있지만, 어쨌든 범주형 데이터는 팩터로 저장합니다.) 

```
survey_vector <- c("M", "F", "F", "M", "M")
```

성별을 `"M"`과 `"F"` 같은 약어로 기록하면 펜과 종이로 데이터를 수집할 때는 편리하지만, 데이터를 분석할 때 혼란을 줄 수 있습니다. 분석 단계에서는 보통 명확성을 위해 `"M"`과 `"F"` 대신 `"Male"`과 `"Female"`로 팩터 레벨을 바꾸고 싶어질 거예요.

주의: 레벨을 할당하는 순서는 매우 중요합니다. `levels(factor_survey_vector)`를 입력하면 출력이 `[1] "F" "M"`임을 볼 수 있어요. 벡터를 만들 때 팩터의 레벨을 지정하지 않으면 `R`은 알파벳 순으로 자동 할당합니다. `"F"`를 `"Female"`에, `"M"`을 `"Male"`에 올바르게 매핑하려면 레벨을 이 순서로 `c("Female", "Male")`로 설정해야 합니다.

`@instructions`
- `survey_vector`에서 팩터 벡터를 만드는 코드를 확인하세요. 다음 지침에서는 `factor_survey_vector`를 사용합니다.
- `factor_survey_vector`의 팩터 레벨을 `c("Female", "Male")`로 변경하세요. 여기서 벡터 요소의 순서에 유의하세요.

`@hint`
팩터 레벨을 입력하는 순서에 주의하세요. 힌트: `levels(factor_survey_vector)`를 입력했을 때 출력되는 레벨의 순서를 확인해 보세요.

`@pre_exercise_code`
```{r}
# no pec
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
```

`@sample_code`
```{r}
# Code to build factor_survey_vector
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)

# Specify the levels of factor_survey_vector
levels(factor_survey_vector) <-

factor_survey_vector
```

`@solution`
```{r}
# Code to build factor_survey_vector
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)

# Specify the levels of factor_survey_vector
levels(factor_survey_vector) <- c("Female", "Male")

factor_survey_vector
```

`@sct`
```{r}
msg = " `survey_vector`의 정의를 변경하지 마십시오!"
ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)
msg = "팩터 벡터를 생성하는 코드를 변경하거나 제거하지 마십시오."
ex()  %>% check_function("factor", not_called_msg = msg) %>% check_arg('x')  %>% check_equal(incorrect_msg = msg)

# MC-note: ideally would want to test assign operator `<-`, and have it highlight whole line.

# MC-note: or negate this test_student_typed, to highlight where they type this incorrect phrase
# test_student_typed('c("Male", "Female")')

ex()  %>% check_object("factor_survey_vector") %>% check_equal(eq_condition = "equal", incorrect_msg = paste(" `factor_survey_vector`에 올바른 팩터 레벨을 할당했습니까? `levels(factor_survey_vector) <- c(\"Female\", \"Male\")`을 사용하십시오. R은 대소문자를 구분한다는 것을 기억하십시오!"))


success_msg("훌륭합니다! 다음 연습으로 진행하십시오.")
```

---

## 팩터 요약하기

```yaml
type: NormalExercise
key: a549f13c0644ccc89cd39a10aa48706754637ed0
xp: 100
skills:
  - 1
```

이 과정을 마치고 나면, R에서 가장 좋아하는 함수 중 하나가 `summary()`가 될 거예요. 이 함수는 변수의 내용을 빠르게 훑어볼 수 있게 해줍니다:

```
summary(my_var)
```

설문 예시로 돌아가서, 연구에 `"Male"` 응답이 몇 개이고 `"Female"` 응답이 몇 개인지 알고 싶다고 해볼게요. `summary` 함수가 이 질문에 대한 답을 제공합니다.

`@instructions`
`survey_vector`와 `factor_survey_vector`에 대해 각각 `summary()`를 호출하세요. 두 벡터의 결과를 해석해 보세요. 이 경우 두 결과가 똑같이 유용한가요?

`@hint`
`survey_vector`와 `factor_survey_vector` 각각에 `summary()` 함수를 호출하세요. 그대로 하면 됩니다!

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")
factor_survey_vector

# Generate summary for survey_vector


# Generate summary for factor_survey_vector

```

`@solution`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")
factor_survey_vector

# Generate summary for survey_vector
summary(survey_vector)

# Generate summary for factor_survey_vector
summary(factor_survey_vector)
```

`@sct`
```{r}
msg = "첫 번째 몇 줄에서 `survey_vector`와 `factor_survey_vector`를 정의하는 부분을 변경하지 마십시오."

ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_survey_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

msg <- "`%s`에 대한 요약을 생성하기 위해 `summary()`를 올바르게 사용하셨습니까?"

ex() %>% check_output_expr("summary(survey_vector)", missing_msg = sprintf(msg, "survey_vector"))

ex() %>% check_output_expr("summary(factor_survey_vector)", missing_msg = sprintf(msg, "factor_survey_vector"))

success_msg("좋습니다! 출력을 확인해 보세요. `factor_survey_vector`에서 `\"Male\"`과 `\"Female\"`을 팩터 수준으로 식별한 덕분에 R이 각 범주에 대한 요소 수를 표시할 수 있습니다.")
```

---

## 남녀의 대결

```yaml
type: NormalExercise
key: 90ecc160d1ebf2f75bf53f9c3843fc1632bdd0a5
xp: 100
skills:
  - 1
```

요인(factor)의 원소를 비교하면 어떻게 될지 궁금하실 수 있어요. `factor_survey_vector`에는 `"Male"`와 `"Female"` 두 수준(level)이 있는 요인이 들어 있어요. 그렇다면 R은 이 둘의 상대적인 값을 어떻게 판단할까요?

`@instructions`
에디터의 코드를 읽고, `male`이 `female`보다 큰지(`>`)를 테스트하기 위해 답변을 제출하세요.

`@hint`
그냥 답변을 제출하고 콘솔에 출력되는 결과를 확인해 보세요.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")

# Male
male <- factor_survey_vector[1]

# Female
female <- factor_survey_vector[2]

# Battle of the sexes: Male 'larger' than female?
male > female
```

`@solution`
```{r}
# Build factor_survey_vector with clean levels
survey_vector <- c("M", "F", "F", "M", "M")
factor_survey_vector <- factor(survey_vector)
levels(factor_survey_vector) <- c("Female", "Male")

# Male
male <- factor_survey_vector[1]

# Female
female <- factor_survey_vector[2]

# Battle of the sexes: Male 'larger' than female?
male > female
```

`@sct`
```{r}
msg = "코드에 대해 아무것도 변경하지 말고, 답을 제출한 후 결과를 확인하십시오."

ex() %>% check_object("survey_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_survey_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("male", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("female", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("male > female", missing_msg = msg)


success_msg("참 흥미롭습니다! 기본적으로, R은 팩터 내의 값을 비교하려고 할 때 `NA`를 반환합니다. 이는 그 아이디어가 의미가 없기 때문입니다. 다음으로, 더 의미 있는 비교가 가능한 순서가 있는 팩터에 대해 배우게 될 것입니다.")
```

---

## 순서가 있는 팩터

```yaml
type: NormalExercise
key: 9ab0928916bf84ab225713a9a1ce40d9e322c6a0
xp: 100
skills:
  - 1
```

`"Male"`과 `"Female"`은 순서가 없는(또는 명목형) 팩터 수준이기 때문에, R은 보다 큼 연산자가 의미가 없다고 경고 메시지를 반환합니다. 앞서 본 것처럼, 이런 팩터의 수준에는 R이 동일한 값을 부여해요.

하지만 항상 그런 것은 아닙니다! 때로는 범주 간에 자연스러운 순서가 있는 팩터를 다루기도 합니다. 이런 경우에는 이 정보를 R에 반드시 알려줘야 해요...

여러분이 다섯 명의 데이터 애널리스트로 구성된 연구팀을 이끌고 있고, 그들의 성과를 평가하려 한다고 가정해 볼게요. 이를 위해 각 애널리스트의 속도를 `"slow"`, `"medium"`, 또는 `"fast"`로 평가하고, 그 결과를 `speed_vector`에 저장하려고 합니다.

`@instructions`
첫 단계로, 각 애널리스트당 하나씩 총 5개의 항목을 가진 벡터를 `speed_vector`에 할당하세요. 각 항목은 `"slow"`, `"medium"`, 또는 `"fast"` 중 하나여야 합니다. 아래 목록을 사용하세요.

- 애널리스트 1은 medium,
- 애널리스트 2는 slow,
- 애널리스트 3은 slow,
- 애널리스트 4는 medium, 그리고
- 애널리스트 5는 fast.

아직 이를 팩터로 지정할 필요는 없습니다.

`@hint`
`speed_vector`에 `"slow"`, `"medium"`, 또는 `"fast"`라는 문자열을 담은 벡터를 할당하세요.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create speed_vector
speed_vector <-
```

`@solution`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
```

`@sct`
```{r}
ex() %>% check_object("speed_vector") %>% check_equal(incorrect_msg = "`speed_vector`는 각 분석가의 속도 등급에 대한 5개의 항목이 있는 벡터여야 합니다. 대문자를 사용하지 마십시오. R은 대소문자를 구분합니다!")

success_msg("잘하셨습니다! 다음 연습으로 계속하십시오.")
```

---

## 순서형 팩터 (2)

```yaml
type: NormalExercise
key: 279077d10248ce03d5f972939ef8576430a16683
xp: 100
skills:
  - 1
```

`speed_vector`의 범주는 자연스러운 순서가 있으므로 순서형(ordinal) 팩터로 변환해야 해요. 기본적으로 `factor()` 함수는 `speed_vector`를 순서가 없는 팩터로 변환합니다. 순서형 팩터를 만들려면 `ordered`와 `levels`라는 두 개의 추가 인수를 제공해야 해요.

```
factor(some_vector,
       ordered = TRUE,
       levels = c("lev1", "lev2" ...))
```

`factor()` 함수에서 `ordered` 인수를 `TRUE`로 설정하면 해당 팩터가 순서형임을 나타냅니다. `levels` 인수에는 올바른 순서대로 팩터의 값을 지정하세요.

`@instructions`
`speed_vector`에서 순서형 팩터 벡터 `factor_speed_vector`를 만드세요. `ordered`는 `TRUE`로, `levels`는 `c("slow", "medium", "fast")`로 설정하세요.

`@hint`
자연스러운 순서가 있으므로 `factor()` 함수를 사용해 `speed_character_vector`를 기반으로 `factor_speed_vector`를 만드세요. `ordered` 인수는 `TRUE`로 설정하고, `levels = c("slow", "medium", "fast")`로 지정하세요.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")

# Convert speed_vector to ordered factor vector
factor_speed_vector <-

# Print factor_speed_vector
factor_speed_vector
summary(factor_speed_vector)
```

`@solution`
```{r}
# Create speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")

# Convert speed_vector to ordered factor vector
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Print factor_speed_vector
factor_speed_vector
summary(factor_speed_vector)
```

`@sct`
```{r}
msg = " `speed_vector` 변수를 지정하는 명령에 대해 아무것도 변경하지 마십시오."

ex() %>% check_object("speed_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_function("factor") %>% {
	check_arg(., 'x') %>% check_equal(incorrect_msg="`factor()`에 전달하는 첫 번째 인수는 `speed_vector`여야 합니다.")
	check_arg(., 'ordered') %>% check_equal(incorrect_msg="`factor()` 호출 내에서 `ordered = TRUE`로 설정했는지 확인하십시오.")
	check_arg(., 'levels') %>% check_equal(incorrect_msg="`factor()` 호출 내에서 `levels = c(\"slow\", \"medium\", \"fast\")`로 설정했는지 확인하십시오.")
	}

ex() %>% check_object("factor_speed_vector") %>% check_equal(eq_condition = "equal", incorrect_msg="`factor_speed_vector`에 여전히 문제가 있습니다. `factor()` 호출 내에서 `speed_vector`, `ordered = TRUE` 및 `levels = c(\"slow\", \"medium\", \"fast\")`만 전달했는지 확인하십시오.")

                      
                      
success_msg("훌륭합니다! 콘솔을 확인하십시오. 이제 `<` 기호와 함께 레벨에 순서가 연결되어 있음을 나타냅니다. 다음 연습으로 계속하십시오.")
```

---

## 순서 있는 팩터 비교하기

```yaml
type: NormalExercise
key: db16e69805625bcfde227743a8cbc985f8482a37
xp: 100
skills:
  - 1
```

업무가 잘 풀리지 않는 '데이터 분석가 2번'이 당신의 사무실에 와서 '데이터 분석가 5번' 때문에 프로젝트 전체가 느려진다고 불평합니다. 그가 약간 잘난 체로 유명하다는 걸 알기에, 먼저 그 말이 사실인지 확인해 보기로 합니다.

`factor_speed_vector`가 이제 순서가 있는 상태이므로 서로 다른 요소(여기서는 데이터 분석가)를 비교할 수 있어요. 잘 알려진 비교 연산자를 사용하면 간단히 확인할 수 있습니다.

`@instructions`
- `factor_speed_vector`에서 두 번째 데이터 분석가의 요인 값을 선택하려면 `[2]`를 사용하세요. 선택한 값을 `da2`에 저장하세요.
- 다섯 번째 데이터 분석가의 요인 값을 선택하려면 `[5]`를 사용해 `factor_speed_vector`에서 값을 가져오세요. 선택한 값을 `da5`에 저장하세요.
- `da2`가 `da5`보다 큰지 확인하고, 그 결과를 그대로 출력하세요. 한 요소가 다른 요소보다 큰지 확인하려면 `>` 연산자를 사용할 수 있습니다.

`@hint`
- 세 번째 데이터 분석가의 요인 값을 선택하려면 `factor_speed_vector[3]`를 사용하세요.
- 두 값을 비교하려면 `>`를 사용할 수 있어요. 예: `da3 > da4`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Create factor_speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Factor value for second data analyst
da2 <-

# Factor value for fifth data analyst
da5 <-

# Is data analyst 2 faster than data analyst 5?

```

`@solution`
```{r}
# Create factor_speed_vector
speed_vector <- c("medium", "slow", "slow", "medium", "fast")
factor_speed_vector <- factor(speed_vector, ordered = TRUE, levels = c("slow", "medium", "fast"))

# Factor value for second data analyst
da2 <- factor_speed_vector[2]

# Factor value for fifth data analyst
da5 <- factor_speed_vector[5]

# Is data analyst 2 faster data analyst 5?
da2 > da5
```

`@sct`
```{r}
msg = "speed_vector와 factor_speed_vector를 정의하는 명령어에 대해 아무것도 변경하지 마십시오!"
ex() %>% check_object("speed_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("factor_speed_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)


msg <- "두 번째 데이터 분석가에 대한 요인 값을 올바르게 선택하셨습니까? `factor_speed_vector[%s]`를 사용할 수 있습니다."
ex() %>% check_object("da2") %>% check_equal(eq_condition = "equal", incorrect_msg = sprintf(msg,"second", "2"))

ex() %>% check_object("da5") %>% check_equal(eq_condition = "equal",incorrect_msg = sprintf("fifth", "5"))

ex() %>% check_output_expr("da2 > da5", missing_msg = "`da2`와 `da5`를 올바르게 비교하셨습니까? `>`를 사용할 수 있습니다. 결과를 단순히 출력하십시오.")


success_msg("Bellissimo! 결과가 무엇을 말해줍니까? 데이터 분석가 두 명이 데이터 분석가 다섯 명에 대해 불평하고 있지만 실제로는 그들이 모든 것을 느리게 하고 있습니다! 벡터, 행렬 및 요인에 대한 확고한 기초를 바탕으로 R에서 매우 중요한 데이터 구조인 데이터 프레임의 멋진 세계로 뛰어들 준비가 되었습니다!")
```
