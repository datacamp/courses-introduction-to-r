---
title_meta: "Kapitel\_6"
title: Listen
description: "Im Gegensatz zu Vektoren können Listen Komponenten verschiedenen Typs enthalten\_– etwa so, wie deine To-Do-Listen verschiedene Kategorien von Aufgaben enthalten können. In diesem Kapitel lernst du, wie du diese Listen erstellst, benennst und ihre Elemente auswählst."
---

## Vorteile von Listen

```yaml
type: NormalExercise
key: 2afcdb6a76ec91bf266de9b2ac295d844d7bb004
xp: 100
skills:
  - 1
```

Glückwunsch! An diesem Punkt des Kurses bist du bereits vertraut mit:

- **Vektoren** (eindimensionale Arrays): können Zahlen, Zeichenketten oder logische Werte enthalten. Die Elemente in einem Vektor haben alle denselben Datentyp.
- **Matrizen** (zweidimensionale Arrays): können Zahlen, Zeichenketten oder logische Werte enthalten. Die Elemente in einer Matrix haben alle denselben Datentyp.
- **Dataframes** (zweidimensionale Objekte): können Zahlen, Zeichenketten oder logische Werte enthalten. Innerhalb einer Spalte haben alle Elemente denselben Datentyp, aber verschiedene Spalten können unterschiedliche Datentypen haben.

Für einen R-Neuling weißt du doch schon recht viel, oder?!

`@instructions`
Sende den Code mit „Antworten“ ab, um gleich mehr über Listen zu erfahren!

`@hint`
Klicke einfach auf die „Antworten“-Schaltfläche.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Just submit the answer
```

`@solution`
```{r}
# Just submit the answer.
```

`@sct`
```{r}
success_msg("Fertig, los geht's! Weiter zur nächsten Übung.")
```

---

## Vorteile von Listen (2)

```yaml
type: NormalExercise
key: 68f93c5c504616bd18876da52cd123277d56fc8b
xp: 100
skills:
  - 1
```

Eine **Liste** in R ähnelt deiner To-Do-Liste bei der Arbeit oder in der Schule: Die verschiedenen Punkte auf der Liste unterscheiden sich höchstwahrscheinlich durch ihre Länge, ihre Eigenschaften und die Art der zu erledigenden Aufgabe. 

Eine Liste in R ermöglicht es, eine Vielzahl von Objekten unter einem Namen (dem Namen der Liste) geordnet zu erfassen. Diese Objekte können zum Beispiel Matrizen, Vektoren, Dataframes oder sogar weitere Listen sein. Es ist nicht einmal erforderlich, dass diese Objekte in irgendeiner Weise miteinander verbunden sind.

Man könnte sagen, dass eine Liste eine Art Superdatentyp ist: Du kannst praktisch jede Information in ihr speichern!

`@instructions`
Sende den Code mit „Antworten“ ab, um mit der ersten Übung zu Listen zu beginnen!

`@hint`
Klicke auf die „Antworten“-Schaltfläche, um zur ersten Übung zum Thema Listen zu gelangen.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Just submit the answer to start the first exercise on lists.
```

`@solution`
```{r}
# Just submit the answer to start the first exercise on lists.
```

`@sct`
```{r}
success_msg("Cool. Lass uns loslegen!")
```

---

## Listen erstellen

```yaml
type: NormalExercise
key: 4beee9cb532c889903218b49b83ab5ef133eac83
xp: 100
skills:
  - 1
```

Nun ist es Zeit, deine erste Liste zu erstellen! Zum Erstellen einer Liste verwendest du die Funktion `list()`: 

```
my_list <- list(comp1, comp2 ...)
```

Als Argumente für die Listenfunktion übergibst du die gewünschten Komponenten der Liste. Zur Erinnerung: Dies können zum Beispiel Matrizen, Vektoren oder weitere Listen sein.

`@instructions`
Erstelle eine Liste namens `my_list`, die als Komponenten die Variablen `my_vector`, `my_matrix` und `my_df` enthält.

`@hint`
Verwende die Funktion `list()` mit den Argumenten `my_vector`, `my_matrix` und `my_df`, jeweils getrennt durch ein Komma.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Vector with numerics from 1 up to 10
my_vector <- 1:10 

# Matrix with numerics from 1 up to 9
my_matrix <- matrix(1:9, ncol = 3)

# First 10 elements of the built-in data frame mtcars
my_df <- mtcars[1:10,]

# Construct list with these different elements:
my_list <-
```

`@solution`
```{r}
# Vector with numerics from 1 up to 10
my_vector <- 1:10 

# Matrix with numerics from 1 up to 9
my_matrix <- matrix(1:9, ncol = 3)

# First 10 elements of the built-in data frame mtcars
my_df <- mtcars[1:10,]

# Construct list with these different elements:
my_list <- list(my_vector, my_matrix, my_df)
```

`@sct`
```{r}
msg = "Ändere nicht die Definition der Variablen `my_vector`, `my_matrix` oder `my_df`!"
ex() %>% check_object("my_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("my_matrix", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("my_df", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("my_list") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `my_list` nicht die richtigen Elemente enthält. Stelle sicher, dass du die Variablen `my_vector`, `my_matrix` und `my_df` an die `list()`-Funktion übergibst, getrennt durch Kommas, in dieser Reihenfolge.")

success_msg("Wunderbar! Gehe zur nächsten Übung über.")
```

---

## Benannte Liste erstellen

```yaml
type: NormalExercise
key: 89dd0126568b1ff5a84033c571907a8a282245e4
xp: 100
skills:
  - 1
```

Gut gemacht! Du bist auf dem richtigen Weg.

Genau wie bei deiner To-Do-Liste willst du vermeiden, dass du nicht weißt oder dich nicht mehr erinnerst, wofür die Komponenten deiner Liste stehen. Deshalb solltest du ihnen Namen geben: 

```
my_list <- list(name1 = your_comp1, 

                name2 = your_comp2)

``` 

So entsteht eine Liste mit Komponenten namens `name1`, `name2` und so weiter. Wenn du Listen benennen willst, nachdem du sie schon erstellt hast, kannst du wie bei den Vektoren die Funktion `names()` verwenden. Die folgenden Befehle sind völlig gleichwertig mit der obigen Zuweisung:

```
my_list <- list(your_comp1, your_comp2)
names(my_list) <- c("name1", "name2")
```

`@instructions`
- Ändere den Code der vorigen Übung (siehe Editor), indem du den Komponenten Namen gibst. Verwende für `my_vector` den Namen `vec`, für `my_matrix` den Namen `mat` und für `my_df` den Namen `df`. 
- Gib `my_list` aus, damit du das Ergebnis überprüfen kannst.

`@hint`
Die erste Option, deinen Listenkomponenten Namen zuzuweisen, ist die einfachste. Sie beginnt so: 
```
my_list <- list(vec = my_vector)
```
Füge die anderen beiden Komponenten auf ähnliche Weise hinzu.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Vector with numerics from 1 up to 10
my_vector <- 1:10 

# Matrix with numerics from 1 up to 9
my_matrix <- matrix(1:9, ncol = 3)

# First 10 elements of the built-in data frame mtcars
my_df <- mtcars[1:10,]

# Adapt list() call to give the components names
my_list <- list(my_vector, my_matrix, my_df)

# Print out my_list
```

`@solution`
```{r}
# Vector with numerics from 1 up to 10
my_vector <- 1:10 

# Matrix with numerics from 1 up to 9
my_matrix <- matrix(1:9, ncol = 3)

# First 10 elements of the built-in data frame mtcars
my_df <- mtcars[1:10,]

# Adapt list() call to give the components names
my_list <- list(vec = my_vector, mat = my_matrix, df = my_df)

# Print out my_list
my_list
```

`@sct`
```{r}
msg = "Ändere nicht die Definition der Variablen `my_vector`, `my_matrix` oder `my_df`!"
ex() %>% check_object("my_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("my_matrix", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("my_df", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("my_list") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `my_list` nicht die richtigen Elemente enthält.")

ex() %>% check_object("my_list") %>% check_equal(eq_condition = "equal",incorrect_msg = "Es sieht so aus, als ob `my_list` nicht die richtige Benennung für die Komponenten enthält. Stelle sicher, dass du die Namen `vec`, `mat` und `df` verwendest. Schau dir den Hinweis an, wenn du Schwierigkeiten hast.")

ex() %>% check_output_expr("my_list", missing_msg = "Vergiss nicht, `my_list` in der Konsole auszugeben! Füge einfach `my_list` in einer neuen Zeile im Editor hinzu.")

success_msg("Super! Du weißt jetzt nicht nur, wie man Listen erstellt, sondern kannst sie auch benennen; eine Fähigkeit, die sich in der Praxis als sehr nützlich erweisen wird. Weiter zur nächsten Übung.")
```

---

## Benannte Liste erstellen (2)

```yaml
type: NormalExercise
key: 19fd17cc792ef870c1558b3a9bae08c1d1e3acae
xp: 100
skills:
  - 1
```

Da du ein großer Filmfan bist (und bei Lucasfilm arbeitest), willst du Daten zu guten Filmen mithilfe von Listen speichern. 

Als Erstes erstellst du daher eine Liste für den Film „The Shining“. Die Variablen `mov`, `act` und `rev` wurden bereits in deinem R-Workspace vorbereitet. Du kannst sie dir gerne in der Konsole anschauen.

`@instructions`
Vervollständige den Code im Editor, um `shining_list` zu erstellen. Diese Liste soll drei Komponenten enthalten:

- `moviename`: eine Zeichenkette mit dem Filmtitel (gespeichert in `mov`)
- `actors`: ein Vektor mit den Namen der Hauptdarsteller (gespeichert in `act`)
- `reviews`: ein Dataframe mit einigen Rezensionen (gespeichert in `rev`)

Vergiss nicht, die Listenkomponenten entsprechend zu benennen (die Namen sind `moviename`, `actors` und `reviews`)!

`@hint`
`shining_list <- list(moviename = mov)` ist nur ein Teil der Lösung. Du musst nun auch `act` und `rev` mit den entsprechenden Namen in die Liste aufnehmen.

`@pre_exercise_code`
```{r}
mov <- "The Shining"
act      <- c("Jack Nicholson","Shelley Duvall","Danny Lloyd","Scatman Crothers","Barry Nelson")
sources     <- c("IMDb1","IMDb2","IMDb3")
comments    <- c("Best Horror Film I Have Ever Seen","A truly brilliant and scary film from Stanley Kubrick","A masterpiece of psychological horror")
scores      <- c(4.5,4,5)
rev     <- data.frame(scores, sources, comments)
rm(scores, sources, comments)
```

`@sample_code`
```{r}
# The variables mov, act and rev are available

# Finish the code to build shining_list
shining_list <- list(moviename = mov)
```

`@solution`
```{r}
# The variables mov, act and rev are available

# Finish the code to build shining_list
shining_list <- list(moviename = mov, actors = act, reviews = rev)
```

`@sct`
```{r}
msg = "Entferne oder ändere nicht die Definition der voreingestellten Variablen!"
ex()  %>% check_object("mov", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex()  %>% check_object("rev", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex()  %>% check_object("act", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("shining_list") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `shining_list` nicht die richtigen Elemente enthält: Das erste Element sollte `mov` sein, das zweite Element `act` und das dritte `rev`.")

ex() %>% check_object("shining_list") %>% check_equal(eq_condition = "equal",incorrect_msg = "Es sieht so aus, als ob `shining_list` nicht die richtige Benennung für die Komponenten hat: Benenne das erste Element `moviename`, das zweite Element `actors` und das dritte Element `reviews`.")

success_msg("Wunderbar! Du weißt jetzt, wie man Listen erstellt und benennt. Wie in den vorherigen Kapiteln, lass uns anschauen, wie man Elemente für Listen auswählt. Gehe zur nächsten Übung.")
```

---

## Elemente einer Liste auswählen

```yaml
type: NormalExercise
key: 1ef3278944562caef64b9927dd2ddb6ee52334cd
xp: 100
skills:
  - 1
```

Listen setzen sich oft aus zahlreichen Elementen und Komponenten zusammen. Deshalb ist es nicht immer einfach, ein einzelnes Element, mehrere Elemente oder eine ganze Komponente auszuwählen. 

Eine Möglichkeit zur Auswahl einer Komponente ist die Angabe der nummerierten Position dieser Komponente. Um dir zum Beispiel die erste Komponente von `shining_list` zu schnappen, nutzt du folgenden Code: 

```
shining_list[[1]]
```

Du kannst das schnell überprüfen, indem du den Code in der Konsole eingibst. Wichtig: Um Elemente aus Vektoren auszuwählen, verwendest du einzelne eckige Klammern: `[ ]`. Verwechsle sie nicht!

Du kannst auch auf die Namen der Komponenten verweisen, und zwar entweder mit `[[ ]]` oder mit dem Zeichen `$`. Beide der folgenden Optionen wählen den Dataframe aus, der die Filmrezensionen („Reviews“) enthält: 

```
shining_list[["reviews"]]
shining_list$reviews
``` 

Oft möchtest du nicht nur eine Komponente, sondern ganz bestimmte Elemente dieser Komponente auswählen. Mit `shining_list[[2]][1]` wählst du zum Beispiel aus der zweiten Komponente `actors` (`shining_list[[2]]`) das erste Element (`[1]`). Wenn du das in der Konsole eingibst, erhältst du als Ausgabe den Namen Jack Nicholson.

`@instructions`
- Wähle aus `shining_list` den Vektor aus, der die Schauspieler repräsentiert. Gib diesen Vektor einfach aus.
- Wähle aus `shining_list` das zweite Element des Vektors aus, der die Schauspieler repräsentiert. Gib den Wert wieder einfach nur aus.

`@hint`
- Um den Vektor auszuwählen, der die Schauspieler repräsentiert, kannst du `$actors` verwenden.
- Um das dritte Element des Vektors auszuwählen, der die Schauspieler repräsentiert, verwendest du `shining_list$actors[3]`. Was musst du am Code ändern, um das zweite Element auszuwählen?

`@pre_exercise_code`
```{r}
load(url("https://assets.datacamp.com/course/intro_to_r/shining_list.RData"))
```

`@sample_code`
```{r}
# shining_list is already pre-loaded in the workspace

# Print out the vector representing the actors


# Print the second element of the vector representing the actors
```

`@solution`
```{r}
# shining_list is already pre-loaded in the workspace

# Print out the vector representing the actors
shining_list$actors

# Print the second element of the vector representing the actors
shining_list$actors[2]
```

`@sct`
```{r}
msg <- "Entferne oder ändere nicht die Definition von `shining_list`, die im Arbeitsbereich vorab geladen ist!"
ex() %>% check_object("shining_list", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_output_expr("shining_list$actors", missing_msg = "Hast du den Vektor, der die Schauspieler darstellt, korrekt ausgewählt und ausgegeben? Du kannst zum Beispiel `shining_list$actors` verwenden.")

ex() %>% check_output_expr("shining_list$actors[2]", missing_msg = "Um den zweiten Schauspieler aus dem Vektor, der die Schauspieler darstellt, auszuwählen, solltest du deine Auswahl verketten: `shining_list$actors` repräsentiert die Schauspieler, also kannst du ein `[2]` hinzufügen, um das zweite Element auszuwählen.")

success_msg("Super! Elemente aus Listen auszuwählen ist ziemlich einfach, oder? Weiter zur nächsten Übung.")
```

---

## Neue Liste für einen Film erstellen

```yaml
type: NormalExercise
key: ce10c83e5b
xp: 100
```

Du hast folgende englische Rezensionen zu einem anderen, neueren Film mit Jack Nicholson gefunden: The Departed.

| Bewertung | Kommentare               |
|--------|------------------------|
| 4.6    | <span translate="no">I would watch it again</span> |
| 5      | <span translate="no">Amazing!</span>               |
| 4.8    | <span translate="no">I liked it</span>             |
| 5      | <span translate="no">One of the best movies</span> |
| 4.2    | <span translate="no">Fascinating plot</span>       |

Es wäre sinnvoll, alle Daten zum Film, wie den Titel, die Besetzung und die Rezensionen, in einer einzigen Variablen zusammenzufassen. Da diese Daten unterschiedliche Formate haben, ist es naheliegend, sie in einer Listenvariablen zu kombinieren.

`movie_title` mit dem Titel des Films und `movie_actors` mit den Namen einiger Schauspieler im Film sind in deinem Workspace verfügbar.

`@instructions`
- Erstelle zwei Vektoren namens `scores` und `comments` für die Daten aus der Tabelle mit den Rezensionen.
- Berechne den Durchschnitt des Vektors `scores` und speichere ihn als `avg_review`. 
- Kombiniere die Vektoren `scores` und `comments` zu einem Dataframe namens `reviews_df`. 
- Erstelle eine Liste namens `departed_list` mit den folgenden Komponenten: `movie_title`, `movie_actors`, Dataframe `reviews_df` mit Rezensionen sowie die durchschnittliche Bewertung `avg_review`. Gib diese Liste aus.

`@hint`
- Zum Erstellen der Vektoren benötigst du die Funktion `c()`. 
- Um den Durchschnitt eines Vektors zu ermitteln, übergibst du den Namen des Vektors an die Funktion `mean()`. 
- Verwende die Funktion `data.frame()`, um den Dataframe `reviews_df` zu erstellen.

`@pre_exercise_code`
```{r}
movie_title <- "The Departed"
movie_actors <- c("Leonardo DiCaprio", "Matt Damon", "Jack Nicholson","Mark Wahlberg","Vera Farmiga", "Martin Sheen")
```

`@sample_code`
```{r}
# Use the table from the exercise to define the comments and scores vectors
scores <- c(4.6, 5, 4.8, 5, ___
comments <- c("I would watch it again", "Amazing!", "I liked it", "One of the best movies", ___ 

# Save the average of the scores vector as avg_review


# Combine scores and comments into the reviews_df data frame


# Create and print out a list, called departed_list


```

`@solution`
```{r}
# Use the table from the exercise to define the comments and scores vectors
scores <- c(4.6, 5, 4.8, 5, 4.2)
comments <- c("I would watch it again", "Amazing!", "I liked it", "One of the best movies", "Fascinating plot")

# Save the average of the scores vector as avg_review
avg_review <- mean(scores)

# Combine scores and comments into the reviews_df data frame
reviews_df <- data.frame(scores, comments)

# Create and print out a list, called departed_list
departed_list <- list(movie_title, movie_actors, reviews_df, avg_review)
departed_list
```

`@sct`
```{r}
msg <- "Ändere oder entferne die vorgegebenen Objekte nicht."

ex() %>% check_object("movie_title") %>% check_equal(eq_condition = "equal", incorrect_msg = msg)
ex() %>% check_object("movie_actors")  %>% check_equal(eq_condition = "equal", incorrect_msg = msg)
ex() %>% check_object("scores") %>% check_equal(eq_condition = "equal", incorrect_msg = "Hast du den `scores`-Vektor korrekt erstellt?")
ex() %>% check_object("comments") %>% check_equal(eq_condition = "equal", incorrect_msg = "Hast du den `comments`-Vektor korrekt erstellt?")
ex() %>% check_correct(
  check_object(.,"avg_review") %>% check_equal(eq_condition = "equal", incorrect_msg = "Hast du das `avg_review`-Objekt korrekt erstellt?"),
  check_function(.,"mean") %>% check_arg("x") %>% check_equal()
)
ex() %>% check_correct(
  check_object(.,"reviews_df") %>% check_equal(eq_condition = "equal", incorrect_msg = "Hast du `reviews_df` korrekt erstellt?"),
  check_function(.,"data.frame") %>% {
    check_arg(.,"..1") %>% check_equal()
    check_arg(.,"..2") %>% check_equal()
  }
)
ex() %>% check_correct(
  check_object(.,"departed_list") %>% check_equal(eq_condition = "equal", incorrect_msg = "Hast du `departed_list` korrekt erstellt?"),
  check_function(.,"list") %>% {
    check_arg(.,"..1") %>% check_equal()
    check_arg(.,"..2") %>% check_equal()
    check_arg(.,"..3") %>% check_equal()
    check_arg(.,"..4") %>% check_equal()
  }
)
ex() %>% check_output_expr("departed_list", missing_msg = "Hast du die `departed_list` ausgegeben?")

success_msg("Gute Arbeit! Du hast erfolgreich eine weitere Liste mit Filminformationen erstellt und verschiedene Komponenten zu einer einzigen Liste kombiniert. Glückwunsch zum Abschluss des Kurses!")
```
