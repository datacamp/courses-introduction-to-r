---
title_meta: "Kapitel\_2"
title: Vektoren
description: >-
  Wir nehmen dich mit auf eine Reise nach Las Vegas und du lernst, wie du deine
  Leistung beim Glücksspiel mithilfe von Vektoren in R analysieren kannst. Nach
  Abschluss dieses Kapitels bist du in der Lage, Vektoren in R zu erstellen, sie
  zu benennen, darin enthaltene Elemente auszuwählen und verschiedene Vektoren
  zu vergleichen.
---

## Vektoren erstellen

```yaml
type: NormalExercise
key: d9b453dbdd
xp: 100
skills:
  - 1
```

Hältst du dich für ein Glückskind? Hoffentlich kannst du diese Frage bejahen, denn dieses Kapitel nimmt dich mit auf eine Reise in die „Stadt der Sünde“, auch bekannt als *Paradies für Statistikfans*!

Du lernst nun, wie du mit R und deinen neuen Datenanalysefähigkeiten deinen Erfolg am Spieltisch steigern und eine Karriere als Glücksspielprofi starten kannst. In diesem Kapitel erfährst du, wie du ganz einfach den Verlauf deiner Wetten erfasst und simple Analysen früherer Aktionen durchführst. Nächster Halt: Vegas Baby... VEGAS!!

`@instructions`
- Kannst du dich noch an das erinnern, was du im ersten Kapitel gelernt hast? Weise der Variablen `vegas` den Wert `"Go!"` zu. Zur Erinnerung: Bei R wird zwischen Groß- und Kleinschreibung unterschieden.

`@hint`
Gib einfach die folgende Zeile im Editor ein:
```
vegas <- "Go!"
```

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Define the variable vegas
vegas <- 
```

`@solution`
```{r}
# Define the variable vegas
vegas <- "Go!"
```

`@sct`
```{r}
ex() %>% check_object("vegas") %>% check_equal(incorrect_msg = "Stelle sicher, dass du den richtigen Wert `vegas` zuweist. Vergiss nicht, dass R zwischen Groß- und Kleinschreibung unterscheidet!")

success_msg("Super! Mach weiter mit der nächsten Übung.")
```

---

## Vektoren erstellen (2)

```yaml
type: NormalExercise
key: fd427db76f
xp: 100
skills:
  - 1
```

Jetzt ist erst mal etwas Konzentration gefragt! 

Auf deinem Weg vom Tellerwäscher zum Millionär wirst du ausgiebig Gebrauch von Vektoren machen. Vektoren sind eindimensionale Arrays, die numerische Daten, Zeichenketten oder logische Daten enthalten können. Mit anderen Worten: Ein Vektor ist eine einfache Struktur, um Daten zu speichern. Du kannst zum Beispiel deine täglichen Gewinne und Verluste in den Casinos speichern. 

In R erstellst du einen Vektor mit der Funktion `c()`. (Das c steht für „combine“ – es werden sozusagen Werte in einem Vektor kombiniert.) Du gibst die Vektorelemente durch Komma getrennt in den Klammern an. Zum Beispiel:

```
numeric_vector <- c(1, 2, 3)
character_vector <- c("a", "b", "c")
```

Sobald du diese Vektoren in R erstellt hast, kannst du sie für Berechnungen verwenden.

`@instructions`
Vervollständige den Code so, dass `boolean_vector` folgende drei Elemente enthält: `TRUE`, `FALSE` und `TRUE` (in dieser Reihenfolge).

`@hint`
Weise `c(TRUE, FALSE, TRUE)` der Variablen `boolean_vector` zu. Nutze dafür den Operator `<-`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Complete the code for boolean_vector
boolean_vector <-
```

`@solution`
```{r}
numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Complete the code for boolean_vector
boolean_vector <- c(TRUE, FALSE, TRUE)
```

`@sct`
```{r}
msg <- "Ändere nicht den Code, der `numeric_vector` und `character_vector` definiert hat!"
ex() %>% check_object("numeric_vector", undefined_msg = msg) %>% check_equal(, incorrect_msg = msg)

ex() %>% check_object("character_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("boolean_vector") %>% check_equal(incorrect_msg = "Stelle sicher, dass du die richtigen Werte `boolean_vector` zuweist. Verwende `c(TRUE, FALSE, TRUE)`. Setze keine Anführungszeichen um `TRUE` und `FALSE`! Achte auch darauf, die gleiche Reihenfolge wie in den Anweisungen angegeben zu übernehmen.")

success_msg("Perfekt! Beachte, dass das Hinzufügen eines Leerzeichens hinter den Kommas in der `c()`-Funktion die Lesbarkeit deines Codes verbessert. Lass uns im nächsten Übungsbeispiel weiter mit der Erstellung von Vektoren üben.")
```

---

## Vektoren erstellen (3)

```yaml
type: NormalExercise
key: 9f41229dbc
xp: 100
skills:
  - 1
```

Nach einer Woche in Las Vegas hast du immer noch keinen Ferrari in deiner Garage und beschließt daher, dass es an der Zeit ist, deine analytischen Superkräfte einzusetzen.

Vor deiner ersten Analyse stellst du zunächst alle Gewinne und Verluste der letzten Woche zusammen: 

Für `poker_vector`: 

- Gewinn am Montag: $140
- Verlust am Dienstag: $50
- Gewinn am Mittwoch: $20 
- Verlust am Donnerstag: $120
- Gewinn am Freitag: $240

Für `roulette_vector`: 

- Verlust am Montag: $24
- Verlust am Dienstag: $50
- Gewinn am Mittwoch: $100
- Verlust am Donnerstag: $350
- Gewinn am Freitag: $10

Du hast nur Poker und Roulette gespielt, weil die Craps-Tische leider durch eine Schar von Hellsehern besetzt waren. Um diese Daten in R nutzen zu können, erstellst du die Variablen `poker_vector` und `roulette_vector`.

`@instructions`
Weise die Gewinne/Verluste im Roulette der Variablen `roulette_vector` zu. Du hast zuerst 24 Dollar und dann 50 Dollar verloren, dann 100 Dollar gewonnen, wieder 350 Dollar verloren und nochmal 10 Dollar gewonnen.

`@hint`
Um dir bei diesem Schritt zu helfen, enthält der Editor bereits den Code zum Erstellen von `poker_vector`. Weise `roulette_vector` die richtigen Werte zu, basierend auf den Angaben in der Übungsaufgabe. Vergiss nicht, dass Verluste negative Zahlen sind!

`@pre_exercise_code`
```{r}

```

`@sample_code`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <-  
```

`@solution`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)
```

`@sct`
```{r}
ex() %>% check_object("poker_vector") %>% check_equal(incorrect_msg = "Stelle sicher, dass du die richtigen Werte `poker_vector` zuweist.")

ex() %>% check_object("roulette_vector") %>% check_equal(incorrect_msg = "Stelle sicher, dass du die richtigen Werte `roulette_vector` zuweist. Achte darauf, die richtige Reihenfolge zu übernehmen!")

success_msg("Sehr gut! Um den Inhalt deiner Vektoren zu überprüfen, denke daran, dass du immer einfach die Variable in die Konsole eingeben und Enter drücken kannst. Gehe zur nächsten Übung weiter!")
```

---

## Vektoren benennen

```yaml
type: NormalExercise
key: 3b0b80b192
xp: 100
skills:
  - 1
```

Datenanalyseprofis brauchen einen klaren Blick auf die von ihnen genutzten Daten. Deshalb ist es wichtig zu verstehen, worauf sich jedes Element bezieht. 

In der vorigen Übung hast du einen Vektor mit deinen Gewinnen und Verlusten im Verlauf der Woche erstellt. Jedes Vektorelement bezieht sich auf einen Wochentag, aber es ist schwer zu erkennen, welches Element zu welchem Tag gehört. Es wäre schön, wenn man das im Vektor selbst sehen könnte. 

Mit der Funktion `names()` kannst du den Elementen eines Vektors Namen geben. Sieh dir dieses Beispiel an:

```
some_vector <- c("John Doe", "poker player")
names(some_vector) <- c("Name", "Profession")
```

Dieser Code erstellt zunächst den Vektor `some_vector` und gibt dann den beiden Elementen einen Namen. Das erste Element erhält den Namen `Name`, während das zweite Element den Namen `Profession` bekommt. Wenn du die Inhalte in der Konsole ausgibst, erhältst du folgende Ausgabe:

```

          Name     Profession 

    "John Doe" "poker player" 

```

`@instructions`
- Der Code im Editor benennt die Elemente in `poker_vector` mit den Wochentagen. Füge Code hinzu, um dasselbe für `roulette_vector` zu tun.

`@hint`
Du kannst `names(roulette_vector)` verwenden, um die Namen für die Variable `roulette_vector` festzulegen. Achte darauf, dass du den richtigen Vektor angibst, um die Wochentage als Namen zuzuweisen! Denk daran, dass bei R zwischen Groß- und Kleinschreibung unterschieden wird!

`@pre_exercise_code`
```{r}

```

`@sample_code`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# Assign days as names of poker_vector
names(poker_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Assign days as names of roulette_vector

```

`@solution`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# Assign days as names of poker_vector
names(poker_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Assign days as names of roulette_vector
names(roulette_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
```

`@sct`
```{r}
ex() %>% check_object("poker_vector") %>% check_equal(incorrect_msg = "Ändere die Werte in `poker_vector` nicht; sie wurden bereits für dich kodiert.")

ex() %>% check_object("roulette_vector") %>% check_equal(incorrect_msg = "Ändere die Werte in `roulette_vector` nicht; sie wurden bereits für dich kodiert.")

ex() %>% check_object("poker_vector") %>% check_equal(eq_condition = 'equal', incorrect_msg = "Ändere den Code, der die Elemente in `poker_vector` benennt, nicht; konzentriere dich auf `roulette_vector`!")

ex() %>% check_object("roulette_vector") %>% check_equal(eq_condition = 'equal',incorrect_msg = "Stelle sicher, dass du den richtigen Namensvektor `roulette_vector` zuweist. Verwende genau denselben Vektor, der verwendet wurde, um `poker_vector` zu benennen.")

success_msg("Gut gemacht! Weiter zur nächsten Übung.")
```

---

## Vektoren benennen (2)

```yaml
type: NormalExercise
key: 6858c65a4a
xp: 100
skills:
  - 1
```

Wenn du ein richtiger Statistikprofi werden willst, musst du dir eine effiziente Arbeitsweise angewöhnen.

In den vorigen Übungen ist dir bestimmt aufgefallen, wie langweilig und frustrierend es ist, Daten wie die Wochentagsnamen immer wieder abzutippen. Mit etwas Abstand betrachtet, wird aber eine viel effizientere Lösung ersichtlich: Der Vektor mit den Wochentagen kann einer **Variablen** zugewiesen werden! 

Genau wie für die Gewinne und Verluste beim Poker und Roulette kannst du auch für die Wochentage eine Variable erstellen. So kannst du sie immer wieder verwenden.

`@instructions`
- Eine Variable `days_vector` mit den Wochentagen wurde bereits für dich erstellt.
- Verwende `days_vector`, um die Namen für `poker_vector` und `roulette_vector` festzulegen.

`@hint`
Du kannst `names(poker_vector) <- days_vector` verwenden, um die Namen der Elemente von `poker_vector` festzulegen. Schreibe analog dazu den Code für `roulette_vector`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# The variable days_vector
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
 
# Assign the names of the day to roulette_vector and poker_vector
names(poker_vector) <-   
names(roulette_vector) <-
```

`@solution`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# The variable days_vector
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Assign the names of the day to roulette_vector and poker_vector
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector
```

`@sct`
```{r}
msg <- "Ändere nicht die vordefinierten Variablen `poker_vector`, `roulette_vector` oder `days_vector`."

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("days_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector") %>% check_equal(incorrect_msg = "Stelle sicher, dass du `days_vector` den Namen von `poker_vector` zuweist.", eq_condition = "equal",)

ex() %>% check_object("roulette_vector") %>% check_equal(eq_condition = "equal", incorrect_msg = "Stelle sicher, dass du `days_vector` den Namen von `roulette_vector` zuweist.")


success_msg("Gut gemacht! Ein Tipp: Versuche, Code-Duplikate jederzeit zu vermeiden. Gehe zur nächsten Übung und lerne, wie man mit Vektoren rechnet!")
```

---

## Gesamtgewinn berechnen

```yaml
type: NormalExercise
key: da995f099f
xp: 100
skills:
  - 1
```

Da du nun die Poker- und Roulettegewinne als hübsch benannte Vektoren vorliegen hast, kannst du mit der Datenanalyse beginnen. 

Du möchtest folgende Dinge herausfinden:

- Wie hoch war dein Gesamtgewinn bzw. Gesamtverlust an jedem Tag der Woche?
- Hast du in dieser Woche insgesamt Geld verloren?
- Gewinnst du oder verlierst du Geld beim Pokern und beim Roulette?

Um Antworten zu erhalten, musst du arithmetische Berechnungen mit Vektoren durchführen. 

Beachte dabei, dass die Summe zweier Vektoren in R elementweise berechnet wird. Die folgenden drei Formulierungen sind zum Beispiel völlig gleichwertig:

```
c(1, 2, 3) + c(4, 5, 6)
c(1 + 4, 2 + 5, 3 + 6)
c(5, 7, 9)
```

Du kannst die Berechnungen auch mit Variablen durchführen, die Vektoren darstellen:

```
a <- c(1, 2, 3) 
b <- c(4, 5, 6)
c <- a + b
```

`@instructions`
- Erstelle die Summe der Variablen `A_vector` und `B_vector` und weise sie `total_vector` zu.
- Überprüfe das Ergebnis, indem du `total_vector` ausgeben lässt.

`@hint`
Benutze den `+`-Operator, um `A_vector` und `B_vector` zu addieren. Verwende `<-`, um das Ergebnis `total_vector` zuzuweisen.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
A_vector <- c(1, 2, 3)
B_vector <- c(4, 5, 6)

# Take the sum of A_vector and B_vector
total_vector <- 
  
# Print out total_vector

```

`@solution`
```{r}
A_vector <- c(1, 2, 3)
B_vector <- c(4, 5, 6)

# Take the sum of A_vector and B_vector
total_vector <- A_vector + B_vector

# Print out total_vector
total_vector
```

`@sct`
```{r}
msg <- "Ändere nicht den Inhalt von `A_vector` oder `B_vector`!"
ex() %>% check_object("A_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("B_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("total_vector") %>% check_equal(incorrect_msg = "Stelle sicher, dass `total_vector` die Summe von `A_vector` und `B_vector` enthält.")

ex() %>% check_output_expr("total_vector", missing_msg = "Vergiss nicht, `total_vector` auszugeben! Schreibe einfach `total_vector` in eine neue Zeile.")

success_msg("Gut gemacht! Weiter zur nächsten Übung.")
```

---

## Gesamtgewinn berechnen (2)

```yaml
type: NormalExercise
key: 2969d8ed65
xp: 100
skills:
  - 1
```

Du verstehst inzwischen, wie R mit Vektoren rechnet. Jetzt ist es an der Zeit, ein paar Ferraris in deine Garage zu holen! Zuerst musst du herausfinden, wie hoch der Gesamtgewinn oder Gesamtverlust pro Wochentag war. Der tägliche Gesamtgewinn ist die Summe aus dem Gewinn/Verlust, den du am jeweiligen Tag beim Poker und beim Roulette erzielt hast. 

In R ist dies einfach die Summe aus `roulette_vector` und `poker_vector`.

`@instructions`
Weise der Variablen `total_daily` deinen Gesamtgewinn bzw. Gesamtverlust an jedem Wochentag zu (kombiniert für Poker und Roulette).

`@hint`
Ähnlich wie in der vorigen Übung musst du die Summe der beiden Vektoren der neuen Variablen `total_daily` zuweisen.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Assign to total_daily how much you won/lost on each day
total_daily <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Assign to total_daily how much you won/lost on each day
total_daily <- poker_vector + roulette_vector
```

`@sct`
```{r}
msg = "Ändere nichts an der Definition und Benennung von `poker_vector` und `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal( incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("total_daily") %>% check_equal(incorrect_msg = "Stelle sicher, dass du die Summe von `poker_vector` und `roulette_vector` `total_daily` zuweist.")

success_msg("Super! Weiter zur nächsten Übung.")
```

---

## Gesamtgewinn berechnen (3)

```yaml
type: NormalExercise
key: e66a56b9f0
xp: 100
skills:
  - 1
```

Nach der soeben erstellten Analyse sieht es so aus, als hättest du mal gute und mal schlechte Tage gehabt. Das hatte sich dein Ego anders vorgestellt. Daher fragst du dich nun, ob du womöglich im Verlauf der gesamten Woche sogar Geld verloren hast. 

Eine Funktion, mit der du diese Frage beantworten kannst, ist `sum()`. Sie berechnet die Summe aller Elemente eines Vektors. Um zum Beispiel den Gesamtbetrag zu berechnen, den du beim Pokern verloren/gewonnen hast, nutze folgenden Code: 

```
total_poker <- sum(poker_vector)
```

`@instructions`
- Berechne, wie viel Geld du insgesamt beim Roulette gewonnen/verloren hast, und weise den Wert der Variablen `total_roulette` zu.
- Jetzt hast du die Gesamtwerte für Roulette und Poker und kannst somit ganz einfach `total_week` berechnen (die Summe aller Gewinne und Verluste für die gesamte Woche).
- Gib `total_week` aus.

`@hint`
Benutze die Funktion `sum()`, um den Gesamtwert von `roulette_vector` zu erhalten. `total_week` ist dann die Summe aus `total_roulette` und `total_poker`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Total winnings with poker
total_poker <- sum(poker_vector)

# Total winnings with roulette
total_roulette <-  

# Total winnings overall
total_week <- 

# Print out total_week
  
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Total winnings with poker
total_poker <- sum(poker_vector)

# Total winnings with roulette
total_roulette <-  sum(roulette_vector)

# Total winnings overall
total_week <- total_roulette + total_poker

# Print out total_week
total_week
```

`@sct`
```{r}
msg = "Ändere nichts an der Definition und Benennung von `poker_vector` und `roulette_vector`."
ex()  %>% check_object("days_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)
ex() %>% check_object("poker_vector",  undefined_msg = msg, ) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)


ex() %>% check_object("total_poker") %>% check_equal(incorrect_msg = "Stelle sicher, dass du `total_poker` die Summe des `poker_vector` zuweist.")

ex() %>% check_object("total_roulette") %>% check_equal(incorrect_msg = "Stelle sicher, dass du `total_roulette` die Summe des `roulette_vector` zuweist.")

ex() %>% check_object("total_week") %>% check_equal(incorrect_msg = "Stelle sicher, dass du `total_week` die Summe der beiden anderen Gesamtvektoren zuweist: `total_roulette` und `total_poker`.")

ex() %>% check_output_expr("total_week", missing_msg = "Vergiss nicht, `total_week` in einer neuen Zeile zu schreiben, um die Variable auszugeben.")

success_msg("Gut gemacht. Das sind ziemlich schlechte Nachrichten...")
```

---

## Gesamtgewinne vergleichen

```yaml
type: NormalExercise
key: f532f5332d
xp: 100
skills:
  - 1
```

Hoppla, es sieht so aus, als ob du Geld verlierst! Du solltest deine Strategie überdenken und anpassen. Das erfordert eine genauere Analyse… 

Nach kurzem Brainstorming im Whirlpool deines Hotels wird dir eine mögliche Erklärung bewusst: Vielleicht sind deine Fähigkeiten beim Roulette nicht so gut wie beim Poker. Vielleicht sind also deine Gesamtgewinne beim Poker höher (also `>`) als beim Roulette.

`@instructions`
- Berechne `total_poker` und `total_roulette` wie in der vorigen Übung. Verwende die Funktion `sum()` zweimal.
- Überprüfe, ob dein Gesamtgewinn beim Poker höher ist als beim Roulette, indem du die Werte vergleichst. Gib das Ergebnis dieses Vergleichs aus. Was schlussfolgerst du daraus? Solltest du lieber Roulette oder Poker spielen?

`@hint`
- Die Lösung hast du teilweise schon in der vorigen Übung berechnet.
- Um zu prüfen, ob 6 größer als 5 ist, gibst du `6 > 5` ein. Dies gibt einen logischen Wert zurück (`TRUE` oder `FALSE`).

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Calculate total gains for poker and roulette
total_poker <-
total_roulette <-

# Check if you realized higher total gains in poker than in roulette

```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Calculate total gains for poker and roulette
total_poker <- sum(poker_vector)
total_roulette <- sum(roulette_vector)

# Check if you realized higher total gains in poker than in roulette
total_poker > total_roulette
```

`@sct`
```{r}
msg <- "Ändere nichts an der Definition und Benennung von `poker_vector` und `roulette_vector`."
ex()  %>% check_object("days_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg)  %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)


ex()  %>% check_object("total_poker")  %>% check_equal(incorrect_msg = "Stelle sicher, dass du `total_poker` die Summe des `poker_vector` zuweist. Verwende `sum()`.")
ex()  %>% check_object("total_roulette")  %>% check_equal(incorrect_msg = "Stelle sicher, dass du `total_roulette` die Summe des `roulette_vector` zuweist. Verwende `sum()`.")
ex() %>% check_output_expr("total_poker > total_roulette",missing_msg = "Hast du den Vergleich korrekt durchgeführt? Um zu überprüfen, ob `total_poker` größer als `total_roulette` ist, kannst du `total_poker > total_roulette` verwenden.")

success_msg("Gut gemacht! Weiter zur nächsten Übung.")
```

---

## Vektorelemente auswählen

```yaml
type: NormalExercise
key: 8d78be44e9
xp: 100
skills:
  - 1
```

Deine Vermutung scheint richtig zu sein. Offenbar ist Poker eher dein Ding als Roulette. 

Eine weitere mögliche Erklärung könnte sich durch den Vergleich deiner Leistung zu Wochenbeginn und zum Wochenende ergeben. Schließlich hast du ja am Ende der Woche ein paar Margarita-Cocktails getrunken… 

Um diese Frage zu beantworten, musst du dich auf einen Teil von `total_vector` konzentrieren. Mit anderen Worten: Du möchtest bestimmte Elemente des Vektors auswählen. Um Elemente von Vektoren (und später von Matrizen, Dataframes usw.) auszuwählen, kannst du eckige Klammern verwenden. Zwischen den eckigen Klammern gibst du an, welche Elemente du auswählen möchtest. Um zum Beispiel das erste Element des Vektors auszuwählen, gibst du `poker_vector[1]` ein. Um das zweite Element des Vektors auszuwählen, gibst du `poker_vector[2]` ein. Und so weiter. Beachte, dass das erste Element in einem Vektor den Index 1 hat, also nicht 0 wie in vielen anderen Programmiersprachen!

`@instructions`
Weise den Gewinn/Verlust beim Poker vom Mittwoch der Variablen `poker_wednesday` zu.

`@hint`
Der Mittwoch („Wednesday“) ist das dritte Element von `poker_vector` und kann somit mit `poker_vector[3]` ausgewählt werden.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_wednesday <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_wednesday <- poker_vector[3]
```

`@sct`
```{r}
msg = "Ändere nichts an der Definition und Benennung von `poker_vector` und `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("poker_wednesday", undefined_msg = "Bitte stelle sicher, dass du eine Variable `poker_wednesday` definierst.") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `poker_wednesday` nicht den korrekten Wert des `poker_vector` enthält.")

success_msg("Super! R ermöglicht es auch, mehrere Elemente aus einem Vektor gleichzeitig auszuwählen. Lerne im nächsten Übungsschritt, wie das geht!")
```

---

## Vektorelemente auswählen (2)

```yaml
type: NormalExercise
key: '1351521670'
xp: 100
skills:
  - 1
```

Wie wär’s nun mit einer Analyse deiner Leistung in der Wochenmitte? 

Um mehrere Elemente aus einem Vektor auszuwählen, kannst du eckige Klammern am Ende des Vektors hinzufügen. Zwischen den Klammern gibst du an, welche Elemente ausgewählt werden sollen. Ein Beispiel: Angenommen, du möchtest den ersten und den fünften Tag der Woche auswählen. Verwende den Vektor `c(1, 5)` innerhalb der eckigen Klammern. Der folgende Code wählt das erste und das fünfte Element von `poker_vector` aus:

```
poker_vector[c(1, 5)]
```

`@instructions`
Weise den Gewinn/Verlust beim Poker vom Dienstag, Mittwoch und Donnerstag der Variablen `poker_midweek` zu.

`@hint`
Benutze den Vektor `c(2, 3, 4)` in eckigen Klammern, um die richtigen Elemente von `poker_vector` auszuwählen.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_midweek <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_midweek <- poker_vector[c(2, 3, 4)]
```

`@sct`
```{r}
msg = "Ändere nichts an der Definition und Benennung von `poker_vector` und `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("poker_midweek") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `poker_midweek` nicht die korrekten Werte aus `poker_vector` enthält. Du kannst den Vektor `c(2, 3, 4)` in eckigen Klammern verwenden.")

success_msg("Gut gemacht! Mach weiter mit der nächsten Übung, um dich noch mehr auf die Auswahl von Vektoren zu spezialisieren!");
```

---

## Vektorelemente auswählen (3)

```yaml
type: NormalExercise
key: 27976b79f4
xp: 100
skills:
  - 1
```

Die Auswahl mehrerer Elemente von `poker_vector` mit `c(2, 3, 4)` ist etwas umständlich. Deshalb hat man sich dafür eine einfachere Methode ausgedacht: `c(2, 3, 4)` wird zu `2:4` verkürzt, wodurch ein Vektor mit allen natürlichen Zahlen von 2 bis 4 erzeugt wird.

Eine alternative Möglichkeit, deine Leistung in der Wochenmitte zu bestimmen, ist also `poker_vector[2:4]`. Beachte, wie der Vektor `2:4` in eckigen Klammern geschrieben wird, um die Elemente 2 bis 4 auszuwählen.

`@instructions`
Weise die Gewinne/Verluste beim Roulette von Dienstag bis Freitag der Variablen `roulette_selection_vector` zu. Benutze die Schreibweise mit `:`, wenn dies für dich einfacher ist.

`@hint`
Weise der Variablen `roulette_selection_vector` einen Teil von `roulette_vector` zu, indem du `2:5` in eckige Klammern setzt.

`@pre_exercise_code`
```{r}

```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
roulette_selection_vector <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
roulette_selection_vector <- roulette_vector[2:5]
```

`@sct`
```{r}
msg = "Ändere nichts an der Definition und Benennung von `poker_vector` und `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal( eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_selection_vector", undefined_msg = "Bitte stelle sicher, dass du eine Variable `roulette_selection_vector` definierst.") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `roulette_selection_vector` nicht die richtige Auswahl aus `roulette_vector` enthält. Stelle sicher, dass du die richtigen Indizes verwendest.")

success_msg("Super! Der Doppelpunkt-Operator ist extrem nützlich und wird sehr oft in der R-Programmierung verwendet, also merke ihn dir gut. Gehe zur nächsten Übung weiter.")
```

---

## Vektorelemente auswählen (4)

```yaml
type: NormalExercise
key: e6c263ddee
xp: 100
skills:
  - 1
```

Eine andere Möglichkeit, die vorige Aufgabe zu lösen, ist die Verwendung der Namen der Vektorelemente (<span translate="no">Monday, Tuesday,</span>…) anstelle ihrer numerischen Positionen. Ein Beispiel: 

```
poker_vector["Monday"]
```

Dieser Code wählt das erste Element von `poker_vector` aus, da `"Monday"` der Name dieses ersten Elements ist.

Genau wie in der vorigen Übung mit numerischen Werten kannst du auch hier die Elementnamen verwenden, um mehrere Elemente auszuwählen. Zum Beispiel: 

```
poker_vector[c("Monday","Tuesday")]
```

`@instructions`
- Wähle die ersten drei Elemente in `poker_vector` anhand ihrer Namen aus: `"Monday"`, `"Tuesday"` und `"Wednesday"`. Weise dieses Auswahlergebnis `poker_start` zu.
- Berechne den Durchschnitt der Werte in `poker_start` mit der Funktion `mean()`. Gib das Ergebnis einfach aus, damit du es überprüfen kannst.

`@hint`
- Du kannst `c("Monday", "Tuesday", "Wednesday")` in eckigen Klammern verwenden, um die entsprechenden Elemente von `poker_vector` auszuwählen.
- Mit `mean(poker_start)` kannst du den Mittelwert der Elemente in `poker_start` berechnen. Du brauchst nicht den Mittelwert aller Pokertage, sondern nur den der ersten drei Tage.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Select poker results for Monday, Tuesday and Wednesday
poker_start <- 
  
# Calculate the average of the elements in poker_start

```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Select poker results for Monday, Tuesday and Wednesday
poker_start <- poker_vector[c("Monday", "Tuesday", "Wednesday")]
  
# Calculate the average of the elements in poker_start
mean(poker_start)
```

`@sct`
```{r}
msg = "Ändere nichts an der Definition und Benennung von `poker_vector` und `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("poker_start") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `poker_start` nicht die ersten drei Werte von `poker_vector` enthält. Du kannst `c(\"Monday\", \"Tuesday\", \"Wednesday\")` in eckigen Klammern verwenden, um dies zu tun.")

ex() %>% check_output_expr("mean(poker_start)", missing_msg = "Hast du den Durchschnitt der Werte in `poker_start` korrekt berechnet und ausgegeben? Verwende `mean(poker_start)`.")

success_msg("Gut gemacht! Neben dem Subsetten von Vektoren nach Index oder Name kannst du Vektoren auch durch Vergleich subsetten. Die nächsten Übungen zeigen dir, wie das geht!")
```

---

## Auswahl durch Vergleich – Schritt 1

```yaml
type: NormalExercise
key: f0f619c901
xp: 100
skills:
  - 1
```

Mithilfe von Vergleichsoperatoren kannst du die zuvor gestellte Frage proaktiver angehen. 

Die in R bekannten (logischen) Vergleichsoperatoren sind:

- `<` (kleiner als)
- `>` (größer als)
- `<=` (kleiner als oder gleich)
- `>=` (größer als oder gleich)
- `==` (gleich)
- `!=` (ungleich)

Wie im vorigen Kapitel gesehen, liefert `6 > 5` die Ausgabe `TRUE`. Das Schöne an R ist, dass du diese Vergleichsoperatoren auch auf Vektoren anwenden kannst. Zum Beispiel:

```
c(4, 5, 6) > 5
[1] FALSE FALSE TRUE
```

Dieser Code prüft für jedes Element des Vektors, ob die für den Vergleichsoperator angegebene Bedingung als `TRUE` oder `FALSE` ausgewertet wird, und zeigt das Ergebnis an.

`@instructions`
- Überprüfe, welche Elemente in `poker_vector` positiv sind (also > 0), und weise dies `selection_vector` zu. 
- Gib `selection_vector` aus, damit du das Ergebnis überprüfen kannst. Die Ausgabe zeigt dir für jeden Tag, ob du Geld gewonnen (`TRUE`) oder verloren (`FALSE`) hast.

`@hint`
Um herauszufinden, an welchen Tagen du im Poker Gewinne erzielt hast, sollte R für jedes Element von `poker_vector` prüfen, ob es größer als null ist. Mittels `some_vector > 0` kannst du R mitteilen, wonach du suchst.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- 
  
# Print out selection_vector

```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0
  
# Print out selection_vector
selection_vector
```

`@sct`
```{r}
msg <- "Ändere nichts an der Definition und Benennung von `poker_vector` und `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("selection_vector") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `selection_vector` nicht das richtige Ergebnis enthält. Denk daran, dass R elementweise Operationen für Vektoren verwendet.")

ex() %>% check_output_expr("selection_vector", missing_msg = "Vergiss nicht, `selection_vector` auszugeben, indem du den Variablennamen in eine neue Zeile schreibst.")

success_msg("Super!")
```

---

## Auswahl durch Vergleich – Schritt 2

```yaml
type: NormalExercise
key: 2754fc5cd4
xp: 100
skills:
  - 1
```

Die Nutzung von Vergleichen wird dir viele Datenanalysen erleichtern. Anstatt selbst (wie bisher) eine Teilmenge von zu analysierenden Tagen auszuwählen, kannst du R einfach anweisen, nur die Tage auszugeben, an denen du beim Pokern einen Gewinn erzielt hast. 

In den vorigen Übungen hast du `selection_vector <- poker_vector > 0` benutzt, um die Tage zu bestimmen, an denen du beim Poker Geld gewonnen hast. Jetzt möchtest du nicht nur wissen, an welchen Tagen du Geld gewonnen hast, sondern auch, wie viel du an diesen Tagen gewonnen hast. 

Du kannst die gewünschten Elemente auswählen, indem du `selection_vector` in die eckigen Klammern nach `poker_vector` setzt:

```
poker_vector[selection_vector]
```

R weiß, was zu tun ist, wenn du einen logischen Vektor in eckigen Klammern übergibst: Es werden nur die Elemente ausgewählt, für die `TRUE` in `selection_vector` zurückgegeben wird.

`@instructions`
Benutze `selection_vector` in eckigen Klammern, um die an guten Tagen gewonnenen Beträge der Variablen `poker_winning_days` zuzuweisen.

`@hint`
Benutze `poker_vector[selection_vector]`, um die gewünschten Elemente aus `poker_vector` auszuwählen, und weise das Ergebnis `poker_winning_days` zu.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0

# Select from poker_vector these days
poker_winning_days <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0

# Select from poker_vector these days
poker_winning_days <- poker_vector[selection_vector]
```

`@sct`
```{r}
msg = "Ändere nichts an der Definition und Benennung von `poker_vector` und `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("selection_vector") %>% check_equal(incorrect_msg = "Ändere nicht die Art und Weise, wie `selection_vector` berechnet wird.")

ex() %>% check_object("poker_winning_days") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `poker_winning_days` nicht das richtige Ergebnis enthält. Verwende `poker_vector[selection_vector]`.")

success_msg("Gut gemacht! Weiter zur nächsten Übung.")
```

---

## Erweiterte Auswahl

```yaml
type: NormalExercise
key: 59e8dcbbd5
xp: 100
skills:
  - 1
```

Genau wie für Poker willst du auch für Roulette wissen, an welchen Tagen du einen Gewinn erzielt hast.

`@instructions`
- Erstelle die Variable `selection_vector`, um nun herauszufinden, an welchen Tagen du beim Roulette Gewinn gemacht hast.
- Weise die Geldbeträge, die du an guten Tagen im Roulette gewonnen hast, der Variablen `roulette_winning_days` zu. Dieser Vektor enthält also die positiven Werte von `roulette_vector`.

`@hint`
Wenn du `selection_vector` richtig berechnet hast, kannst du wieder `roulette_vector[selection_vector]` verwenden, um die positiven Werte von `roulette_vector` auszuwählen.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on roulette?
selection_vector <-

# Select from roulette_vector these days
roulette_winning_days <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on roulette?
selection_vector <- roulette_vector > 0

# Select from roulette_vector these days
roulette_winning_days <- roulette_vector[selection_vector]
```

`@sct`
```{r}
msg = "Ändere nichts an der Definition und Benennung von `poker_vector` und `roulette_vector`."

ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("selection_vector") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `selection_vector` nicht das richtige Ergebnis enthält. Verwende `roulette_vector > 0`.")

ex() %>% check_object("roulette_winning_days") %>% check_equal(incorrect_msg = "Es sieht so aus, als ob `roulette_winning_days` nicht das richtige Ergebnis enthält. Verwende `roulette_vector[selection_vector]`.")


success_msg("Super! Diese Übung schließt das Kapitel über Vektoren ab. Das nächste Kapitel wird dich in die zweidimensionale Version von Vektoren einführen: Matrizen.")
```
