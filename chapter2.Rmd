---
title_meta: Chapitre 2
title: Vecteurs
description: >-
  Nous vous emmenons à Las Vegas, où vous apprendrez à analyser les résultats de
  vos jeux de hasard à l'aide de vecteurs dans R. Après avoir terminé ce
  chapitre, vous serez en mesure de créer des vecteurs dans R, de les nommer,
  d'en sélectionner des éléments et de comparer différents vecteurs.
---

## Créer un vecteur

```yaml
type: NormalExercise
key: d9b453dbdd
xp: 100
skills:
  - 1
```

Vous vous sentez en veine ? Vous avez intérêt, car ce chapitre vous emmène dans la Ville aux mille tentations, également connue sous le nom de *Paradis des statisticiens* !

Grâce à R et à vos nouvelles compétences en matière d'analyse de données, vous apprendrez à améliorer vos performances aux tables de jeu et à lancer votre carrière de joueur professionnel. Ce chapitre vous montrera comment vous pouvez facilement suivre l'évolution de vos paris et comment vous pouvez effectuer des analyses simples sur vos actions passées. Prochain arrêt, Vegas Baby... VEGAS !

`@instructions`
- Vous souvenez-vous encore de ce que vous avez appris dans le premier chapitre ? Affectez la valeur `"Go!"` à la variable `vegas`. Rappelez-vous : R est sensible à la casse !

`@hint`
Tapez simplement la ligne suivante dans l'éditeur :
```
vegas <- "Go!"
```

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Define the variable vegas
vegas <- 
```

`@solution`
```{r}
# Define the variable vegas
vegas <- "Go!"
```

`@sct`
```{r}
ex() %>% check_object("vegas") %>% check_equal(incorrect_msg = "Assurez-vous d'assigner la valeur correcte à `vegas`. N'oubliez pas que R est sensible à la casse !")

success_msg("Super ! Passez à l'exercice suivant.")
```

---

## Créer un vecteur (2)

```yaml
type: NormalExercise
key: fd427db76f
xp: 100
skills:
  - 1
```

Concentrons-nous d'abord ! 

Sur le chemin qui vous mènera de la misère à la fortune, vous ferez un usage intensif des vecteurs. Les vecteurs sont des tableaux à une dimension qui peuvent contenir des valeurs numériques, des chaînes de caractères ou des données logiques. En d'autres termes, un vecteur est un outil simple pour stocker des données. Par exemple, vous pouvez enregistrer vos gains et pertes quotidiens dans les casinos. 

Dans R, vous créez un vecteur avec la fonction combine `c()`. Vous placez les éléments vectoriels séparés par une virgule entre les parenthèses. Par exemple :

```
numeric_vector <- c(1, 2, 3)
character_vector <- c("a", "b", "c")
```

Une fois que vous avez créé ces vecteurs dans R, vous pouvez les utiliser pour effectuer des calculs.

`@instructions`
Complétez le code de manière à ce que `boolean_vector` contienne les trois éléments : `TRUE`, `FALSE` et `TRUE` (dans cet ordre).

`@hint`
Affectez `c(TRUE, FALSE, TRUE)` à la variable `boolean_vector` avec l'opérateur `<-`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Complete the code for boolean_vector
boolean_vector <-
```

`@solution`
```{r}
numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Complete the code for boolean_vector
boolean_vector <- c(TRUE, FALSE, TRUE)
```

`@sct`
```{r}
msg <- "Ne modifiez pas le code qui a défini `numeric_vector` et `character_vector` !"
ex() %>% check_object("numeric_vector", undefined_msg = msg) %>% check_equal(, incorrect_msg = msg)

ex() %>% check_object("character_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("boolean_vector") %>% check_equal(incorrect_msg = "Assurez-vous d'assigner les valeurs correctes à `boolean_vector`. Utilisez `c(TRUE, FALSE, TRUE)`. Ne mettez pas de guillemets autour de `TRUE` et `FALSE` ! Assurez-vous également d'adopter le même ordre que celui indiqué dans les instructions.")

success_msg("Parfait ! Remarquez qu'ajouter un espace derrière les virgules dans la fonction `c()` améliore la lisibilité de votre code. Pratiquons encore un peu la création de vecteurs dans le prochain exercice.")
```

---

## Créer un vecteur (3)

```yaml
type: NormalExercise
key: 9f41229dbc
xp: 100
skills:
  - 1
```

Après une semaine à Las Vegas et toujours aucune Ferrari dans votre garage, vous décidez qu'il est temps de commencer à utiliser vos super-pouvoirs d'analyse de données.

Avant de procéder à une première analyse, vous décidez de rassembler tous les gains et toutes les pertes de la semaine écoulée : 

Pour `poker_vector` : 

- Lundi, vous avez gagné $140
- Mardi, vous avez perdu $50
- Mercredi, vous avez gagné $20 
- Jeudi, vous avez perdu $120
- Vendredi, vous avez gagné $240

Pour `roulette_vector` : 

- Lundi, vous avez perdu $24
- Mardi, vous avez perdu $50
- Mercredi, vous avez gagné $100
- Jeudi, vous avez perdu $350
- Vendredi, vous avez gagné $10

Vous ne jouiez qu'au poker et à la roulette, car une délégation de médiums occupait les tables de craps. Pour pouvoir utiliser ces données dans R, vous décidez de créer les variables `poker_vector` et `roulette_vector`.

`@instructions`
Affectez les gains et les pertes de la roulette à la variable `roulette_vector`. Vous avez perdu $24, puis $50, gagné $100, perdu $350 et gagné $10.

`@hint`
Pour vous aider dans cette étape, l'éditeur contient déjà le code de création de `poker_vector`. Attribuez les valeurs correctes à `roulette_vector` en fonction des chiffres de l'affectation. N'oubliez pas que les pertes sont des nombres négatifs.

`@pre_exercise_code`
```{r}

```

`@sample_code`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <-  
```

`@solution`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)
```

`@sct`
```{r}
ex() %>% check_object("poker_vector") %>% check_equal(incorrect_msg = "Assurez-vous d'assigner les valeurs correctes à `poker_vector`.")

ex() %>% check_object("roulette_vector") %>% check_equal(incorrect_msg = "Assurez-vous d'assigner les valeurs correctes à `roulette_vector`. Assurez-vous d'adopter le bon ordre !")

success_msg("Très bien ! Pour vérifier le contenu de vos vecteurs, n'oubliez pas que vous pouvez toujours simplement taper la variable dans la console et appuyer sur Entrée. Passez à l'exercice suivant !")
```

---

## Nommer un vecteur

```yaml
type: NormalExercise
key: 3b0b80b192
xp: 100
skills:
  - 1
```

En tant qu'analyste de données, il est important d'avoir une vision claire des données que vous utilisez. Il est donc essentiel de comprendre à quoi se réfère chaque élément. 

Dans l'exercice précédent, nous avons créé un vecteur avec vos gains au cours de la semaine. Chaque élément du vecteur fait référence à un jour de la semaine, mais il est difficile de savoir quel élément correspond à quel jour. Il serait bon que vous puissiez l'indiquer dans le vecteur lui-même. 

Vous pouvez donner un nom aux éléments d'un vecteur à l'aide de la fonction `names()`. Regardez cet exemple :

```
some_vector <- c("John Doe", "poker player")
names(some_vector) <- c("Name", "Profession")
```

Ce code crée d'abord un vecteur `some_vector` et donne ensuite un nom aux deux éléments. Le premier élément porte le nom de `Name`, tandis que le second élément porte le nom de `Profession`. L'affichage du contenu sur la console produit la sortie suivante :

```

          Name     Profession 

    "John Doe" "poker player" 

```

`@instructions`
- Le code dans l'éditeur nomme les éléments dans `poker_vector` avec les jours de la semaine. Ajoutez du code pour faire la même chose pour `roulette_vector`.

`@hint`
Vous pouvez utiliser `names(roulette_vector)` pour définir les noms de la variable `roulette_vector`. Veillez à utiliser le même vecteur avec les jours de la semaine comme noms. N'oubliez pas que R est sensible à la casse !

`@pre_exercise_code`
```{r}

```

`@sample_code`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# Assign days as names of poker_vector
names(poker_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Assign days as names of roulette_vector

```

`@solution`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# Assign days as names of poker_vector
names(poker_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Assign days as names of roulette_vector
names(roulette_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
```

`@sct`
```{r}
ex() %>% check_object("poker_vector") %>% check_equal(incorrect_msg = "Ne modifiez pas les valeurs à l'intérieur de `poker_vector`; elles ont déjà été codées pour vous.")

ex() %>% check_object("roulette_vector") %>% check_equal(incorrect_msg = "Ne modifiez pas les valeurs à l'intérieur de `roulette_vector`; elles ont déjà été codées pour vous.")

ex() %>% check_object("poker_vector") %>% check_equal(eq_condition = 'equal', incorrect_msg = "Ne modifiez pas le code qui nomme les éléments dans `poker_vector`; concentrez-vous sur `roulette_vector`!")

ex() %>% check_object("roulette_vector") %>% check_equal(eq_condition = 'equal',incorrect_msg = "Assurez-vous d'assigner le bon vecteur de noms à `roulette_vector`. Utilisez exactement le même vecteur que celui qui a été utilisé pour nommer `poker_vector`.")


success_msg("Bien joué ! Continuez à l'exercice suivant.")
```

---

## Nommer un vecteur (2)

```yaml
type: NormalExercise
key: 6858c65a4a
xp: 100
skills:
  - 1
```

Si vous voulez devenir un bon statisticien, vous devez devenir efficace.

Dans les exercices précédents, vous avez probablement constaté qu'il est ennuyeux et frustrant de taper et retaper des informations telles que les jours de la semaine. Toutefois, si l'on prend un peu de hauteur, on constate qu'il existe un moyen plus efficace de procéder, à savoir affecter le vecteur des jours de la semaine à une **variable**. 

Tout comme vous l'avez fait pour les rendements du poker et de la roulette, vous pouvez également créer une variable contenant les jours de la semaine. Vous pouvez ainsi l'utiliser et la réutiliser.

`@instructions`
- Une variable `days_vector` contenant les jours de la semaine a déjà été créée pour vous.
- Utilisez `days_vector` pour définir les noms de `poker_vector` et `roulette_vector`.

`@hint`
Vous pouvez utiliser `names(poker_vector) <- days_vector` pour définir les noms des éléments `poker_vector`. Faites de même pour `roulette_vector`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# The variable days_vector
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
 
# Assign the names of the day to roulette_vector and poker_vector
names(poker_vector) <-   
names(roulette_vector) <-
```

`@solution`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# The variable days_vector
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Assign the names of the day to roulette_vector and poker_vector
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector
```

`@sct`
```{r}
msg <- "Ne modifiez pas les variables prédéfinies `poker_vector`, `roulette_vector` ou `days_vector`."

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("days_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector") %>% check_equal(incorrect_msg = "Assurez-vous d\'assigner `days_vector` aux noms de `poker_vector`.", eq_condition = "equal",)

ex() %>% check_object("roulette_vector") %>% check_equal(eq_condition = "equal", incorrect_msg = "Assurez-vous d\'assigner `days_vector` aux noms de `roulette_vector`.")

success_msg("Bien joué ! Un conseil : essayez d\'éviter la duplication de code à tout moment. Continuez à l\'exercice suivant et apprenez à faire des calculs avec des vecteurs !")
```

---

## Calculer le total des gains

```yaml
type: NormalExercise
key: da995f099f
xp: 100
skills:
  - 1
```

Maintenant que vous disposez des gains de poker et de roulette sous forme de vecteurs nommés, vous pouvez commencer à faire de l'analyse de données. 

Vous souhaitez obtenir le type d'informations suivant :

- Quel a été votre bénéfice ou votre perte globale par jour de la semaine ?
- Avez-vous perdu de l'argent au total au cours de la semaine ?
- Est-ce que vous gagnez ou perdez de l'argent plutôt au poker ou à la roulette ?

Pour obtenir les réponses, vous devez effectuer des calculs arithmétiques sur les vecteurs. 

Il est important de savoir que si vous additionnez deux vecteurs dans R, c'est la somme par élément qui est prise en compte. Par exemple, les trois affirmations suivantes sont tout à fait équivalentes :

```
c(1, 2, 3) + c(4, 5, 6)
c(1 + 4, 2 + 5, 3 + 6)
c(5, 7, 9)
```

Vous pouvez également effectuer les calculs avec des variables représentant des vecteurs :

```
a <- c(1, 2, 3) 
b <- c(4, 5, 6)
c <- a + b
```

`@instructions`
- Prenez la somme des variables `A_vector` et `B_vector` et affectez-la à `total_vector`.
- Vérifiez le résultat en affichant `total_vector`.

`@hint`
Utilisez l'opérateur `+` pour additionner `A_vector` et `B_vector`. Utilisez `<-` pour affecter le résultat à `total_vector`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
A_vector <- c(1, 2, 3)
B_vector <- c(4, 5, 6)

# Take the sum of A_vector and B_vector
total_vector <- 
  
# Print out total_vector

```

`@solution`
```{r}
A_vector <- c(1, 2, 3)
B_vector <- c(4, 5, 6)

# Take the sum of A_vector and B_vector
total_vector <- A_vector + B_vector

# Print out total_vector
total_vector
```

`@sct`
```{r}
msg <- "Ne modifiez pas le contenu de `A_vector` ou `B_vector` !"
ex() %>% check_object("A_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("B_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("total_vector") %>% check_equal(incorrect_msg = "Assurez-vous que `total_vector` contient la somme de `A_vector` et `B_vector`.")

ex() %>% check_output_expr("total_vector", missing_msg = "N'oubliez pas d'imprimer `total_vector` ! Écrivez simplement `total_vector` sur une nouvelle ligne.")

success_msg("Bon travail ! Continuez à l'exercice suivant.")
```

---

## Calculer le total des gains (2)

```yaml
type: NormalExercise
key: 2969d8ed65
xp: 100
skills:
  - 1
```

Maintenant que vous avez compris comment R fait de l'arithmétique avec des vecteurs, il est temps de mettre les Ferrari dans votre garage ! Tout d'abord, vous devez comprendre quel a été le bénéfice ou la perte globale par jour de la semaine. Le bénéfice quotidien total est la somme des bénéfices et des pertes que vous avez réalisés au poker par jour et des bénéfices et des pertes que vous avez réalisés à la roulette par jour. 

Dans R, il s'agit simplement de la somme de `roulette_vector` et `poker_vector`.

`@instructions`
Attribuez à la variable `total_daily` le montant total que vous avez gagné ou perdu chaque jour (poker et roulette confondus).

`@hint`
Comme dans l'exercice précédent, affectez la somme de deux vecteurs à une nouvelle variable, `total_daily`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Assign to total_daily how much you won/lost on each day
total_daily <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Assign to total_daily how much you won/lost on each day
total_daily <- poker_vector + roulette_vector
```

`@sct`
```{r}
msg = "Ne changez rien à la définition et au nommage de `poker_vector` et `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal( incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("total_daily") %>% check_equal(incorrect_msg = "Assurez-vous d'assigner la somme de `poker_vector` et `roulette_vector` à `total_daily`.")

success_msg("Super ! Continuez à l'exercice suivant.")
```

---

## Calculer le total des gains (3)

```yaml
type: NormalExercise
key: e66a56b9f0
xp: 100
skills:
  - 1
```

Sur la base de l'analyse précédente, il semble que vous ayez eu un mélange de bons et de mauvais jours. Ce n'est pas ce que votre ego attendait, et vous vous demandez s'il n'y a pas une infime chance que vous ayez perdu de l'argent au total sur la semaine ? 

Une fonction qui vous aide à répondre à cette question est `sum()`. Il calcule la somme de tous les éléments d'un vecteur. Par exemple, pour calculer la somme totale d'argent que vous avez perdue ou gagnée au poker, vous devez faire ce qui suit : 

```
total_poker <- sum(poker_vector)
```

`@instructions`
- Calculez le montant total d'argent que vous avez gagné ou perdu à la roulette et affectez-le à la variable `total_roulette`.
- Maintenant que vous avez les totaux pour la roulette et le poker, vous pouvez facilement calculer `total_week` (qui est la somme de tous les gains et pertes de la semaine).
- Affichez `total_week`.

`@hint`
Utilisez la fonction `sum()` pour obtenir le total de `roulette_vector`. `total_week` est alors la somme de `total_roulette` et `total_poker`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Total winnings with poker
total_poker <- sum(poker_vector)

# Total winnings with roulette
total_roulette <-  

# Total winnings overall
total_week <- 

# Print out total_week
  
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Total winnings with poker
total_poker <- sum(poker_vector)

# Total winnings with roulette
total_roulette <-  sum(roulette_vector)

# Total winnings overall
total_week <- total_roulette + total_poker

# Print out total_week
total_week
```

`@sct`
```{r}
msg = "Ne changez rien à la définition et au nommage de `poker_vector` et `roulette_vector`."
ex()  %>% check_object("days_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)
ex() %>% check_object("poker_vector",  undefined_msg = msg, ) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)


ex() %>% check_object("total_poker") %>% check_equal(incorrect_msg = "Assurez-vous d\'attribuer à `total_poker` la somme de `poker_vector`.")

ex() %>% check_object("total_roulette") %>% check_equal(incorrect_msg = "Assurez-vous d\'attribuer à `total_roulette` la somme de `roulette_vector`.")

ex() %>% check_object("total_week") %>% check_equal(incorrect_msg = "Assurez-vous d\'attribuer à `total_week` la somme des deux autres vecteurs totaux : `total_roulette` et `total_poker`.")

ex() %>% check_output_expr("total_week", missing_msg = "N\'oubliez pas d\'écrire `total_week` sur une nouvelle ligne pour afficher la variable.")

success_msg("Bien joué. C'est une très mauvaise nouvelle...")
```

---

## Comparaison du total des gains

```yaml
type: NormalExercise
key: f532f5332d
xp: 100
skills:
  - 1
```

Oups, il semble que vous perdiez de l'argent. Il est temps de repenser et d'adapter votre stratégie ! Cela nécessitera une analyse plus approfondie… 

Après une brève réflexion dans le jacuzzi de votre hôtel, vous vous rendez compte qu'une explication possible est que vos compétences à la roulette ne sont pas aussi développées que vos compétences au poker. Il se peut donc que le total de vos gains au poker soit plus élevé que (ou `>`) vos gains à la roulette.

`@instructions`
- Calculez `total_poker` et `total_roulette` comme dans l'exercice précédent. Utilisez deux fois la fonction `sum()`.
- Vérifiez si le total de vos gains au poker est plus élevé qu'à la roulette en utilisant une comparaison. Il suffit d'afficher le résultat de cette comparaison. Qu'en concluez-vous ? Avez-vous plutôt intérêt à vous concentrer sur la roulette ou sur le poker ?

`@hint`
- Vous avez déjà partiellement calculé la réponse à cette question dans l'exercice précédent.
- Pour vérifier si 6 est plus grand que 5, vous tapez `6 > 5`. Cette fonction renvoie une valeur logique (`TRUE` ou `FALSE`).

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Calculate total gains for poker and roulette
total_poker <-
total_roulette <-

# Check if you realized higher total gains in poker than in roulette

```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Calculate total gains for poker and roulette
total_poker <- sum(poker_vector)
total_roulette <- sum(roulette_vector)

# Check if you realized higher total gains in poker than in roulette
total_poker > total_roulette
```

`@sct`
```{r}
msg <- "Ne changez rien à la définition et au nommage de `poker_vector` et `roulette_vector`."
ex()  %>% check_object("days_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg)  %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)


ex()  %>% check_object("total_poker")  %>% check_equal(incorrect_msg = "Assurez-vous d\'assigner à `total_poker` la somme de `poker_vector`. Utilisez `sum()`.")
ex()  %>% check_object("total_roulette")  %>% check_equal(incorrect_msg = "Assurez-vous d\'assigner à `total_roulette` la somme de `roulette_vector`. Utilisez `sum()`.")
ex() %>% check_output_expr("total_poker > total_roulette",missing_msg = "Avez-vous correctement effectué la comparaison ? Pour vérifier si `total_poker` est supérieur à `total_roulette`, vous pouvez utiliser `total_poker > total_roulette`.")

success_msg("Bon travail ! Continuez avec l'exercice suivant.")
```

---

## Sélection de vecteurs : les bons moments

```yaml
type: NormalExercise
key: 8d78be44e9
xp: 100
skills:
  - 1
```

Votre intuition semblait être la bonne. Il semble que le poker soit plus votre tasse de thé que la roulette. 

Votre performance au début de la semaine par rapport à la fin est une autre piste à explorer. Vous avez bu quelques Margaritas à la fin de la semaine… 

Pour répondre à cette question, vous ne devez vous intéresser qu'à une sélection du vecteur `total_vector`. En d'autres termes, notre objectif est de sélectionner des éléments spécifiques du vecteur. Pour sélectionner des éléments d'un vecteur (et plus tard des matrices, des trames de données…), vous pouvez utiliser des crochets. Entre les crochets, vous indiquez les éléments à sélectionner. Par exemple, pour sélectionner le premier élément du vecteur, vous tapez `poker_vector[1]`. Pour sélectionner le deuxième élément du vecteur, vous tapez `poker_vector[2]`, etc. Notez que le premier élément d'un vecteur a l'indice 1, et non 0 comme dans de nombreux autres langages de programmation.

`@instructions`
Affectez les résultats du poker de <span translate="no">Wednesday</span> à la variable `poker_wednesday`.

`@hint`
Mercredi est le troisième élément de `poker_vector`, et peut donc être sélectionné avec `poker_vector[3]`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_wednesday <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_wednesday <- poker_vector[3]
```

`@sct`
```{r}
msg = "Ne changez rien à la définition et au nommage de `poker_vector` et `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("poker_wednesday", undefined_msg = "Veuillez vous assurer de définir une variable `poker_wednesday`.") %>% check_equal(incorrect_msg = "Il semble que `poker_wednesday` ne contient pas la valeur correcte de `poker_vector`.")

success_msg("Super ! R permet également de sélectionner plusieurs éléments d'un vecteur à la fois. Apprenez comment dans l'exercice suivant !")
```

---

## Sélection de vecteurs : les bons moments (2)

```yaml
type: NormalExercise
key: '1351521670'
xp: 100
skills:
  - 1
```

Et si vous analysiez vos résultats en milieu de semaine ? 

Pour sélectionner plusieurs éléments d'un vecteur, vous pouvez ajouter des crochets à la fin de celui-ci. Vous pouvez indiquer entre les crochets quels éléments doivent être sélectionnés. Par exemple : supposons que vous souhaitiez sélectionner le premier et le cinquième jour de la semaine : utilisez le vecteur `c(1, 5)` entre les crochets. Par exemple, le code ci-dessous sélectionne le premier et le cinquième élément de `poker_vector` :

```
poker_vector[c(1, 5)]
```

`@instructions`
Attribuez les résultats de poker de <span translate="no">Tuesday, Wednesday</span> et <span translate="no">Thursday</span> à la variable `poker_midweek`.

`@hint`
Utilisez le vecteur `c(2, 3, 4)` entre crochets pour sélectionner les éléments corrects de `poker_vector`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_midweek <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_midweek <- poker_vector[c(2, 3, 4)]
```

`@sct`
```{r}
msg = "Ne changez rien à la définition et au nommage de `poker_vector` et `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("poker_midweek") %>% check_equal(incorrect_msg = "Il semble que `poker_midweek` ne contienne pas les valeurs correctes de `poker_vector`. Vous pouvez utiliser le vecteur `c(2, 3, 4)` entre crochets.")

success_msg("Bien joué ! Continuez à l'exercice suivant pour vous spécialiser davantage dans la sélection de vecteurs !");
```

---

## Sélection de vecteurs : les bons moments (3)

```yaml
type: NormalExercise
key: 27976b79f4
xp: 100
skills:
  - 1
```

La sélection de plusieurs éléments de `poker_vector` à l'aide de `c(2, 3, 4)` n'est pas très pratique. Les statisticiens ont créé une méthode plus simple : `c(2, 3, 4)` peut être abrégé en`2:4`, qui génère un vecteur contenant tous les nombres naturels de 2 à 4.

Pour trouver les résultats en milieu de semaine, une autre méthode serait donc de consulter `poker_vector[2:4]`. Remarquez que le vecteur est placé entre les crochets pour sélectionner les éléments 2 à 4.

`@instructions`
Attribuez à `roulette_selection_vector` les résultats de la roulette de <span translate="no">Tuesday</span> jusqu'à <span translate="no">Friday</span>; utilisez `:` si cela vous facilite la tâche.

`@hint`
Attribuez une sélection de `roulette_vector` à `roulette_selection_vector` en plaçant `2:5` entre crochets.

`@pre_exercise_code`
```{r}

```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
roulette_selection_vector <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
roulette_selection_vector <- roulette_vector[2:5]
```

`@sct`
```{r}
msg = "Ne changez rien à la définition et au nommage de `poker_vector` et `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal( eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_selection_vector", undefined_msg = "Veuillez vous assurer de définir une variable `roulette_selection_vector`.") %>% check_equal(incorrect_msg = "Il semble que `roulette_selection_vector` ne contient pas la sélection correcte de `roulette_vector`. Assurez-vous d'utiliser les bons index.")

success_msg("Génial ! L'opérateur deux-points est extrêmement utile et très souvent utilisé en programmation R, alors retenez-le bien. Passez à l'exercice suivant.")
```

---

## Sélection de vecteurs : les bons moments (4)

```yaml
type: NormalExercise
key: e6c263ddee
xp: 100
skills:
  - 1
```

IL y a une autre façon d'aborder l'exercice précédent, qui consiste à utiliser les noms des éléments du vecteur (<span translate="no">Monday, Tuesday,</span> ...) au lieu de leurs positions numériques. Par exemple, 

```
poker_vector["Monday"]
```

sélectionnera le premier élément de `poker_vector` puisque `"Monday"` est le nom de ce premier élément.

Tout comme vous l'avez fait dans l'exercice précédent avec les chiffres, vous pouvez également utiliser les noms des éléments pour sélectionner plusieurs éléments, par exemple : 

```
poker_vector[c("Monday","Tuesday")]
```

`@instructions`
- Sélectionnez les trois premiers éléments de `poker_vector` en utilisant leurs noms : `"Monday"`, `"Tuesday"` et `"Wednesday"`. Affectez le résultat de la sélection à `poker_start`.
- Calculez la moyenne des valeurs de `poker_start` à l'aide de la fonction `mean()`. Il suffit d'afficher le résultat pour pouvoir l'examiner.

`@hint`
- Vous pouvez utiliser `c("Monday", "Tuesday", "Wednesday")` entre crochets afin de subdiviser `poker_vector` de manière appropriée.
- Vous pouvez utiliser `mean(poker_start)` pour obtenir la moyenne des éléments de `poker_start`. Vous n'avez pas besoin de la moyenne de tous les éléments de poker, mais seulement des trois premiers jours.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Select poker results for Monday, Tuesday and Wednesday
poker_start <- 
  
# Calculate the average of the elements in poker_start

```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Select poker results for Monday, Tuesday and Wednesday
poker_start <- poker_vector[c("Monday", "Tuesday", "Wednesday")]
  
# Calculate the average of the elements in poker_start
mean(poker_start)
```

`@sct`
```{r}
msg = "Ne changez rien à la définition et au nommage de `poker_vector` et `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("poker_start") %>% check_equal(incorrect_msg = "Il semble que `poker_start` ne contient pas les trois premières valeurs de `poker_vector`. Vous pouvez utiliser `c(\"Monday\", \"Tuesday\", \"Wednesday\")` entre crochets pour le faire.")

ex() %>% check_output_expr("mean(poker_start)", missing_msg = "Avez-vous correctement calculé la moyenne des valeurs dans `poker_start` et l\'avez-vous imprimée ? Utilisez `mean(poker_start)`.")

success_msg("Bon travail ! En plus de sous-ensembles de vecteurs par index ou par nom, vous pouvez également sous-ensembles de vecteurs par comparaison. Les prochains exercices vous montreront comment !")
```

---

## Sélection par comparaison - Étape 1

```yaml
type: NormalExercise
key: f0f619c901
xp: 100
skills:
  - 1
```

En utilisant des opérateurs de comparaison, nous pouvons aborder la question précédente de manière plus proactive. 

Les opérateurs de comparaison (logiques) connus de R sont les suivants :

- `<` inférieur à
- `>` supérieur à
- `<=` inférieur ou égal à
- `>=` supérieur ou égal à
- `==` égal à
- `!=` différent de

Comme nous l'avons vu dans le chapitre précédent, l'énoncé de `6 > 5` renvoie à `TRUE`. L'avantage de R est que vous pouvez utiliser ces opérateurs de comparaison également sur les vecteurs. Par exemple :

```
c(4, 5, 6) > 5
[1] FALSE FALSE TRUE
```

Cette commande teste chaque élément du vecteur si la condition énoncée par l'opérateur de comparaison est `TRUE` ou `FALSE`.

`@instructions`
- Vérifiez quels éléments de `poker_vector` sont positifs (c'est-à-dire > 0) et affectez-les à `selection_vector`. 
- Affichez `selection_vector` afin de pouvoir l'examiner. La sortie vous indique si vous avez gagné (`TRUE`) ou perdu (`FALSE`) de l'argent pour chaque jour.

`@hint`
Afin de vérifier pour quels jours vos gains au poker sont positifs, R doit vérifier pour chaque élément de `poker_vector` s'il est supérieur à zéro. `some_vector > 0` est le moyen d'indiquer à R ce que vous recherchez.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- 
  
# Print out selection_vector

```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0
  
# Print out selection_vector
selection_vector
```

`@sct`
```{r}
msg <- "Ne changez rien à la définition et au nommage de `poker_vector` et `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("selection_vector") %>% check_equal(incorrect_msg = "Il semble que `selection_vector` ne contient pas le résultat correct. N'oubliez pas que R utilise des opérations élément par élément pour les vecteurs.")

ex() %>% check_output_expr("selection_vector", missing_msg = "N'oubliez pas d'imprimer `selection_vector` en écrivant le nom de la variable sur une nouvelle ligne.")

success_msg("Super !")
```

---

## Sélection par comparaison - Étape 2

```yaml
type: NormalExercise
key: 2754fc5cd4
xp: 100
skills:
  - 1
```

Travailler avec des comparaisons vous facilitera la vie en matière d'analyse de données. Au lieu de sélectionner un sous-ensemble de jours à étudier vous-même (comme auparavant), vous pouvez simplement demander à R de vous renvoyer uniquement les jours où vous avez réalisé un résultat positif au poker. 

Dans les exercices précédents, vous avez utilisé `selection_vector <- poker_vector > 0` pour trouver les jours où vous avez eu un résultat positif au poker. Vous souhaitez maintenant connaître non seulement les jours où vous avez gagné, mais aussi le montant de vos gains. 

Vous pouvez sélectionner les éléments souhaités en plaçant `selection_vector` entre les crochets qui suivent `poker_vector` :

```
poker_vector[selection_vector]
```

R sait ce qu'il faut faire lorsque vous passez un vecteur logique entre crochets : il ne sélectionnera que les éléments qui correspondent à `TRUE` dans `selection_vector`.

`@instructions`
Utilisez `selection_vector` entre crochets pour affecter les montants que vous avez gagnés les jours rentables à la variable `poker_winning_days`.

`@hint`
Utilisez `poker_vector[selection_vector]` pour sélectionner les éléments souhaités à partir de `poker_vector`, et affectez le résultat à `poker_winning_days`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0

# Select from poker_vector these days
poker_winning_days <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0

# Select from poker_vector these days
poker_winning_days <- poker_vector[selection_vector]
```

`@sct`
```{r}
msg = "Ne changez rien à la définition et au nommage de `poker_vector` et `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("selection_vector") %>% check_equal(incorrect_msg = "Ne changez pas la façon dont `selection_vector` est calculé.")

ex() %>% check_object("poker_winning_days") %>% check_equal(incorrect_msg = "Il semble que `poker_winning_days` ne contient pas le bon résultat. Utilisez `poker_vector[selection_vector]`.")

success_msg("Bon travail ! Continuez à l'exercice suivant.")
```

---

## Sélection avancée

```yaml
type: NormalExercise
key: 59e8dcbbd5
xp: 100
skills:
  - 1
```

Tout comme vous l'avez fait pour le poker, vous souhaitez également connaître les jours où vous avez réalisé un résultat positif à la roulette.

`@instructions`
- Créez la variable `selection_vector`, cette fois pour voir si vous avez fait des bénéfices avec la roulette pour différents jours.
- Affectez les montants que vous avez gagnés les jours où vous avez terminé positivement à la roulette à la variable `roulette_winning_days`. Ce vecteur contient donc les gains positifs de `roulette_vector`.

`@hint`
Une fois que vous avez correctement calculé `selection_vector`, vous pouvez à nouveau utiliser `roulette_vector[selection_vector]` pour sélectionner les résultats positifs de `roulette_vector`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on roulette?
selection_vector <-

# Select from roulette_vector these days
roulette_winning_days <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on roulette?
selection_vector <- roulette_vector > 0

# Select from roulette_vector these days
roulette_winning_days <- roulette_vector[selection_vector]
```

`@sct`
```{r}
msg = "Ne changez rien à la définition et au nommage de `poker_vector` et `roulette_vector`."

ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("selection_vector") %>% check_equal(incorrect_msg = "Il semble que `selection_vector` ne contient pas le bon résultat. Utilisez `roulette_vector > 0`.")

ex() %>% check_object("roulette_winning_days") %>% check_equal(incorrect_msg = "Il semble que `roulette_winning_days` ne contient pas le bon résultat. Utilisez `roulette_vector[selection_vector]`.")

success_msg("Super ! Cet exercice conclut le chapitre sur les vecteurs. Le prochain chapitre vous introduira à la version bidimensionnelle des vecteurs : les matrices.")
```
