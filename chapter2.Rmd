---
title_meta: Hoofdstuk 2
title: Vectors
description: >-
  We nemen je mee naar Las Vegas, waar je leert hoe je je gokresultaten
  analyseert met vectors in R. Na dit hoofdstuk kun je vectors in R maken, ze
  een naam geven, er elementen uit selecteren en verschillende vectors met
  elkaar vergelijken.
---

## Maak een vector

```yaml
type: NormalExercise
key: d9b453dbdd
xp: 100
skills:
  - 1
```

Heb je geluk? Mooi zo, want dit hoofdstuk neemt je mee naar de City of Sins, ook wel bekend als het *paradijs voor statistici*!

Dankzij R en je nieuwe data-analytische skills leer je hoe je je prestaties in het casino kunt verbeteren en je carrière als professioneel gokker een kickstart kunt geven. In dit hoofdstuk zie je hoe je eenvoudig je inzet bijhoudt en simpele analyses doet op eerdere acties. Volgende halte: Vegas, baby… VEGAS!!

`@instructions`
- Weet je nog wat je in het eerste hoofdstuk hebt geleerd? Ken de waarde `"Go!"` toe aan de variabele `vegas`. Let op: R is hoofdlettergevoelig!

`@hint`
Typ gewoon de volgende regel in de editor:
```
vegas <- "Go!"
```

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Define the variable vegas
vegas <- 
```

`@solution`
```{r}
# Define the variable vegas
vegas <- "Go!"
```

`@sct`
```{r}
ex() %>% check_object("vegas") %>% check_equal(incorrect_msg = "Zorg ervoor dat je de juiste waarde toewijst aan `vegas`. Vergeet niet dat R hoofdlettergevoelig is!")

success_msg("Geweldig! Ga verder naar de volgende oefening.")
```

---

## Maak een vector (2)

```yaml
type: NormalExercise
key: fd427db76f
xp: 100
skills:
  - 1
```

Eerst even focussen!

Op weg van blut naar rijk maak je veel gebruik van vectors. Vectors zijn eendimensionale arrays die numerieke, tekstuele of logische data kunnen bevatten. Met andere woorden: een vector is een eenvoudig hulpmiddel om data op te slaan. Je kunt er bijvoorbeeld je dagelijkse winst en verlies in het casino in opslaan.

In R maak je een vector met de combine-functie `c()`. Je plaatst de elementen, gescheiden door komma’s, tussen de haakjes. Bijvoorbeeld:

```
numeric_vector <- c(1, 2, 3)
character_vector <- c("a", "b", "c")
```

Zodra je deze vectors in R hebt aangemaakt, kun je ze gebruiken om berekeningen mee te doen.

`@instructions`
Maak de code af, zodat `boolean_vector` de drie elementen `TRUE`, `FALSE` en `TRUE` bevat (in die volgorde).

`@hint`
Wijs `c(TRUE, FALSE, TRUE)` toe aan de variabele `boolean_vector` met de operator `<-`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Complete the code for boolean_vector
boolean_vector <-
```

`@solution`
```{r}
numeric_vector <- c(1, 10, 49)
character_vector <- c("a", "b", "c")

# Complete the code for boolean_vector
boolean_vector <- c(TRUE, FALSE, TRUE)
```

`@sct`
```{r}
msg <- "Verander de code die `numeric_vector` en `character_vector` definieerde niet!"
ex() %>% check_object("numeric_vector", undefined_msg = msg) %>% check_equal(, incorrect_msg = msg)

ex() %>% check_object("character_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("boolean_vector") %>% check_equal(incorrect_msg = "Zorg ervoor dat je de juiste waarden toewijst aan `boolean_vector`. Gebruik `c(TRUE, FALSE, TRUE)`. Plaats geen aanhalingstekens rond `TRUE` en `FALSE`! Zorg er ook voor dat je dezelfde volgorde aanhoudt als in de instructies.")

success_msg("Perfect! Merk op dat het toevoegen van een spatie achter de komma's in de `c()` functie de leesbaarheid van je code verbetert. Laten we in de volgende oefening nog wat meer oefenen met het maken van vectoren.")
```

---

## Maak een vector (3)

```yaml
type: NormalExercise
key: 9f41229dbc
xp: 100
skills:
  - 1
```

Na een week in Las Vegas en nog geen enkele Ferrari in je garage, besluit je dat het tijd is om je data-analytische superkrachten te gebruiken.

Voor je aan een eerste analyse begint, verzamel je eerst alle winsten en verliezen van de afgelopen week: 

Voor `poker_vector`: 

- Op maandag won je $ 140
- Dinsdag verloor je $ 50
- Woensdag won je $ 20 
- Donderdag verloor je $ 120
- Vrijdag won je $ 240

Voor `roulette_vector`: 

- Op maandag verloor je $ 24
- Dinsdag verloor je $ 50
- Woensdag won je $ 100
- Donderdag verloor je $ 350
- Vrijdag won je $ 10

Je speelde alleen poker en roulette, want er was een delegatie mediums die de craps-tafels bezet hield. Om deze gegevens in R te kunnen gebruiken, besluit je de variabelen `poker_vector` en `roulette_vector` te maken.

`@instructions`
Ken de winsten/verliezen voor roulette toe aan de variabele `roulette_vector`. Je verloor $ 24, vervolgens verloor je $ 50, won je $ 100, verloor je $ 350 en won je $ 10.

`@hint`
Om je op weg te helpen, staat de code om `poker_vector` te maken al in de editor. Ken de juiste waarden toe aan `roulette_vector` op basis van de getallen in de opdracht. Vergeet niet dat verliezen negatieve getallen zijn.

`@pre_exercise_code`
```{r}

```

`@sample_code`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <-  
```

`@solution`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)
```

`@sct`
```{r}
ex() %>% check_object("poker_vector") %>% check_equal(incorrect_msg = "Zorg ervoor dat je de juiste waarden toewijst aan `poker_vector`.")

ex() %>% check_object("roulette_vector") %>% check_equal(incorrect_msg = "Zorg ervoor dat je de juiste waarden toewijst aan `roulette_vector`. Zorg ervoor dat je de juiste volgorde aanhoudt!")

success_msg("Heel goed! Om de inhoud van je vectoren te bekijken, onthoud dat je altijd simpelweg de variabele in de console kunt typen en op Enter kunt drukken. Ga door naar de volgende oefening!")
```

---

## Een vector een naam geven

```yaml
type: NormalExercise
key: 3b0b80b192
xp: 100
skills:
  - 1
```

Als data-analist is het belangrijk dat je een helder beeld hebt van de data die je gebruikt. Begrijpen waar elk element naar verwijst, is daarom essentieel.

In de vorige oefening hebben we een vector gemaakt met je winst per dag van de week. Elk element in de vector hoort bij een weekdag, maar het is lastig te zien welk element bij welke dag hoort. Het zou handig zijn als je dat in de vector zelf kunt aangeven.

Je kunt de elementen van een vector een naam geven met de functie `names()`. Kijk maar naar dit voorbeeld:

```
some_vector <- c("John Doe", "poker player")
names(some_vector) <- c("Name", "Profession")
```

Deze code maakt eerst een vector `some_vector` en geeft vervolgens de twee elementen een naam. Het eerste element krijgt de naam `Name`, terwijl het tweede element het label `Profession` krijgt. Als je de inhoud naar de console print, krijg je de volgende output:

```
          Name     Profession 
    "John Doe" "poker player" 
```

`@instructions`
- De code in de editor kent de weekdagen toe als naam aan de elementen in `poker_vector`. Voeg code toe om hetzelfde te doen voor `roulette_vector`.

`@hint`
Je kunt `names(roulette_vector)` gebruiken om de namen van de variabele `roulette_vector` in te stellen. Gebruik hetzelfde vectorobject met de weekdagen als naam. Onthoud dat R hoofdlettergevoelig is!

`@pre_exercise_code`
```{r}

```

`@sample_code`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# Assign days as names of poker_vector
names(poker_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Assign days as names of roulette_vector

```

`@solution`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# Assign days as names of poker_vector
names(poker_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Assign days as names of roulette_vector
names(roulette_vector) <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
```

`@sct`
```{r}
ex() %>% check_object("poker_vector") %>% check_equal(incorrect_msg = "Verander de waarden in `poker_vector` niet; ze waren al voor je gecodeerd.")

ex() %>% check_object("roulette_vector") %>% check_equal(incorrect_msg = "Verander de waarden in `roulette_vector` niet; ze waren al voor je gecodeerd.")

ex() %>% check_object("poker_vector") %>% check_equal(eq_condition = 'equal', incorrect_msg = "Verander de code die de elementen in `poker_vector` benoemt niet; concentreer je op `roulette_vector`!")

ex() %>% check_object("roulette_vector") %>% check_equal(eq_condition = 'equal',incorrect_msg = "Zorg ervoor dat je de juiste namenvector toewijst aan `roulette_vector`. Gebruik exact dezelfde vector als degene die werd gebruikt om `poker_vector` te benoemen.")

success_msg("Goed gedaan! Ga door naar de volgende oefening.")
```

---

## Een vector een naam geven (2)

```yaml
type: NormalExercise
key: 6858c65a4a
xp: 100
skills:
  - 1
```

Als je een goede statisticus wilt worden, moet je efficiënt werken.

In de vorige oefeningen merkte je waarschijnlijk dat het saai en frustrerend is om informatie zoals de dagen van de week steeds opnieuw te typen. Als je het van een hoger niveau bekijkt, is er een efficiëntere manier: geef de vector met de dagen van de week een **variabele**!

Net zoals je deed met je poker- en roulette-opbrengsten, kun je ook een variabele maken die de dagen van de week bevat. Die kun je hergebruiken wanneer je wilt.

`@instructions`
- Er is al een variabele `days_vector` voor je aangemaakt met de dagen van de week.
- Gebruik `days_vector` om de namen van `poker_vector` en `roulette_vector` in te stellen.

`@hint`
Je kunt `names(poker_vector) <- days_vector` gebruiken om de namen van de elementen van `poker_vector` in te stellen. Doe iets vergelijkbaars voor `roulette_vector`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# The variable days_vector
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
 
# Assign the names of the day to roulette_vector and poker_vector
names(poker_vector) <-   
names(roulette_vector) <-
```

`@solution`
```{r}
# Poker winnings from Monday to Friday
poker_vector <- c(140, -50, 20, -120, 240)

# Roulette winnings from Monday to Friday
roulette_vector <- c(-24, -50, 100, -350, 10)

# The variable days_vector
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")

# Assign the names of the day to roulette_vector and poker_vector
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector
```

`@sct`
```{r}
msg <- "Verander de vooraf gedefinieerde variabelen `poker_vector`, `roulette_vector` of `days_vector` niet."

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("days_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector") %>% check_equal(incorrect_msg = "Zorg ervoor dat je `days_vector` toewijst aan de namen van `poker_vector`.", eq_condition = "equal",)

ex() %>% check_object("roulette_vector") %>% check_equal(eq_condition = "equal", incorrect_msg = "Zorg ervoor dat je `days_vector` toewijst aan de namen van `roulette_vector`.")


success_msg("Goed gedaan! Een tip: probeer altijd code duplicatie te vermijden. Ga door naar de volgende oefening en leer hoe je rekenkundige bewerkingen met vectoren kunt uitvoeren!")
```

---

## Totale winst berekenen

```yaml
type: NormalExercise
key: da995f099f
xp: 100
skills:
  - 1
```

Nu je de poker- en roulettewinsten netjes in benoemde vectoren hebt vastgelegd, kun je wat data-analyse-magie gaan doen.

Je wilt het volgende te weten komen:

- Wat is je totale winst of verlies per dag van de week?
- Heb je over de hele week gezien geld verloren?
- Win of verlies je geld met poker of met roulette?

Om de antwoorden te krijgen, moet je rekenkundige bewerkingen op vectoren uitvoeren.

Het is belangrijk om te weten dat als je in R twee vectoren optelt, dit elementgewijs gebeurt. De volgende drie statements zijn bijvoorbeeld volledig gelijkwaardig:

```
c(1, 2, 3) + c(4, 5, 6)
c(1 + 4, 2 + 5, 3 + 6)
c(5, 7, 9)
```

Je kunt de berekeningen ook doen met variabelen die naar vectoren verwijzen:

```
a <- c(1, 2, 3) 
b <- c(4, 5, 6)
c <- a + b
```

`@instructions`
- Tel de variabelen `A_vector` en `B_vector` op en ken dit toe aan `total_vector`.
- Bekijk het resultaat door `total_vector` te printen.

`@hint`
Gebruik de `+`-operator om `A_vector` en `B_vector` op te tellen. Gebruik `<-` om het resultaat toe te kennen aan `total_vector`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
A_vector <- c(1, 2, 3)
B_vector <- c(4, 5, 6)

# Take the sum of A_vector and B_vector
total_vector <- 
  
# Print out total_vector

```

`@solution`
```{r}
A_vector <- c(1, 2, 3)
B_vector <- c(4, 5, 6)

# Take the sum of A_vector and B_vector
total_vector <- A_vector + B_vector

# Print out total_vector
total_vector
```

`@sct`
```{r}
msg <- "Verander de inhoud van `A_vector` of `B_vector` niet!"
ex() %>% check_object("A_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("B_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("total_vector") %>% check_equal(incorrect_msg = "Zorg ervoor dat `total_vector` de som van `A_vector` en `B_vector` bevat.")

ex() %>% check_output_expr("total_vector", missing_msg = "Vergeet niet om `total_vector` af te drukken! Schrijf gewoon `total_vector` op een nieuwe regel.")

success_msg("Goed gedaan! Ga door naar de volgende oefening.")
```

---

## Totale winst berekenen (2)

```yaml
type: NormalExercise
key: 2969d8ed65
xp: 100
skills:
  - 1
```

Nu je weet hoe R rekent met vectoren, is het tijd om die Ferrari's in je garage te zetten! Eerst moet je begrijpen wat de totale winst of het totale verlies per dag van de week was. De totale dagelijkse winst is de som van de winst of het verlies per dag met poker en de winst of het verlies per dag met roulette.

In R is dit gewoon de som van `roulette_vector` en `poker_vector`.

`@instructions`
Ken aan de variabele `total_daily` toe hoeveel je per dag in totaal hebt gewonnen of verloren (poker en roulette samen).

`@hint`
Net als in de vorige oefening ken je de som van twee vectoren toe aan een nieuwe variabele, `total_daily`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Assign to total_daily how much you won/lost on each day
total_daily <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Assign to total_daily how much you won/lost on each day
total_daily <- poker_vector + roulette_vector
```

`@sct`
```{r}
msg = "Verander niets aan de definitie en naamgeving van `poker_vector` en `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal( incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("total_daily") %>% check_equal(incorrect_msg = "Zorg ervoor dat je de som van `poker_vector` en `roulette_vector` toewijst aan `total_daily`.")

success_msg("Geweldig! Ga door naar de volgende oefening.")
```

---

## Totale winst berekenen (3)

```yaml
type: NormalExercise
key: e66a56b9f0
xp: 100
skills:
  - 1
```

Op basis van de vorige analyse had je een mix van goede en slechte dagen. Dat is niet wat je ego verwachtte, en je vraagt je af of er een heel kleine kans is dat je over de hele week gezien geld hebt verloren?

Een functie die je helpt bij het beantwoorden van deze vraag is `sum()`. Die berekent de som van alle elementen in een vector. Om bijvoorbeeld het totale bedrag te berekenen dat je met poker hebt verloren/gewonnen, voer je uit:

```
total_poker <- sum(poker_vector)
```

`@instructions`
- Bereken het totale bedrag dat je met roulette hebt gewonnen/verloren en ken dit toe aan de variabele `total_roulette`.
- Nu je de totalen voor roulette en poker hebt, kun je eenvoudig `total_week` berekenen (dit is de som van alle winsten en verliezen van de week).
- Print `total_week`.

`@hint`
Gebruik de functie `sum()` om het totaal van `roulette_vector` te krijgen. `total_week` is vervolgens de som van `total_roulette` en `total_poker`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Total winnings with poker
total_poker <- sum(poker_vector)

# Total winnings with roulette
total_roulette <-  

# Total winnings overall
total_week <- 

# Print out total_week
  
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Total winnings with poker
total_poker <- sum(poker_vector)

# Total winnings with roulette
total_roulette <-  sum(roulette_vector)

# Total winnings overall
total_week <- total_roulette + total_poker

# Print out total_week
total_week
```

`@sct`
```{r}
msg = "Verander niets aan de definitie en naamgeving van `poker_vector` en `roulette_vector`."
ex()  %>% check_object("days_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)
ex() %>% check_object("poker_vector",  undefined_msg = msg, ) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)


ex() %>% check_object("total_poker") %>% check_equal(incorrect_msg = "Zorg ervoor dat je aan `total_poker` de som van de `poker_vector` toewijst.")

ex() %>% check_object("total_roulette") %>% check_equal(incorrect_msg = "Zorg ervoor dat je aan `total_roulette` de som van de `roulette_vector` toewijst.")

ex() %>% check_object("total_week") %>% check_equal(incorrect_msg = "Zorg ervoor dat je aan `total_week` de som van de andere twee totale vectoren toewijst: `total_roulette` en `total_poker`.")

ex() %>% check_output_expr("total_week", missing_msg = "Vergeet niet om `total_week` op een nieuwe regel te schrijven om de variabele af te drukken.")

success_msg("Goed gedaan. Dit is behoorlijk slecht nieuws...")
```

---

## Totale winst vergelijken

```yaml
type: NormalExercise
key: f532f5332d
xp: 100
skills:
  - 1
```

Oeps, het lijkt erop dat je geld verliest. Tijd om je strategie te herzien en aan te passen! Dat vraagt om wat diepere analyse...

Na een korte brainstorm in de jacuzzi van je hotel bedenk je dat een mogelijke verklaring is dat je skills in roulette minder goed ontwikkeld zijn dan in poker. Misschien is je totale winst in poker dus hoger (of `>`) dan in roulette.

`@instructions`
- Bereken `total_poker` en `total_roulette` zoals in de vorige oefening. Gebruik twee keer de functie `sum()`.
- Check of je totale winst in poker hoger is dan in roulette met een vergelijking. Print simpelweg het resultaat van deze vergelijking. Wat concludeer je: moet je je richten op roulette of op poker?

`@hint`
- Je hebt het antwoord op deze vraag al deels in de vorige oefening berekend!
- Om te checken of 6 groter is dan 5, typ je `6 > 5`. Dit levert een logische waarde op (`TRUE` of `FALSE`).

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Calculate total gains for poker and roulette
total_poker <-
total_roulette <-

# Check if you realized higher total gains in poker than in roulette

```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Calculate total gains for poker and roulette
total_poker <- sum(poker_vector)
total_roulette <- sum(roulette_vector)

# Check if you realized higher total gains in poker than in roulette
total_poker > total_roulette
```

`@sct`
```{r}
msg <- "Verander niets aan de definitie en naamgeving van `poker_vector` en `roulette_vector`."
ex()  %>% check_object("days_vector", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg)  %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex()  %>% check_object("total_poker")  %>% check_equal(incorrect_msg = "Zorg ervoor dat je aan `total_poker` de som van de `poker_vector` toewijst. Gebruik `sum()`.")
ex()  %>% check_object("total_roulette")  %>% check_equal(incorrect_msg = "Zorg ervoor dat je aan `total_roulette` de som van de `roulette_vector` toewijst. Gebruik `sum()`.")
ex() %>% check_output_expr("total_poker > total_roulette",missing_msg = "Heb je de vergelijking correct uitgevoerd? Om te controleren of `total_poker` groter is dan `total_roulette`, kun je `total_poker > total_roulette` gebruiken.")

success_msg("Goed gedaan! Ga door naar de volgende oefening.")
```

---

## Vectorselectie: mooie tijden

```yaml
type: NormalExercise
key: 8d78be44e9
xp: 100
skills:
  - 1
```

Je voorgevoel klopte. Het lijkt erop dat poker meer jouw ding is dan roulette.

Een andere aanpak is om je prestaties aan het begin van de werkweek te vergelijken met die aan het einde. Je had aan het einde van de week tenslotte een paar Margarita’s op...

Om die vraag te beantwoorden, wil je je alleen richten op een selectie van `total_vector`. Met andere woorden: we willen specifieke elementen van de vector selecteren. Om elementen uit een vector (en later ook matrices, data frames, …) te selecteren, gebruik je vierkante haken. Tussen de vierkante haken geef je aan welke elementen je wilt selecteren. Zo typ je bijvoorbeeld `poker_vector[1]` om het eerste element van de vector te selecteren. Voor het tweede element typ je `poker_vector[2]`, enzovoort. Let op: het eerste element in een vector heeft index 1, niet 0 zoals in veel andere programmeertalen.

`@instructions`
Wijs de pokerresultaten van <span translate="no">Wednesday</span> toe aan de variabele `poker_wednesday`.

`@hint`
Wednesday is het derde element van `poker_vector` en kun je dus selecteren met `poker_vector[3]`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_wednesday <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_wednesday <- poker_vector[3]
```

`@sct`
```{r}
msg = "Verander niets aan de definitie en naamgeving van `poker_vector` en `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("poker_wednesday", undefined_msg = "Zorg ervoor dat je een variabele `poker_wednesday` definieert.") %>% check_equal(incorrect_msg = "Het lijkt erop dat `poker_wednesday` niet de juiste waarde van de `poker_vector` bevat.")

success_msg("Geweldig! R maakt het ook mogelijk om meerdere elementen uit een vector tegelijk te selecteren. Leer hoe in de volgende oefening!")
```

---

## Elementen kiezen: mooie tijden (2)

```yaml
type: NormalExercise
key: '1351521670'
xp: 100
skills:
  - 1
```

Zullen we je resultaten midden in de week eens bekijken?

Om meerdere elementen uit een vector te selecteren, zet je vierkante haken achter de vector. Tussen de haken geef je aan welke elementen je wilt selecteren. Stel dat je de eerste en vijfde dag van de week wilt selecteren: gebruik dan de vector `c(1, 5)` tussen de vierkante haken. De code hieronder selecteert bijvoorbeeld het eerste en vijfde element van `poker_vector`:

```
poker_vector[c(1, 5)]
```

`@instructions`
Wijs de pokerresultaten van <span translate="no">Tuesday, Wednesday</span> en <span translate="no">Thursday</span> toe aan de variabele `poker_midweek`.

`@hint`
Gebruik de vector `c(2, 3, 4)` tussen vierkante haken om de juiste elementen van `poker_vector` te selecteren.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_midweek <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
poker_midweek <- poker_vector[c(2, 3, 4)]
```

`@sct`
```{r}
msg = "Verander niets aan de definitie en naamgeving van `poker_vector` en `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("poker_midweek") %>% check_equal(incorrect_msg = "Het lijkt erop dat `poker_midweek` niet de juiste waarden van `poker_vector` bevat. Je kunt de vector `c(2, 3, 4)` binnen vierkante haken gebruiken.")

success_msg("Goed gedaan! Ga verder naar de volgende oefening om je verder te specialiseren in vectorselectie!");
```

---

## Vectorselectie: mooie tijden (3)

```yaml
type: NormalExercise
key: 27976b79f4
xp: 100
skills:
  - 1
```

Meerdere elementen uit `poker_vector` selecteren met `c(2, 3, 4)` is niet erg handig. Statistici hebben hiervoor een eenvoudiger manier bedacht: `c(2, 3, 4)` kun je afkorten tot `2:4`, wat een vector genereert met alle natuurlijke getallen van 2 tot en met 4.

Een andere manier om de resultaten midden in de week te vinden, is dus `poker_vector[2:4]`. Let op: de vector `2:4` staat tussen vierkante haken om element 2 tot en met 4 te selecteren.

`@instructions`
Ken de roulette-resultaten van <span translate="no">Tuesday</span> tot en met <span translate="no">Friday</span> toe aan `roulette_selection_vector`; gebruik `:` als dat makkelijker is.

`@hint`
Wijs een selectie van `roulette_vector` toe aan `roulette_selection_vector` door `2:5` tussen vierkante haken te zetten.

`@pre_exercise_code`
```{r}

```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
roulette_selection_vector <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Define a new variable based on a selection
roulette_selection_vector <- roulette_vector[2:5]
```

`@sct`
```{r}
msg = "Verander niets aan de definitie en naamgeving van `poker_vector` en `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal( eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_selection_vector", undefined_msg = "Zorg ervoor dat je een variabele `roulette_selection_vector` definieert.") %>% check_equal(incorrect_msg = "Het lijkt erop dat `roulette_selection_vector` niet de juiste selectie uit `roulette_vector` bevat. Zorg ervoor dat je de juiste indexen gebruikt.")

success_msg("Geweldig! De dubbele punt operator is enorm handig en wordt vaak gebruikt in R-programmering, dus onthoud het goed. Ga door naar de volgende oefening.")
```

---

## Vectorselectie: mooie tijden (4)

```yaml
type: NormalExercise
key: e6c263ddee
xp: 100
skills:
  - 1
```

Een andere manier om de vorige oefening aan te pakken, is door de namen van de vector-elementen (<span translate="no">Monday, Tuesday,</span> ...) te gebruiken in plaats van hun numerieke posities. Bijvoorbeeld,

```
poker_vector["Monday"]
```

zal het eerste element van `poker_vector` selecteren, omdat `"Monday"` de naam is van dat eerste element.

Net zoals je in de vorige oefening met getallen deed, kun je ook de elementnamen gebruiken om meerdere elementen te selecteren, bijvoorbeeld:

```
poker_vector[c("Monday","Tuesday")]
```

`@instructions`
- Selecteer de eerste drie elementen in `poker_vector` door hun namen te gebruiken: `"Monday"`, `"Tuesday"` en `"Wednesday"`. Ken het resultaat van de selectie toe aan `poker_start`.
- Bereken het gemiddelde van de waarden in `poker_start` met de functie `mean()`. Print het resultaat, zodat je het kunt bekijken.

`@hint`
- Je kunt `c("Monday", "Tuesday", "Wednesday")` binnen rechte haken gebruiken om `poker_vector` op de juiste manier te subsetten.
- Je kunt `mean(poker_start)` gebruiken om het gemiddelde van de elementen in `poker_start` te berekenen. Je hebt niet het gemiddelde van alle poker-elementen nodig, maar alleen van de eerste drie dagen.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Select poker results for Monday, Tuesday and Wednesday
poker_start <- 
  
# Calculate the average of the elements in poker_start

```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Select poker results for Monday, Tuesday and Wednesday
poker_start <- poker_vector[c("Monday", "Tuesday", "Wednesday")]
  
# Calculate the average of the elements in poker_start
mean(poker_start)
```

`@sct`
```{r}
msg = "Verander niets aan de definitie en naamgeving van `poker_vector` en `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("poker_start") %>% check_equal(incorrect_msg = "Het lijkt erop dat `poker_start` niet de eerste drie waarden van `poker_vector` bevat. Je kunt `c(\"Monday\", \"Tuesday\", \"Wednesday\")` binnen vierkante haken gebruiken om dit te doen.")

ex() %>% check_output_expr("mean(poker_start)", missing_msg = "Heb je correct het gemiddelde van de waarden in `poker_start` berekend en afgedrukt? Gebruik `mean(poker_start)`.")

success_msg("Goed gedaan! Naast het subsetten van vectors op index of naam, kun je ook vectors subsetten door vergelijking. De volgende oefeningen laten je zien hoe!")
```

---

## Selecteren door te vergelijken - Stap 1

```yaml
type: NormalExercise
key: f0f619c901
xp: 100
skills:
  - 1
```

Met vergelijkingsoperatoren kunnen we de vorige vraag proactiever aanpakken.

De (logische) vergelijkingsoperatoren die R kent zijn:

- `<` voor kleiner dan
- `>` voor groter dan
- `<=` voor kleiner dan of gelijk aan
- `>=` voor groter dan of gelijk aan
- `==` voor gelijk aan elkaar
- `!=` voor niet gelijk aan elkaar

Zoals je in het vorige hoofdstuk zag, geeft `6 > 5` `TRUE` terug. Het mooie aan R is dat je deze vergelijkingsoperatoren ook op vectoren kunt gebruiken. Bijvoorbeeld:

```
c(4, 5, 6) > 5
[1] FALSE FALSE TRUE
```

Deze opdracht test voor elk element van de vector of de voorwaarde die door de vergelijkingsoperator is aangegeven `TRUE` of `FALSE` is.

`@instructions`
- Controleer welke elementen in `poker_vector` positief zijn (dus > 0) en ken dit toe aan `selection_vector`.
- Print `selection_vector`, zodat je het kunt bekijken. De output vertelt je voor elke dag of je geld hebt gewonnen (`TRUE`) of verloren (`FALSE`).

`@hint`
Om te controleren voor welke dagen je pokerrendement positief is, moet R voor elk element van `poker_vector` nagaan of het groter is dan nul. Met `some_vector > 0` laat je R weten wat je bedoelt.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- 
  
# Print out selection_vector

```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0
  
# Print out selection_vector
selection_vector
```

`@sct`
```{r}
msg <- "Verander niets aan de definitie en naamgeving van `poker_vector` en `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("selection_vector") %>% check_equal(incorrect_msg = "Het lijkt erop dat `selection_vector` niet het juiste resultaat bevat. Vergeet niet dat R elementgewijze bewerkingen voor vectoren gebruikt.")

ex() %>% check_output_expr("selection_vector", missing_msg = "Vergeet niet om `selection_vector` af te drukken door de variabelenaam op een nieuwe regel te schrijven.")

success_msg("Goed gedaan!")
```

---

## Selectie via vergelijking - stap 2

```yaml
type: NormalExercise
key: 2754fc5cd4
xp: 100
skills:
  - 1
```

Werken met vergelijkingen maakt je analytische leven een stuk makkelijker. In plaats van zelf een subset van dagen te kiezen om te onderzoeken (zoals eerder), kun je R simpelweg vragen om alleen die dagen terug te geven waarop je een positief resultaat voor poker behaalde.

In de vorige oefeningen gebruikte je `selection_vector <- poker_vector > 0` om de dagen te vinden waarop je een positief pokerresultaat had. Nu wil je niet alleen weten op welke dagen je won, maar ook hoeveel je op die dagen won.

Je kunt de gewenste elementen selecteren door `selection_vector` tussen vierkante haken te zetten achter `poker_vector`:

```
poker_vector[selection_vector]
```

R weet wat het moet doen als je een logische vector tussen vierkante haken doorgeeft: het selecteert alleen de elementen die overeenkomen met `TRUE` in `selection_vector`.

`@instructions`
Gebruik `selection_vector` tussen vierkante haken om de bedragen die je op de winstgevende dagen won toe te kennen aan de variabele `poker_winning_days`.

`@hint`
Gebruik `poker_vector[selection_vector]` om de gewenste elementen uit `poker_vector` te selecteren, en ken het resultaat toe aan `poker_winning_days`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0

# Select from poker_vector these days
poker_winning_days <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on poker?
selection_vector <- poker_vector > 0

# Select from poker_vector these days
poker_winning_days <- poker_vector[selection_vector]
```

`@sct`
```{r}
msg = "Verander niets aan de definitie en naamgeving van `poker_vector` en `roulette_vector`."
ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("roulette_vector",  undefined_msg = msg) %>% check_equal(eq_condition = "equal",incorrect_msg = msg)

ex() %>% check_object("selection_vector") %>% check_equal(incorrect_msg = "Verander niet de manier waarop `selection_vector` wordt berekend.")

ex() %>% check_object("poker_winning_days") %>% check_equal(incorrect_msg = "Het lijkt erop dat `poker_winning_days` niet het juiste resultaat bevat. Gebruik `poker_vector[selection_vector]`.")

success_msg("Goed gedaan! Ga door naar de volgende oefening.")
```

---

## Geavanceerde selectie

```yaml
type: NormalExercise
key: 59e8dcbbd5
xp: 100
skills:
  - 1
```

Net zoals je bij poker deed, wil je ook weten op welke dagen je een positief rendement haalde met roulette.

`@instructions`
- Maak de variabele `selection_vector`, dit keer om te controleren of je op verschillende dagen winst hebt gemaakt met roulette.
- Ken de bedragen die je op de dagen met een positief resultaat voor roulette hebt verdiend toe aan de variabele `roulette_winning_days`. Deze vector bevat dus de positieve opbrengsten uit `roulette_vector`.

`@hint`
Zodra je `selection_vector` correct hebt berekend, kun je `roulette_vector[selection_vector]` opnieuw gebruiken om de positieve resultaten uit `roulette_vector` te selecteren.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on roulette?
selection_vector <-

# Select from roulette_vector these days
roulette_winning_days <- 
```

`@solution`
```{r}
# Poker and roulette winnings from Monday to Friday:
poker_vector <- c(140, -50, 20, -120, 240)
roulette_vector <- c(-24, -50, 100, -350, 10)
days_vector <- c("Monday", "Tuesday", "Wednesday", "Thursday", "Friday")
names(poker_vector) <- days_vector
names(roulette_vector) <- days_vector

# Which days did you make money on roulette?
selection_vector <- roulette_vector > 0

# Select from roulette_vector these days
roulette_winning_days <- roulette_vector[selection_vector]
```

`@sct`
```{r}
msg = "Verander niets aan de definitie en naamgeving van `poker_vector` en `roulette_vector`."

ex() %>% check_object("days_vector", undefined_msg = msg) %>% check_equal(incorrect_msg = msg)

ex() %>% check_object("poker_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("roulette_vector", undefined_msg = msg) %>% check_equal(eq_condition = "equal", incorrect_msg = msg)

ex() %>% check_object("selection_vector") %>% check_equal(incorrect_msg = "Het lijkt erop dat `selection_vector` niet het juiste resultaat bevat. Gebruik `roulette_vector > 0`.")

ex() %>% check_object("roulette_winning_days") %>% check_equal(incorrect_msg = "Het lijkt erop dat `roulette_winning_days` niet het juiste resultaat bevat. Gebruik `roulette_vector[selection_vector]`.")


success_msg("Geweldig! Deze oefening sluit het hoofdstuk over vectoren af. Het volgende hoofdstuk zal je introduceren tot de tweedimensionale versie van vectoren: matrices.")
```
