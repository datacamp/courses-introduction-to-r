---
title_meta: Capitolo 1
title: Introduzione alle basi
description: >-
  Muovi i primi passi con R. In questo capitolo imparerai a usare la console
  come una calcolatrice e ad assegnare le variabili. Conoscerai anche i tipi di
  dati di base in R. Iniziamo.
free_preview: true
---

## Come funziona

```yaml
type: NormalExercise
key: 15d729634a
xp: 100
skills:
  - 1
```

Nell'editor, devi digitare il codice R per risolvere gli esercizi. Quando invii il tuo codice, ogni riga di codice viene interpretata ed eseguita da R e ricevi un messaggio che indica se il tuo codice è corretto o meno. L'output del codice R viene mostrato nella console.

R utilizza il simbolo `#` per aggiungere commenti, in modo da permettere a te e agli altri di capire di cosa tratta il codice R. Proprio come Twitter! I commenti non vengono eseguiti come codice R, quindi non influenzano il risultato. Ad esempio, <span translate="no">_Calculate 3 + 4_</span> nell'editor è un commento.

Puoi anche eseguire i comandi R direttamente nella console. Questo è un buon modo per sperimentare il codice R, in quanto il tuo invio non viene controllato per verificarne la correttezza.

`@instructions`
- Nell'editor sono già presenti alcuni esempi di codice. Riesci a vedere quali sono le righe di codice R e quali i commenti?
- Aggiungi una riga di codice che calcola la somma di 6 e 12 e invia il codice.

`@hint`
Basta aggiungere una riga di codice R che calcola la somma di 6 e 12, proprio come nell'esempio del codice di esempio!

`@pre_exercise_code`
```{r}
# no pec

```

`@sample_code`
```{r}
# Calculate 3 + 4
3 + 4

# Calculate 6 + 12

```

`@solution`
```{r}
# Calculate 3 + 4
3 + 4

# Calculate 6 + 12
6 + 12
```

`@sct`
```{r}
ex() %>% check_output_expr("18", missing_msg = "Assicurati di aggiungere `6 + 12` su una nuova riga. Non iniziare la riga con un `#`, altrimenti il tuo codice R non viene eseguito!")

success_msg("Fantastico! Vedi come la console mostra il risultato del codice R che hai inviato? Ora che hai familiarità con l'interfaccia, passiamo al lavoro con R!")
```

---

## Aritmetica con R

```yaml
type: NormalExercise
key: 720745eda5
xp: 100
skills:
  - 1
```

Nella sua forma più elementare, R può essere utilizzato come una semplice calcolatrice. Considera i seguenti operatori aritmetici:

- Aggiunta: `+`
- Sottrazione: `-`
- Moltiplicazione: `*`
- Divisione: `/`
- Esponenziazione: `^`
- Modulo: `%%`

Gli ultimi due potrebbero richiedere qualche spiegazione:

- L'operatore `^` eleva il numero alla sua sinistra alla potenza del numero alla sua destra: ad esempio `3^2` è 9.
- Il modulo restituisce il resto della divisione del numero a sinistra per il numero alla sua destra, ad esempio 5 modulo 3 o `5 %% 3` è 2.

Con queste conoscenze, segui le istruzioni per completare l'esercizio.

`@instructions`
- Digita `2^5` nell'editor per calcolare 2 alla potenza 5.
- Digita `28 %% 6` per calcolare 28 modulo 6.
- Invia la risposta e guarda l'output di R nella console.
- Nota come il simbolo `#` sia utilizzato per aggiungere commenti al codice R.

`@hint`
Un altro esempio dell'operatore modulo: `9 %% 2` è uguale a `1`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# An addition
5 + 5 

# A subtraction
5 - 5 

# A multiplication
3 * 5

 # A division
(5 + 5) / 2 

# Exponentiation


# Modulo

```

`@solution`
```{r}
# An addition
5 + 5

# A subtraction
5 - 5 

# A multiplication
3 * 5

 # A division
(5 + 5) / 2 

# Exponentiation
2 ^ 5

# Modulo
28 %% 6
```

`@sct`
```{r}
msg = "Non rimuovere gli altri esempi aritmetici!"
ex() %>% check_output_expr("2^5", missing_msg = "L'esempio di esponenziazione non è corretto. Scrivi `2 ^ 5` su una nuova riga.")

ex() %>% check_output_expr("28 %% 6", missing_msg = "Sembra esserci un problema con l'esempio del modulo. Scrivi `28 %% 6` su una nuova riga.")
success_msg("Ottimo! Passa all'esercizio successivo.")
```

---

## Assegnazione delle variabili

```yaml
type: NormalExercise
key: 5f200ffd43
xp: 100
skills:
  - 1
```

Un concetto fondamentale della programmazione (statistica) è chiamato **variabile**. 

Una variabile ti permette di memorizzare un valore (ad esempio 4) o un oggetto (ad esempio la descrizione di una funzione) in R. In seguito potrai utilizzare il nome di questa variabile per accedere facilmente al valore o all'oggetto memorizzato all'interno di questa variabile. 

Puoi assegnare il valore 4 alla variabile `my_var` con il comando

```
my_var <- 4
```

`@instructions`
A te la parola: completa il codice nell'editor in modo da assegnare il valore 42 alla variabile `x` nell'editor. Invia la risposta. Nota che quando chiedi a R di stampare `x`, appare il valore 42.

`@hint`
Guarda come il valore 4 è stato assegnato a `my_variable` nell'assegnazione dell'esercizio. Fai la stessa cosa nell'editor, ma ora assegna 42 alla variabile `x`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Assign the value 42 to x
x <- 

# Print out the value of the variable x
x
```

`@solution`
```{r}
# Assign the value 42 to x
x <- 42

# Print out the value of the variable x
x
```

`@sct`
```{r}
ex() %>% check_object("x", undefined_msg = "Assicurati di definire una variabile `x`.")  %>% check_equal(incorrect_msg = "Assicurati di assegnare il valore corretto a `x`.")

success_msg("Ottimo lavoro! Hai notato che R non stampa il valore di una variabile nella console quando fai l'assegnazione? `x <- 42` non ha generato alcun output, perché R presume che avrai bisogno di questa variabile in futuro. Altrimenti non avresti memorizzato il valore in una variabile, giusto? Procedi al prossimo esercizio!")
```

---

## Assegnazione delle variabili (2)

```yaml
type: NormalExercise
key: c5944b90eb
xp: 100
skills:
  - 1
```

Supponiamo di avere un cesto di frutta con cinque mele. Come analista di dati in formazione, vuoi memorizzare il numero di mele in una variabile con il nome `my_apples`.

`@instructions`
- Digita il seguente codice nell'editor: `my_apples <- 5`. Questo assegnerà il valore 5 a `my_apples`.
- Scrivi: `my_apples` sotto il secondo commento. In questo modo verrà stampato il valore di `my_apples`.
- Invia la tua risposta e guarda l'output: vedrai che viene stampato il numero 5. Quindi R collega ora la variabile `my_apples` al valore 5.

`@hint`
Ricorda che se vuoi assegnare un numero o un oggetto a una variabile in R, puoi usare l'operatore di assegnazione `<-`. In alternativa, puoi utilizzare `=`, ma `<-` è largamente preferito dalla comunità di R.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Assign the value 5 to the variable my_apples


# Print out the value of the variable my_apples

```

`@solution`
```{r}
# Assign the value 5 to the variable my_apples
my_apples <- 5

# Print out the value of the variable my_apples
my_apples
```

`@sct`
```{r}
ex() %>% check_object("my_apples", undefined_msg = "Per favore, assicurati di definire una variabile `my_apples`.") %>% check_equal(incorrect_msg = "Assicurati di assegnare il valore corretto a `my_apples`.")

ex() %>% check_output_expr("my_apples", missing_msg = "Hai detto esplicitamente a R di stampare la variabile `my_apples` nella console?")

success_msg("Ottimo! Continua con l'esercizio successivo!")
```

---

## Assegnazione variabile (3)

```yaml
type: NormalExercise
key: 1c1bd25045
xp: 100
skills:
  - 1
```

Ogni cesto di frutta gustoso ha bisogno di arance, quindi decidi di aggiungere sei arance. In qualità di analista di dati, il tuo riflesso è quello di creare immediatamente la variabile `my_oranges` e assegnarle il valore 6. Successivamente, devi calcolare quanti pezzi di frutta hai in totale. Dal momento che hai dato dei nomi significativi a questi valori, ora puoi codificare il tutto in modo chiaro: 

```
my_apples + my_oranges
```

`@instructions`
- Assegna a `my_oranges` il valore 6.
- Aggiungi le variabili `my_apples` e `my_oranges` e fai in modo che R stampi semplicemente il risultato.
- Assegna il risultato della somma di `my_apples` e `my_oranges` a una nuova variabile `my_fruit`.

`@hint`
`my_fruit` è solo la somma di `my_apples` e `my_oranges`. Puoi usare l'operatore `+` per sommare i due valori e `<-` per assegnare il valore alla variabile `my_fruit`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Assign a value to the variables my_apples and my_oranges
my_apples <- 5


# Add these two variables together


# Create the variable my_fruit

```

`@solution`
```{r}
# Assign a value to the variables my_apples and my_oranges
my_apples  <- 5
my_oranges <- 6

# Add these two variables together
my_apples + my_oranges

# Create the variable my_fruit
my_fruit <- my_apples + my_oranges
```

`@sct`
```{r}
msg <- "Hai usato `my_fruit <- my_apples + my_oranges` per creare la variabile `my_fruit`?"
ex() %>% check_object("my_apples") %>% check_equal(incorrect_msg = "Mantieni la riga che assegna 5 a `my_apples`.")

ex() %>% check_object("my_oranges") %>% check_equal(incorrect_msg = "Mantieni la riga che assegna 6 a `my_oranges`.")

ex() %>% check_output_expr("my_apples + my_oranges",missing_msg = "Assicurati di stampare il risultato dell\'aggiunta di `my_apples` e `my_oranges`. L\'esempio di codice nella descrizione ti dà già la risposta a questa istruzione!")

ex() %>% check_object("my_fruit", undefined_msg = msg)  %>% check_equal(incorrect_msg = msg)
success_msg("Ben fatto! Il grande vantaggio di fare calcoli con le variabili è la riusabilità. Se cambi solo `my_apples` per essere uguale a 12 invece di 5 e riesegui lo script, `my_fruit` si aggiornerà automaticamente. Continua al prossimo esercizio.")
```

---

## Mele e arance

```yaml
type: NormalExercise
key: 915fcc7c99
xp: 100
skills:
  - 1
```

La conoscenza comune ti dice di non sommare mele e arance. Ma ehi, è quello che hai appena fatto, no :-)? Le variabili `my_apples` e `my_oranges` contenevano entrambe un numero nell'esercizio precedente. L'operatore `+` funziona con le variabili numeriche in R. Se davvero provassi a sommare <span translate="no">"apples"</span> e <span translate="no">"oranges"</span>e avessi assegnato un valore di testo alla variabile `my_oranges` (vedi l'editor), staresti cercando di assegnare l'aggiunta di una variabile numerica e di una variabile carattere alla variabile `my_fruit`. Questo non è possibile.

`@instructions`
- Invia la risposta e leggi il messaggio di errore. Assicurati di capire perché non ha funzionato.
- Regola il codice in modo che R sappia che hai 6 arance e quindi un cesto di frutta con 11 pezzi di frutta.

`@hint`
Devi assegnare il valore numerico `6` alla variabile `my_oranges` invece del valore di carattere `"six"`. Nota che le virgolette sono utilizzate per indicare che `"six"` è un carattere.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Assign a value to the variable my_apples
my_apples <- 5 

# Fix the assignment of my_oranges
my_oranges <- "six" 

# Create the variable my_fruit and print it out
my_fruit <- my_apples + my_oranges 
my_fruit
```

`@solution`
```{r}
# Assign a value to the variable my_apples
my_apples <- 5  

# Fix the assignment of my_oranges
my_oranges <- 6

# Create the variable my_fruit and print it out
my_fruit <- my_apples + my_oranges 
my_fruit
```

`@sct`
```{r}
ex() %>% check_error(incorrect_msg = "Puoi farlo impostando la variabile `my_oranges` su un valore numerico, non una stringa!")

ex() %>% check_object("my_apples") %>% check_equal(incorrect_msg = "Assicurati che `my_apples` contenga ancora `5`.")

ex() %>% check_object("my_oranges") %>% check_equal(incorrect_msg = "Assicurati che `my_oranges` sia uguale a `6`.")

ex() %>% check_object("my_fruit") %>% check_equal(incorrect_msg = "Il valore di `my_fruit` non è corretto. Dovrebbe essere 11, la somma di `my_apples` e `my_oranges`.")

ex() %>% check_object("my_fruit")%>% check_or(
  check_code(.,"my_fruit\\s*<-\\s*my_apples\\s*\\+\\s*my_oranges", missing_msg = "Hai creato `my_fruit` come la somma delle variabili `my_apples` e `my_oranges`?", append=F),
  check_code(.,"my_fruit\\s*<-\\s*my_oranges\\s*\\+\\s*my_apples", missing_msg = "Hai creato `my_fruit` come la somma delle variabili `my_apples` e `my_oranges`?", append=F)
 )
ex() %>% check_output_expr("my_fruit", missing_msg = "Non rimuovere la riga che stampa `my_fruit`.")

success_msg("Fantastico, continua così! Passa al prossimo esercizio.")
```

---

## Tipi di dati di base in R

```yaml
type: NormalExercise
key: 0f23107394
xp: 100
skills:
  - 1
```

R lavora con numerosi tipi di dati. Alcuni dei tipi più semplici per iniziare sono:

- I valori decimali come `4.5` sono chiamati **numerici**.
- I numeri interi come `4` sono chiamati **numeri interi**. Gli interi sono anche numeri.
- I valori booleani (`TRUE` o `FALSE`) sono chiamati **logici**.
- I valori di testo (o stringhe) sono chiamati **caratteri**.

Nota che le virgolette nell'editor indicano che `"some text"` è una stringa.

`@instructions`
Cambia il valore di:

- `my_numeric` a `42`.
- `my_character` a `"universe"`. Le virgolette indicano che `"universe"` è un carattere.
- `my_logical` a `FALSE`.

Nota che R è sensibile alle maiuscole!

`@hint`
Sostituisci i valori nell'editor con quelli forniti nell'esercizio. Ad esempio:
`my_numeric <- 42` assegna il valore 42 alla variabile `my_numeric`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Change my_numeric to be 42
my_numeric <- 42.5

# Change my_character to be "universe"
my_character <- "some text"

# Change my_logical to be FALSE
my_logical <- TRUE
```

`@solution`
```{r}
# Change my_numeric to be 42
my_numeric <- 42

# Change my_character to be "universe"
my_character <- "universe"

# Change my_logical to be FALSE
my_logical <- FALSE
```

`@sct`
```{r}
ex() %>% check_object("my_numeric") %>% check_equal(incorrect_msg = "Hai correttamente cambiato la dichiarazione di `my_numeric` in modo che contenga il valore 42?")

ex() %>% check_object("my_character") %>% check_equal(incorrect_msg = "Hai correttamente cambiato `my_character` in `\"universe\"`? Non dimenticare le virgolette!")

ex() %>% check_object("my_logical") %>% check_equal(incorrect_msg = "Hai correttamente cambiato `my_logical` in `FALSE`? Tutte le lettere di `FALSE` devono essere maiuscole!")

success_msg("Ottimo lavoro! Continua con l'esercizio successivo.")
```

---

## Qual è il tipo di dati?

```yaml
type: NormalExercise
key: 99b549229d
xp: 100
skills:
  - 1
```

Ricordi che quando hai aggiunto `5 + "six"`, hai ricevuto un errore dovuto a una mancata corrispondenza dei tipi di dati? Puoi evitare queste situazioni imbarazzanti controllando prima il tipo di dati di una variabile. Puoi farlo con la funzione `class()`, come mostra il codice nell'editor.

`@instructions`
Completa il codice nell'editor e stampa anche le classi di `my_character` e `my_logical`.

`@hint`
Il codice che stampa il tipo di dati di `my_numeric` è già incluso; fai una cosa simile per `my_character` e `my_logical`.

`@pre_exercise_code`
```{r}
# no pec
```

`@sample_code`
```{r}
# Declare variables of different types
my_numeric <- 42
my_character <- "universe"
my_logical <- FALSE 

# Check class of my_numeric
class(my_numeric)

# Check class of my_character


# Check class of my_logical

```

`@solution`
```{r}
# Declare variables of different types:
my_numeric <- 42
my_character <- "universe"
my_logical <- FALSE

# Check class of my_numeric
class(my_numeric)

# Check class of my_character
class(my_character)

# Check class of my_logical
class(my_logical)
```

`@sct`
```{r}
msg <- "Non cambiare la dichiarazione delle variabili!"
ex()  %>% check_object("my_numeric", undefined_msg = msg)  %>%  check_equal(incorrect_msg = msg)

ex()  %>% check_object("my_character", undefined_msg = msg)  %>%  check_equal(incorrect_msg = msg)

ex()  %>% check_object("my_logical", undefined_msg = msg)  %>%  check_equal(incorrect_msg = msg)

patt <- "Hai incluso `class(%1$s)` per stampare il tipo di dato di `%1$s`?"
ex() %>% check_output_expr("class(my_numeric)",missing_msg = "Non rimuovere il codice che stampa il tipo di `my_numeric`.")

ex() %>% check_output_expr("class(my_character)",missing_msg = sprintf(patt, "my_character"))

ex() %>% check_output_expr("class(my_logical)",missing_msg = sprintf(patt, "my_logical"))
success_msg("Congratulazioni! Questo era l'ultimo esercizio di questo capitolo. Vai al prossimo capitolo per immergerti nel mondo dei vettori!")
```
